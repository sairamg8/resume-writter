# The e2e build's (vite build --mode e2e, Cypress) deployment settings: made-up, never a real person's.
# The demo-account specs sign in as this login (cypress/support/demoAccount.js).
VITE_DEMO_ACCOUNTS=owner@example.com
