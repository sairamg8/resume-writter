import { createContext } from 'react';

// Global section-heading style, set from resume.settings.headingStyle.
// Values: 'line' | 'underline' | 'box' | 'plain'
export const HeadingStyleContext = createContext('line');
