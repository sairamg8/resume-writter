# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Local Claude state (worktrees, caches) stays out; the project hooks and settings are shared.
.claude/*
!.claude/settings.json
!.claude/hooks/
.grok

# Test / playwright artifacts
playwright-report/
test-results/
qa-visual-compare/

# graphify's code-graph output: generated on a machine, stale as soon as src/ moves, and read by nothing
graphify-out/


# Local env (real Firebase keys) — copy .env.example instead
.env
.env.*
!.env.example
# The e2e build's made-up demo account, which the Cypress specs sign in as
!.env.e2e

# Yarn 4 (node-modules linker) local state
.yarn/*
!.yarn/patches
!.yarn/releases

# Cypress artifacts
cypress/downloads/
cypress/screenshots/
cypress/videos/

# Personal data kept out of the repo (e.g. private/sairam-resume.json — importable backup)
private/
