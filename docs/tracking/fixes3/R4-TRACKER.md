# Round 4 tracker (generated by docs/tracking/tools/r4_tracker.py — do not edit by hand)

70 rows · ⏸ 68 · ✅ 1 · ✖ 1 · none on master yet

| ID | Cluster | Status | Commits | Tests | Now | Left |
|---|---|---|---|---|---|---|
| R4-APP-01 | r4-app | ⏸ Fixed | `cd1fb2f`, `66b806b`, `5a4a698`, `3e8ef15` | tests/unit/ui-overlays.unit.mjs | Now: until a panel is measured useFloating makes it transparent and unclickable (opacity 0, pointer-events none, its entry animation paused) instead of visibility: hidden, which browsers refuse to focus. Popover's and MenuList's focus-on-open (same commit, before the position renders) lands: the issue view's Labels picker and the board toolbar's filters take typing at once, a Menu opened with ArrowDown or a click has its first item or list focused, and Escape closes the panel, not the IssueDialog. The review's hardening (66b806b) pauses the entry animation so a panel whose anchor ref reaches no element never fades in at the corner. |  |
| R4-APP-02 | r4-app | ⏸ Fixed | `d9e0924`, `66b806b` | tests/unit/ui-overlays.unit.mjs | Now: each toast() call is an instance; a replacement with the same id restarts its countdown from its full duration, so a template picked 6 s after another keeps its 'Template: … · Undo' toast for 8 s. The exit's removal drops only the toast that is leaving, so one shown with the same id within the 150 ms fade stays. |  |
| R4-APP-03 | r4-app | ⏸ Fixed | `188327e`, `ea3605e` | tests/unit/ui-shell.unit.mjs | Now: the drawer belongs to the history entry (its key and path) it was opened on and closes during render on any navigation; Back to the page it was opened on keeps it closed, and so does an address typed into the bar (HashRouter's shared 'default' key; the review's follow-up ea3605e). |  |
| R4-APP-04 | r4-app | ⏸ Fixed | `188327e` | tests/unit/ui-shell.unit.mjs | Now: same root cause and fix as R4-APP-03 — a Link to the current address replaces the entry (a new key), so the drawer closes; Create project on /boards closes it before the create dialog opens. |  |
| R4-APP-05 | r4-app | ⏸ Fixed | `7e26f8e`, `6d4b945` | tests/unit/lazy-page.unit.mjs | Now: the session mark is the list of pages that reloaded the tab (JSON); a failing page reloads once if it is not listed, only its own load removes its name, and offline nothing is marked. A page failing under a shell that loads reloads once, then shows the ErrorBoundary; one page's mark never stops another page's reload after a deploy (the review found the first version's single mark did). An older build's '1' names no page and is cleared by any load. The existing tests that set or read the single mark now use the list. |  |
| R4-APP-06 | r4-app | ⏸ Fixed | `83f6b38` | tests/unit/ui-shell.unit.mjs | Now: WorkspaceLayout reads (min-width: 768px) and closes the drawer once it matches, so the CSS-hidden aria-modal drawer unmounts after its exit and [, c, / and ? work again; narrowing again does not reopen it. |  |
| R4-APP-07 | r4-app | ⏸ Fixed | `92f2abd`, `f0600fd` | tests/unit/ui-shell.unit.mjs | Now: offsets live in a module-level Map for the tab's life, filed under each entry's key and path, and the shell files the shown entry's offset when it unmounts; Back from / returns the list to where it was scrolled. The review's follow-up keys by path too, so a typed address (HashRouter's 'default' key) never takes another page's offset. A reload still starts every page at the top (the map is in memory). |  |
| R4-APP-08 | r4-app | ⏸ Fixed | `59b2e3c` | tests/unit/legal-footer.unit.mjs | Now: the footer link to the page shown scrolls the window to its top and navigates nowhere (no second entry, Back leaves in one press); the other link still opens the other page. |  |
| R4-APP-09 | r4-app | ⏸ Fixed | `3f5441d` | tests/pdf/80-dashboard-import-read-error.test.mjs | Now: showImportError clears the running timer before it shows a message and starts that message's own (4 s, or 8 s for a document import); the timer is cleared on unmount. A failed PDF then a bad .json 6 s later keeps the second message its full 4 s. |  |
| R4-CL-01 | r4-cl | ⏸ Fixed | `16a3bf7`, `6dbf47c` | tests/pdf/r4cl-generator-letter-fields.test.mjs, tests/pdf/61-cover-letter-generator-recipient.test.mjs, cypress/e2e/28-writing-helpers.cy.js | Each opening of Auto-Generate reads the letter's own Recipient Name and Company (a letter with no name is greeted "Dear Hiring Team,", no more "Hiring Manager" default), so a typed recipient and the title typed for them survive Apply and the body names the company the block prints; Apply writes the company whole, so a blank generator Company clears the old one. Product call: the Target Role is not prefilled (the letter stores no role, only a subject). |  |
| R4-CL-02 | r4-cl | ⏸ Fixed | `bcb98e8` | tests/unit/ats-job-keywords.unit.mjs | The job-description scanner keeps an apostrophe (straight or curly) inside its word: "You'll", "we're", "don't" are the stop words they are, any other contraction is dropped, "Stripe's" reads as "Stripe" and "O'Reilly" stays whole; the résumé corpus reads curly apostrophes as straight too. |  |
| R4-CL-03 | r4-cl | ⏸ Fixed | `2d0d6b1` | tests/pdf/r4cl-ats-jd-kept.test.mjs | The pasted job description is kept for the browser tab's session (sessionStorage, key cpwtcv_ats_jd:<résumé id>, via useSessionState) and the ATS panel is keyed by the résumé id, so the posting and its Match % survive a trip to another tab, and another résumé's scanner starts empty. |  |
| R4-CL-04 | r4-cl | ⏸ Fixed | `76d248e`, `26ac1f4` | tests/pdf/57-optimizer-statement.test.mjs | statementRange reads a paragraph or list item that holds a <br> line by line, as bare text already was: the STAR Optimizer opens on the caret's line and Apply replaces only it; a caret just before a <br> is on the line it ends. Product call: a list item split by Shift+Enter is read line by line too, so it is never glued. | A <br> nested inside an inline wrapper (<p><b>A<br>B</b></p>) is still read as one statement (low; rare). |
| R4-CL-05 | r4-cl | ✖ Not a bug | `5f99091`, `78582e9` | tests/unit/bullet-optimizer-autofix.unit.mjs | /\b(did)\b/ cannot match inside "didn't": there is no word boundary between "did" and "n", so Auto-Fix never wrote "deliveredn't". A lookahead fix (5f99091) was proven unnecessary by failfirst (its test passed without it) and reverted (78582e9); the test stays as a guard. Reviewer B checked every weak phrase: none can precede an apostrophe in realistic text. |  |
| R4-CL-06 | r4-cl | ⏸ Fixed | `0cd1320`, `0fdeffa` | tests/pdf/r4cl-parser-grouped-roles.test.mjs | With "Group roles by company" on, printedJobs tags each later role of a group with its first role (groupLead), and jobFields gives it that role's company outcome, and its location outcome where the location is the shared one the employer header prints; a role's own differing location is read under it, and a grouped role with no title is read just after the role before it. |  |
| R4-CL-07 | r4-cl | ⏸ Fixed | `e1a79b7`, `0a014a7` | tests/pdf/r4cl-optimizer-chips.test.mjs | A power verb chip replaces a leading action verb, a multi-word verb phrase Auto-Fix writes ("Contributed to") or a leading weak phrase ("Responsible for" → "Spearheaded"); otherwise it goes before the first word, lowercased when it is an ordinary capitalised word ("AWS" stays). Line breaks, bullet marks and quotes are kept (insertActionVerb in bulletOptimizer.js). | A proper noun leading the statement ("Kubernetes cluster…") is lowercased when the verb is prepended (low). |
| R4-CL-08 | r4-cl | ⏸ Fixed | `e1a79b7` | tests/pdf/r4cl-optimizer-chips.test.mjs | A metric chip goes before the statement's closing punctuation ("…checkout service by 35%."), and on an empty statement it is the phrase alone, no leading space (insertMetric). | A statement ending in an abbreviation's dot ("etc.") loses it before the metric (low). |
| R4-CL-09 | r4-cl | ⏸ Fixed | `3b75d22`, `7690b90` | tests/unit/bullet-optimizer-ats-agree.unit.mjs | hasMetric (the optimizer's and the ATS score's one rule) reads calendar years, decades, year ranges and month/year dates ("in 2021", "the 1990s", "2019–22", "05/2021") as no number; "2000+", "$2019", "2019%" and "2020k" still count. | A bare count that looks like a year ("Served 2000 users") now reads as no metric — the trade-off of the finding's own rule. "FY2021" still counts (pre-existing: the digit rule checks only the first digit). |
| R4-CL-10 | r4-cl | ⏸ Fixed | `28b931d` | tests/unit/ats-checker.unit.mjs | extractBulletsFromItem reads a list description through parseRichText, as the PDF prints it: every list item, nested or not, is its own bullet, and an item's continuation paragraph stays with it. | Text after a nested list inside the same outer item joins the nested bullet, not the outer one (low, count unchanged). |
| R4-CL-11 | r4-cl | ⏸ Fixed | `4c84973` | tests/unit/ats-standardize-headings.unit.mjs, tests/unit/ats-checker.unit.mjs | needsAtsTitle also asks for a shown entry on the résumé's template, the rule has_exp/has_edu/has_skills use, so an empty or all-hidden section is neither flagged as a non-standard heading nor renamed by Standardize Headings (the panel passes resume.template). |  |
| R4-DSN-01 | r4-dsn-pdf | ⏸ Fixed | `f39d48c` | tests/pdf/r4dsn-saved-design-engine.test.mjs | A saved design whose file stores its engine as "Modern" or " modern " is read as 'modern' (ownDesign returns templateId(engine)), so it lists as "Mine · Your design · Modern", is marked selected, and the editor and /new open instead of throwing in pickerCards. |  |
| R4-DSN-02 | r4-dsn-pdf | ⏸ Fixed | `42b619e`, `f71190a` | tests/pdf/r4dsn-design-font-over-custom.test.mjs, tests/pdf/93-template-picker-copy.test.mjs | Picking a design or template that brings a font (Harbor, Academic, Chronicle, Linen…) over a custom Google font clears customFont (new withStyle in templatePresets.js), as Reset and a font button do; a template bringing no font keeps it, a saved design brings its own, and the picker's card looks and Serif chip are built the same way. It clears only where a custom font is set, so a switch adds no setting it did not hold (93-template-picker-copy A5). |  |
| R4-DSN-03 | r4-dsn-pdf | ⏸ Fixed | `2d4e4f0` | tests/pdf/r4dsn-new-resume-look-colours.test.mjs | With no résumé yet, /new's looks are built over defaultSettings('classic') and the starters' Template row over getStarterSettings('classic'), so each picture is drawn in #374151, the accent the created résumé gets, not each template's default blue/navy/red. |  |
| R4-DSN-04 | r4-dsn-pdf | ⏸ Fixed | `6473dfb`, `77bbc1c` | tests/pdf/r4dsn-custom-font-check.test.mjs, tests/unit/r4pdf-fontsource-retry.unit.mjs, tests/pdf/91-design-typography.test.mjs | checkFont returns offline:true when the lookup failed just now (fetch rejected, timed out, 5xx/429) and the panel says it could not check, keeping the typed name; a remembered 404 still says not found. applyCustomFont captures the résumé id, font and customFont and does not select the font if any changed while it waited (it is still kept as a chip, also when the panel closed meanwhile). 91-design-typography's stub now answers an unknown font with jsDelivr's real 404 (it sent no status, which now reads as could-not-check). |  |
| R4-DSN-05 | r4-dsn-pdf | ⏸ Fixed | `61c196f`, `5708691` | tests/pdf/r4dsn-colour-swatches.test.mjs, cypress/e2e/04-design.cy.js | The Accent, Name and Job title swatches hold the colour resolveTemplateSettings resolves for the template (white on Modern's banner, Gridline's #1e40af), as the Text swatch has since R9-7; so they show what the PDF prints and pure black fires a change wherever the printed colour is not black. The labels still read "Template default" when unset. Cypress 04-design's NB-1 test picked #ffffff for the Sidebar's Name colour, now already the swatch's value (no change fires): it picks #fafafa, same intent (5708691). |  |
| R4-DSN-06 | r4-dsn-pdf | ⏸ Fixed | `8b19154`, `5f49105` | tests/unit/r4dsn-gallery-category.unit.mjs | A gallery category that offers no chip any more (My designs after every saved design is deleted) is All, pressed, and is forgotten, so a design saved later does not reopen the gallery filtered to My designs. |  |
| R4-DSN-07 | r4-dsn-pdf | ⏸ Fixed | `cfeb594` | tests/unit/r4dsn-gallery-count.unit.mjs | "Browse templates (N)" counts every card the gallery lists, saved designs included, so it matches the gallery's "Templates (N)" title (product call: one total, not "27 + 2 yours"). |  |
| R4-PDF-01 | r4-dsn-pdf | ⏸ Fixed | `71580c1` | tests/pdf/r4pdf-font-notice.test.mjs | Page pictures (gallery, dashboard cards, /new) build with { reportFont: false }: on the worker path they do not bump lastBuild and their reply never calls setFontFallback; on the main thread the flag goes through renderResumePdf/renderCoverLetterPdf to resolvePdfFonts, which then neither counts nor sets the notice. The preview, Export PDF, 1-Page Fit, the ATS view and the public page still report. |  |
| R4-PDF-02 | r4-dsn-pdf | ⏸ Partly fixed | `514b6c2` | tests/unit/r4pdf-fontsource-retry.unit.mjs | fetchMetadata remembers only a 404; a 429, 5xx or network failure is asked again by the next build, so Inter or a custom font prints once jsDelivr recovers and checkFont stops calling a real font missing. | The fix_hint's optional related case: prepareFonts permanently gives a face that failed once (e.g. a bold on a network hiccup) a donor face's data; resetting donor-filled faces needs tracking which were filled and would refetch truly missing faces every build — not a small change, left. |
| R4-PDF-03 | r4-dsn-pdf | ⏸ Fixed | `9234717`, `5c68032` | tests/unit/r4pdf-photo-retry.unit.mjs, tests/pdf/80-photo-url.test.mjs | A photo URL whose fetch failed for a passing reason (network, timeout, 5xx, 408, 429) prints none for now (printableNow null, the panel still says so) and is fetched again: by the next build if the browser was offline, otherwise a minute on or at the browser's 'online' event, so a slow or refusing host does not hold every preview build. A 4xx or undecodable image is still remembered. 80-photo-url's once-a-session check now uses a 404 (a rejected fetch is the transient case this row fixes). |  |
| R4-PDF-04 | r4-dsn-pdf | ⏸ Fixed | `3b9afcf` | tests/pdf/r4pdf-sidebar-no-name.test.mjs | The two-column Sidebar prints 'Your Name' in its column when Full Name is empty (also used for its fit-to-column size), as every other template, its Word export and its letterhead do. |  |
| R4-ED-01 | r4-ed | ⏸ Fixed | `d84cd90` | tests/pdf/99-language-proficiency-select.test.mjs | A language's Proficiency select shows the stored level: a 'Not set' option holds '', and a level the five do not name ('Conversational', 'C1') is listed as its own option and selected, so the editor shows what the résumé prints and every level can be picked in one step. New languages still start at Professional. The supplied patch was reviewed by a second agent (held) and landed as is. |  |
| R4-ED-02 | r4-ed | ⏸ Fixed | `300b136`, `997281a`, `d537224` | tests/pdf/99-rich-text-no-images.test.mjs | A paste or a drop into a rich-text field is always cancelled and only its text inserted (sanitized), so a copied screenshot or an image file inserts nothing; emit() removes any img/picture/media element before storing; and a field already stored with a base64 data: URL inside a tag is stored again without it when shown (a value without one writes nothing). A paste event with no clipboardData is left to the browser. |  |
| R4-ED-03 | r4-ed | ⏸ Fixed | `e068660` | tests/pdf/99-custom-date-period.test.mjs | Product call: a Custom entry's Date / Period that the month picker cannot hold (an imported 'Jan 2020 – Mar 2021', '2019 – 2021', 'Summer 2020') is a text box showing it as it prints, and stays one while the entry is on screen; picker-shaped and empty dates keep the picker. MonthPicker itself is unchanged. | Other month pickers (Experience/Education Start/End, Award and Certificate dates) still cut free text they cannot read; importers store single dates there, so low risk. |
| R4-ED-04 | r4-ed | ⏸ Fixed | `ca5db38`, `ccc5de8`, `d537224` | tests/pdf/99-rich-text-drag-move.test.mjs | A drag of selected text inside a rich-text field is moved by the editor: dragstart records the selection and marks the drag with the editor's own id; the drop deletes the source (execCommand 'delete', undoable) and inserts the text sanitized at the drop point. A drop with no mark only inserts, even if an earlier drag lost its dragend; a drag with no selection is not a move. Firefox finds the drop point with caretPositionFromPoint. A drag from one field to another still copies. |  |
| R4-ED-05 | r4-ed | ⏸ Fixed | `e1ede9d`, `6d1f0d7` | tests/pdf/99-header-border-width-box.test.mjs | Header Bottom Border's Thickness box is typed with useTypedNumber like the other stepper boxes: it can be emptied, holds what is typed, and writes once on Enter or leaving, clamped to 1–12 pt; Escape and click-in-and-out write nothing. |  |
| R4-ED-06 | r4-ed | ⏸ Fixed | `5116abc`, `a43fabe` | tests/pdf/99-remove-untouched-entry.test.mjs | An entry is untouched when every text field is empty or still its new-entry (NEW_ITEM) value and it has no non-empty bullets; such an entry, a fresh Language row at 'Professional' included, is deleted without 'Delete this entry?'. |  |
| R4-ED-07 | r4-ed | ⏸ Fixed | `8c009c7`, `a43fabe`, `56c69e3` | tests/pdf/99-new-entry-open.test.mjs, cypress/e2e/02-editor.cy.js, cypress/e2e/19-regressions-ids.cy.js | Product call: a card whose entry is untouched (the R4-ED-06 rule) opens when it mounts, so the entry Add makes and a new section's first entry show their fields at once; entries with content stay collapsed. Two Cypress specs that clicked 'New Entry' open were updated to the new behaviour. |  |
| RES-R2-137 | r4-exp | ⏸ Fixed | `39b3bf7`, `a96cebb` | tests/pdf/92-word-header-band.test.mjs, tests/pdf/35-word-name-title-colors.test.mjs, tests/pdf/35-word-summary-look.test.mjs, tests/pdf/33-word-contact-marks.test.mjs, tests/pdf/47-header-title-contacts-gap.test.mjs, tests/pdf/83-templates-ui-leftovers.test.mjs | The Word résumé now prints Modern's and the two-column Sidebar's header on their band, in the colours the PDF prints there. In the Word letter at Right of Name, the contacts sit beside the name. A 2 Grid there is as wide as its two cells, against the right margin. When it would wrap a name and title that fit on their lines, it goes under the name, as in the PDF. The Word tooltip names what the .docx still leaves out: Banner's and Banded's headers and the Sidebar's side column print on the white page, and the designed layouts' rules and bars are left out. The letter prints no photo. Fail-first: CI 36230222904 (39b3bf7) and 36230632974 (a96cebb). | Known differences from the PDF, kept as the patch's product calls: the Sidebar's Word band is full-width with the contacts on one line (the PDF uses a 38 % column with labelled contacts). Word does not draw Banner's rule on the letter's band. Word estimates widths, so a very wide Name Font can still wrap in the name cell. |
| R4-EXP-01 | r4-exp | ⏸ Fixed | `415d45f`, `4819a0f` | tests/unit/r4-exp-markdown-escape.unit.mjs | The Markdown export now backslash-escapes \ ` * _ [ ] < > ~ in every user-text field: rich text, headings, meta lines, links, references, lists, the name, title, contacts and section titles. A line that starts with #, -, +, = or '3.' keeps it as text. Each field is trimmed before ** or * go around it, so '<DataGrid>', '__init__' and 'Acme Corp ' print as typed. The Markdown import takes the new \< \~ \= escapes back off. Fail-first: CI 36230489859 and 36230774136. |  |
| R4-EXP-02 | r4-exp | ⏸ Fixed | `46eceb6`, `4819a0f` | tests/pdf/98-exp-letter-text-contacts.test.mjs | The Cover Letter Text now prints a labelled website, LinkedIn or GitHub as 'LinkedIn (linkedin.com/in/jdoe)', using the Link URL override's address when one is set. A Link URL the PDF would not follow prints as the label alone. Fail-first: CI 36230489859 and 36230774136. |  |
| R4-EXP-03 | r4-exp | ⏸ Fixed | `6f0a799`, `e08e7f4` | tests/unit/r4-exp-ats-education.unit.mjs | The ATS text's education line now reads 'BSc, Computer Science - MIT', the PDF's and the Markdown's form, and 'Computer Science - MIT' with no degree. Product call: the comma form rather than 'BSc in Computer Science', because the text import reads the comma form back and 'X in Y' would have broken 99-import-roundtrip. Fail-first: CI 36230489859. |  |
| R4-EXP-04 | r4-exp | ⏸ Fixed | `844ffe6` | tests/pdf/98-exp-word-interests.test.mjs | Word now splits each Interests entry at its commas, trims the parts and leaves blanks out, as the PDF's chips, the ATS text and the Markdown do: 'Chess,Hiking,' and 'Chess ,  Go' print 'Chess, Hiking, Chess, Go'. Fail-first: CI 36230489859. |  |
| R4-EXP-05 | r4-exp | ✅ Already fixed | `39b3bf7` | tests/pdf/83-templates-ui-leftovers.test.mjs | RES-R2-137's tooltip rewrite (39b3bf7) fixed this: the résumé's Export Word hint no longer mentions a photo, and 83 asserts doesNotMatch(/photo/). That assertion fails on the base's 'no photo' title (failfirst CI 36230222904). The letter's hint 'without its photo' is true. |  |
| R4-EXP-06 | r4-exp | ⏸ Fixed | `94e179e` | tests/pdf/98-exp-word-reference-links.test.mjs | Word now takes a reference's mailto: and tel: links from contactHref, the PDF's check. 'Available on request' prints as plain text, where Word used to link it to an empty 'tel:'. A dialable phone and an e-mail address are still linked. Fail-first: CI 36230489859. |  |
| R4-EXP-07 | r4-exp | ⏸ Fixed | `869845f`, `4819a0f` | tests/unit/r4-exp-export-filename.unit.mjs | Every exported file name (the Export menu, the ATS tab and the public link's PDF) now turns / \ : * ? " < > \| and control characters into '-'. A run of - and _ collapses to one, leading and trailing dots and separators are removed, and each part is capped at 60 characters, never splitting an emoji. 'UI/UX Designer' saves as Jane_Doe_UI-UX_Designer everywhere. Fail-first: CI 36230489859 and 36230774136. |  |
| R4-IMP-01 | r4-imp | ⏸ Fixed | `57be086`, `fbbad00` | tests/unit/import-cert-award-lists.unit.mjs | A Certifications or Awards section that opens with a list item is a list of them: one entry per bullet, its trailing date read ("• AWS Certified Solutions Architect – 2022"), a date or named-field line under an item joining it. A certificate's text with no field for it goes to "Additional Information" as an item titled with the certificate's name, never to a `description` the editor, PDF and Word never show. |  |
| R4-IMP-02 | r4-imp | ⏸ Fixed | `a926344`, `1a63dc3`, `fbbad00` | tests/unit/import-markdown-links.unit.mjs, tests/unit/import-text.unit.mjs | A Markdown link keeps its address: a labelled contact ("[LinkedIn](https://…)") fills its field with the address and keeps the label as its Display label (`linkedinLabel`…); a link in body text reads "label (address)"; a `###` title's link address becomes a field at the line's end (a project's URL; another type keeps it in its description); a certificate's labelled link line fills url and urlLabel. Product call: the label is restored as the Display label, so the app's own export round-trips exactly. One stale expectation in import-text.unit.mjs (the link text in a list item) now includes the address, as the finding asks. |  |
| R4-IMP-03 | r4-imp | ⏸ Fixed | `63b087d` | tests/unit/import-docx-heading-levels.unit.mjs | The Word reader keeps each Heading's level: the top level used starts sections, deeper levels are entries (as Markdown's ###), and a lone top-level first heading over deeper ones is the name. An entry-marked date line under an entry joins its header, and a custom heading with nothing under it keeps its words in "Additional Information". |  |
| R4-IMP-04 | r4-imp | ⏸ Fixed | `13214b8`, `44c8921`, `d28212b` | tests/unit/import-docx-textbox.unit.mjs | docxXmlLines is a paragraph-stack reader: every <mc:Fallback> copy is skipped (nested or self-closing), so a text box is read once; the anchoring paragraph's own text (before and after the box) is kept, read before its boxes, or after them when it is a heading. |  |
| R4-IMP-05 | r4-imp | ⏸ Fixed | `4902804`, `bed19a3`, `fbbad00` | tests/unit/import-docx-header.unit.mjs | The header the first page shows (the first section's 'first' header with <w:titlePg/>, else its default one) is read before the body, with its own hyperlinks; lines the body opens with too are dropped, as is header furniture (Curriculum Vitae, Résumé, Confidential, Page N, a lone number, a '– Resume' or tab 'Page 1' tail). The app's own running header (default header, titlePg, none on page 1) adds nothing. |  |
| R4-IMP-06 | r4-imp | ⏸ Fixed | `c5fa613`, `979b6b8`, `af27a4d` | tests/unit/import-text-encoding.unit.mjs | Text and Markdown files are decoded by BOM (UTF-16 LE/BE), else strict UTF-8, else Windows-1252 — unless the bytes hold real UTF-8 letters around one stray bad byte, which stay UTF-8 (a single "�"). |  |
| R4-IMP-07 | r4-imp | ⏸ Fixed | `3e00b2b` | tests/unit/import-unused-fields.unit.mjs | A named field on an entry's header lines ("Technologies:", "Link:", "ID:", "GPA:", "Expires:", "Location:") that the entry type has no slot for leads its description as written; a certificate's goes to its "Additional Information" item. |  |
| R4-IMP-08 | r4-imp | ⏸ Fixed | `a7cd2e4`, `568b4fe`, `fbbad00` | tests/unit/import-heading-name-only.unit.mjs | Only a heading hint after the name makes a file 'hinted', so a Word file with only its name in Heading 1 finds its capitals sections. In a hinted file, an unmarked known title in capitals is a section too when it is not inside a marked entry and names a type the file does not mark itself (a job's **KEY ACHIEVEMENTS** stays in the job). |  |
| R4-IMP-09 | r4-imp | ⏸ Fixed | `ea0e470`, `764f35c`, `fbbad00` | tests/unit/import-markdown-role-groups.unit.mjs | markdownLines marks a heading deeper than the entry above it a 'role'; an undated one-field employer over role headings (the app's "Group roles by company" export) gives each role its own job with the employer as company and the group's place as default location. A title holding role and company, or a dated entry, is never an employer. |  |
| R4-IMP-10 | r4-imp | ⏸ Fixed | `489c32c`, `2138f79`, `fbbad00` | tests/unit/import-links-docx-pdf.unit.mjs, tests/pdf/imp-import-links.test.mjs | The PDF reader reads each page's Link annotations and the Word reader each hyperlink's target (document and header .rels): a label that is not the address reads "label (address)" and fills the contact with its Display label. A contact line pdf.js reads as one item (Contact style Bar/Bullet) gives each link only its own label's letters, never past the separator. Round trip through the app's own PDF (Icon and Bar styles) and Word with Display labels. |  |
| R4-IMP-11 | r4-imp | ⏸ Fixed | `0f1bcd6`, `764f35c`, `fbbad00` | tests/unit/import-languages-commas.unit.mjs | Languages split at commas and semicolons outside brackets: "English, Spanish, French" is three, "English (Native), Spanish (Fluent)" two with their levels. A level cell after a level, or a lower-case last cell, joins that level ("Full professional, C2", "Working knowledge, written"). |  |
| R4-IMP-12 | r4-imp | ⏸ Fixed | `bdd07fa` | tests/pdf/imp-import-busy.test.mjs | While a document is read the Dashboard's Import (and the demo account's Import menu) and the editor's Export menu say "Reading…" and are disabled; a second pick meanwhile is ignored (a ref catches two in one tick). A finished import still imports but navigates only if the page that started it is still mounted (and, in the editor, still on the same résumé). |  |
| R4-IMP-13 | r4-imp | ⏸ Fixed | `a6c502c` | tests/unit/import-pdf-password.unit.mjs | pdfLines turns pdf.js's PasswordException into "That PDF is password-protected. Save a copy without a password (or as a Word file) and import that."; the loading task is still destroyed; other errors keep their message. |  |
| R4-IMP-14 | r4-imp | ⏸ Fixed | `8f66889` | tests/unit/import-accept.unit.mjs, tests/pdf/99-import-ui.test.mjs | IMPORT_ACCEPT is '.json,.pdf,.docx,.txt,.text,.md,.markdown' (not .doc: that is only told to save as .docx). The 99-import-ui expectation and docs/knowledge/06-templates-export.md were updated to it. |  |
| R4-SYNC-01 | r4-sync-job | ⏸ Fixed | `8832c68`, `1cedd04` | tests/pdf/100-r4-sync-01-one-public-copy.test.mjs | Publish now reads the account's share record first and reuses the link it names, so two tabs or devices that both saw 'not published' make one copy. If the panel's own stale link differs from the recorded one, that copy is taken down in the same batch. Unpublish takes down the panel's copy and the recorded one, each only if it still exists, so an Unpublish from a stale panel no longer fails for good on firestore.rules. No rules change. | A narrow race is still open: two Publishes within one round trip can both read 'no record' and leave one orphan copy. Closing it fully needs runTransaction (see notes). |
| R4-SYNC-02 | r4-sync-job | ⏸ Fixed | `d2edebe` | tests/pdf/100-r4-sync-02-sync-dot-unreachable.test.mjs | When the browser is online but Firestore reports 'client is offline' (sync status 'offline'), the header icon now stays. It shows CloudOff and 'Cannot reach your account — changes saved locally, will retry'; before, the icon vanished. When the browser itself is offline, it still reads 'Offline — changes saved locally'. |  |
| R4-SYNC-03 | r4-sync-job | ⏸ Fixed | `0ab75b3`, `1cedd04` | tests/pdf/100-r4-sync-03-copy-print-settings.test.mjs | publicSnapshot now copies only the settings that print. It leaves out myDesigns and templatePreset, and drops customContactIcons entries for contacts hidden with their eye. publishedIsCurrent runs both sides through publicSnapshot and ignores dataVersion. So saving or deleting a design, a data migration, or a copy published before this fix no longer reads as 'changed since'. The copy still prints identically. |  |
| R4-SYNC-04 | r4-sync-job | ⏸ Fixed | `44decc3` | tests/pdf/100-r4-sync-04-too-large-message.test.mjs | The too-large error carries code 'too-large', and the share panel shows its message on its own. Network failures still get 'Publishing failed (…). Check your connection and try again.' |  |
| R4-SYNC-05 | r4-sync-job | ⏸ Fixed | `e0179e4` | tests/pdf/100-r4-sync-05-copied-label.test.mjs | The Copy button goes back from 'Copied' to 'Copy' whenever the panel opens, on Unpublish, and when a publish gives a new link. It keeps 'Copied' when 'Update the public copy' leaves the link the same, since that link was copied. No timer was added. |  |
| R4-SYNC-06 | r4-sync-job | ⏸ Fixed | `9a429a6` | tests/pdf/100-r4-sync-06-empty-sections.test.mjs | The public copy, and so 'What is public', now drops any section without a shown entry, matching the PDF's sectionPrints. The copy still prints the same as the résumé. |  |
| R4-JOB-01 | r4-sync-job | ⏸ Fixed | `08d0332` | tests/pdf/100-r4-job-01-details-resume.test.mjs | The Details box's Résumé row now shows the linked résumé's name, or 'Résumé deleted' when that résumé is gone (J-21). It shows 'None' only when nothing is linked. |  |
| R4-JOB-02 | r4-sync-job | ⏸ Fixed | `a0f0f19` | tests/pdf/100-r4-job-02-follow-up-fields.test.mjs | Product call: add editors rather than remove the displays. The job form and the Overview now edit Follow-up Date, Work Mode and Source ('— Not set —' = ''). The date formerly labelled 'Deadline / Follow-up' is now 'Deadline'. A follow-up date set in the form puts the job under the Summary's 'Follow-ups due' once that day comes. | Excitement is still not editable; nothing in the app displays it, so it was left alone. |
| R4-JOB-03 | r4-sync-job | ⏸ Fixed | `6bb0ad5` | tests/pdf/100-r4-job-03-closed-not-overdue.test.mjs | An on-hold, rejected or withdrawn job (!isOpen) no longer shows a passed deadline as late. This covers the board card's pill, the Details box's deadline and follow-up pills, the list row's red date (a variant the finding missed) and the Overview's 'Deadline has passed' warning. Open jobs are unchanged. |  |
| R4-JOB-04 | r4-sync-job | ⏸ Fixed | `63f18a1` | tests/pdf/100-r4-job-04-name-kept.test.mjs | The Overview applies the form's canSave rule. Blanking Company or Role (whitespace counts as blank) while the other is blank keeps the old value. Field now reopens on the current value rather than a refused draft. Either one can still be blanked while the other is set. |  |
