# Full audit 2026-09-23 — Low defects

> Part of the tracker: [../bug-status.md](../bug-status.md) → **Full audit (`R2-`)**. Status legend as there: ✅ fixed and pushed · ⏸ fixed, local · 🔴 open · ✖ not a bug (proof in the row).
> Verified: *Confirmed* = an independent second agent reproduced it at `5aafca8`–`5b6ecc0`; *Unverified* = the session limit stopped its check — the fix's fail-first test settles it.

| ID | Area | Severity | Kind | Status | Commit | Tests | Defect (where) | Verified |
|---|---|---|---|---|---|---|---|---|
| R2-071 | EditorHeader / rename | Low | bug | 🔴 Open | — | — | Editor rename box opens with a stale name, and leaving it writes the old name back over a rename made elsewhere (`src/pages/Editor.jsx:32, :71 (`useEffect(() => setResumeName(resume?.name \|\| ''), [resume?.id])`); src/components/EditorHeader.jsx:37 (click → setEditingName(true) without re-priming)`) Repro: Open résumé X in tab 1. In tab 2 (dashboard) rename X. In tab 1 click the name in the header and click away: the rename is undone everywhere. | Unverified |
| R2-072 | Routing / ErrorBoundary | Low | bug | 🔴 Open | — | — | ErrorBoundary never resets: after a crash, browser Back or any navigation stays on 'Something went wrong' (`src/AppRoutes.jsx:20-34; src/components/ErrorBoundary.jsx:10-51`) Repro: Trigger any render error on one page, then press the browser Back button: the error screen stays. | Unverified |
| R2-073 | Routing | Low | bug | 🔴 Open | — | — | No scroll reset on route change: Terms, Privacy and Job Tracker open at the previous page's scroll offset (`src/main.jsx:9 (HashRouter, no scroll handling); footer links at src/pages/Dashboard.jsx:254-255, TermsPage.jsx / PrivacyPage.jsx Footer; 'Job Tracker →' at Dashboard.jsx:222`) Repro: Scroll the dashboard to its footer and click 'Privacy Policy'. The policy opens near its end, not at '1. Overview'. From that page's footer click 'Terms': the Terms page opens at its bottom. | Unverified |
| R2-074 | PrivacyPage / TermsPage | Low | bug | 🔴 Open | — | — | Terms and Privacy back arrow calls navigate(-1): it does nothing on a direct visit and leaves the site when arrived from outside (`src/pages/PrivacyPage.jsx:10; src/pages/TermsPage.jsx:10`) Repro: Open https://<site>/#/privacy in a new tab and click the ← arrow: nothing happens. | Unverified |
| R2-075 | Routing / JobForm | Low | bug | ✅ Fixed | `7f4a3f8` | tests/pdf/67-job-form-save.test.mjs | /jobs/:id/edit with an unknown id shows an empty 'Edit Job Application' form whose Save silently discards what was typed (`src/pages/JobForm.jsx:34-35, :55-58; src/hooks/useJobStore.js:190-200`) Repro: Visit /#/jobs/nope/edit, fill in Company and Role, and click 'Save Changes': 'Job not found.' and nothing is saved. The same happens when the job is deleted in another tab while its edit form is open. **Now:** already fixed before the 2026-09-24 pass, by `7f4a3f8`: Fixed in Lane C (J-16): /jobs/:id/edit with an unknown id shows 'Job not found' instead of a form; a job deleted while its form is open keeps the input and offers to save it as new. | Unverified |
| R2-076 | Routing / Editor deep link | Low | bug | 🔴 Open | — | — | The ?tab= deep link is not validated (an unknown value leaves a blank editor panel) and is never updated, so a reload reopens the old tab (`src/pages/Editor.jsx:20-21, :28; src/components/EditorHeader.jsx:84-115`) Repro: Open #/resume/<id>?tab=foo: the left panel below the tab bar is blank. | Unverified |
| R2-077 | Performance / store persistence | Low | bug | 🔴 Open | — | — | Every keystroke serialises and writes the whole résumé store (all résumés, with base64 photos) to localStorage synchronously (`src/hooks/useResumeStore.js:99-116 (effect on [appState]: setItemWithRoom(STORAGE_KEY, JSON.stringify({...appState})))`) Repro: Keep several résumés with photos and type quickly in a field. Each character pays a full-store stringify and storage write on the main thread. | Unverified |
| R2-078 | ats / bulletOptimizer | Low | bug | 🔴 Open | — | — | The optimizer's tip names the suggested replacement as the weak phrase, and Auto-Fix capitalises verbs mid-sentence (`src/utils/bulletOptimizer.js:135 (uses alternatives[0] instead of phrase), :152-157 (replacement is always Capitalised)`) Repro: Open the optimizer on "Engineered 4 APIs; handled QA". The grey Tip tells you to replace "Resolved". | Confirmed |
| R2-079 | ats / AtsCheckerPanel handleOptimizeExperienceOrder | Low | bug | 🔴 Open | — | — | "Put Job Title First" rewrites hidden experience sections that the report never inspected (`src/components/AtsCheckerPanel.jsx:79-90`) Repro: Keep one visible company-first experience section and one hidden experience section set to Co. / Role. Click Put Job Title First, then un-hide the second section: it now leads with the role. | Confirmed |
| R2-080 | ats / AtsCheckerPanel, BulletOptimizerModal | Low | bug | 🔴 Open | — | — | The Copy buttons in the ATS panel and the optimizer fail silently (`src/components/AtsCheckerPanel.jsx:137-143; src/components/BulletOptimizerModal.jsx:51-56`) Repro: In a browser that denies clipboard-write, click ATS Check → Copy Text. Nothing happens and no error is shown. | Confirmed |
| R2-081 | ATS checker panel · add keyword | Low | bug | 🔴 Open | — | — | ATS tab '+' (add missing keyword) writes into a hidden skill entry, so the keyword never prints and stays missing (`src/components/AtsCheckerPanel.jsx:150-166 (handleAddMissingSkill appends to items[0] regardless of item.visible / hiddenFields)`) Repro: Hide the first skill group → ATS Check → paste a JD → click + on a missing keyword: the button flashes done, the keyword is still listed missing, and the PDF does not show it. | Unverified |
| R2-082 | Design panel · Sidebar single column | Low | bug | 🔴 Open (Colors half ✅ `0b5f6e2`) | `0b5f6e2` (Colors half) | tests/pdf/parity/01-offered.test.mjs | Sidebar 'Single · ATS-safe': Header Text Color and Sidebar Background are still offered but change nothing; Typography and Headings notes describe a side column that is not printed (`src/components/DesignPanelColors.jsx:82, :118 (gated on resume.template === 'sidebar'); src/components/DesignPanelTypography.jsx:154 and src/components/DesignPanelHeadings.jsx:40 (template === 'sidebar'); src/components/DesignPanel.jsx:28 (current = templateId(resume.template), not single-column aware)`) Repro: Design → Template Sidebar → Layout 'Single · ATS-safe' → Colors → pick a Sidebar Background or Header Text Color: the preview does not change. **Now (partly):** the Colors half is fixed in `0b5f6e2`: Header Text Color follows `letterheadLook(t).band` and Sidebar Background follows `headerTemplateId(t) === 'sidebar'`, so on Sidebar · Single · ATS-safe neither is offered (the parity test's `01-offered` failed both on the old code). **Still open:** the Typography and Headings notes (`DesignPanelTypography.jsx:154`, `DesignPanelHeadings.jsx:40`) still describe a side column the single layout does not print. | Unverified |
| R2-083 | Design panel · Spacing presets | Low | bug | 🔴 Open | — | — | Line Height shows a different value than prints after Smart Page Fit presets; presets are only 'tested' by a Playwright test that asserts nothing about spacing (`src/components/DesignPanel.jsx:164-200 (presets write lineHeightValue 1.35 / 1.65); src/components/DesignPanelShared.jsx:60 (display uses toFixed(1)); tests/playwright/pdf-typography-spacing.spec.mjs:51`) Repro: Design → Spacing → Spacious: Line Height reads 1.6. Export: the PDF uses 1.65. | Unverified |
| R2-084 | Dashboard / ResumeCard / Editor rename | Low | bug | 🔴 Open | — | — | Renaming, on a dashboard card or in the editor header, starts from a stale name and reverts a rename made elsewhere; a no-op rename still counts as an edit (`src/components/ResumeCard.jsx:17,21-25; src/pages/Editor.jsx:32,71,73-77; src/hooks/useResumeStore.js:192-197`) Repro: Keep the dashboard open in tab 1. Rename résumé X in tab 2. In tab 1 the card shows the new name. Click its pencil, then click away without typing: X is renamed back to the old name. | Confirmed |
| R2-085 | Dashboard import | Low | bug | 🔴 Open | — | — | Dashboard Import has no reader.onerror: a file the browser will not read does nothing, silently (`src/pages/Dashboard.jsx:56-83`) Repro: Dashboard → Import → pick a file that becomes unreadable (removed USB drive, revoked permission). No message appears. | Confirmed |
| R2-086 | auth / AuthBar | Low | bug | 🔴 Open | — | — | Sign-in failures are swallowed: the Google button does nothing visible when the popup is blocked or the domain is not authorized (`src/components/AuthBar.jsx:73-77`) Repro: In a browser that blocks popups, or on a deploy domain missing from Firebase's authorized domains, click 'Sign in with Google'. The label flips to 'Signing in…' and back, and nothing else happens. | Confirmed |
| R2-087 | design/template picker → store | Low | bug | 🔴 Open | — | — | Clicking the template already selected silently resets the résumé's Section Headings style and Title case (`src/components/DesignPanel.jsx:47 (onClick={() => setTemplate(t.id)}, no guard); src/hooks/useResumeStore.js:251-257 (always merges templateStyleDefaults)`) Repro: Design → Section Headings → Boxed + Abc; then click the Template card that is already highlighted. The headings revert to the template's style. | Confirmed |
| R2-088 | design/section headings panel | Low | bug | 🔴 Open | — | — | Section Headings → Border color shows 'accent' as the default, but several template/style pairs print a grey rule (`src/components/DesignPanelHeadings.jsx:107 (swatch = sectionBorderColor \|\| accentColor), :113 (label 'accent'), :120 (↺ 'Reset to accent color'); src/templates/pdf/shared/sectionHeadingLook.js:283-290`) Repro: Classic → Ruled with a red accent. The panel swatch is red; the PDF rule is light grey. | Confirmed |
| R2-089 | design/reset | Low | bug | 🔴 Open | — | — | Design → Reset on a Sidebar in Single · ATS-safe switches it back to the risky two-column layout, while the copy promises 'ATS-safe defaults' (`src/utils/defaultData.js:68-72 (resetDesignSettings rebuilds from ATS_DEFAULTS, which has no sidebarSingleColumn); copy at src/components/DesignPanel.jsx:227-228`) Repro: Sidebar → Single · ATS-safe (or the ATS panel's layout fix) → Reset Design Settings → Yes. The page is two columns again, and the ATS Check warns about multi-column. | Confirmed |
| R2-090 | design/per-section reset | Low | bug | 🔴 Open | — | — | The Contact icons ↺ also flips Header Customization's Contact Style (Bar or Bullet) to Icon (`src/components/DesignPanel.jsx:23 (ICON_KEYS includes 'contactStyle'), :92`) Repro: Classic, Header Customization → Contact Style Bar; Design → Contact icons → ↺. The header now draws icons. | Confirmed |
| R2-091 | Import — Dashboard | Low | bug | 🔴 Open | — | — | Dashboard Import has no reader.onerror: an unreadable file fails silently (the editor's path was fixed in AUD-23) (`src/pages/Dashboard.jsx:56-82 (handleImport sets only reader.onload); the same pattern in src/pages/JobTracker.jsx:41-54`) Repro: Dashboard → Import → pick a file that the browser then cannot read → nothing happens and no message appears. | Confirmed |
| R2-092 | Photo · cover letter header | Low | bug | ⏸ Fixed | `8c8f99f` | tests/pdf/83-letter-signature-photo.test.mjs | Cover letter prints the résumé photo the user hid (every template); the letter panel still says it uses the résumé photo (`src/templates/pdf/CoverLetterHeaderPDF.jsx:82; src/hooks/usePrintableImage.js:31-49`) Repro: 1. Upload a profile photo. 2. Hide it with the eye on Personal Info → Photo. The résumé shows no photo. 3. Open the Cover Letter. The photo prints in the letterhead of every template, and the panel says 'Using resume photo'. **Now:** A résumé photo hidden under Personal Info → Photo no longer prints on the letter in any template (letterResumePhoto). The panel no longer shows it faded or says 'Using resume photo'; it says 'Résumé photo hidden — upload one for the letter'. The letter's own photo still prints. | Confirmed |
| R2-093 | Photo · import / PdfPhoto | Low | bug | 🔴 Open | — | — | A photo stored as a URL or path (JSON Resume basics.image) that cannot be fetched prints an empty ring and pushes the name aside; the panel says 'Added' (`src/utils/jsonResume.js:95; src/utils/imageUpload.js:54 (drawableImage returns any non-data string); src/templates/pdf/shared/PdfPhoto.jsx`) Repro: 1. Import a JSON Resume whose basics.image is 'photo.jpg' or a URL without CORS. 2. The header shows an empty ring and a gap where the photo should be, and Personal Info → Photo says 'Added'. | Confirmed |
| R2-094 | Photo panel · AUD-25 leftover | Low | bug | 🔴 Open | — | — | Photo panel shows Height (with 'Portrait' active) for an imported unknown shape, but the PDF clamps the shape to Circle and ignores Height (`src/components/PersonalInfoEditorPhoto.jsx:107`) Repro: 1. Import a résumé with settings.photoShape 'oval' and a photo. 2. Open Personal Info → Photo. Height chips are offered and Portrait is active; clicking any of them changes nothing in the PDF. | Confirmed |
| R2-095 | Header Customization · stored values | Low | bug | 🔴 Open | — | — | Header Customization shows no active Contact Style/Layout chip, and hides Icon set/size, for an imported unknown value, while the PDF prints Bar/Justify (`src/components/PersonalInfoEditorHeader.jsx:130,136`) Repro: 1. Import a résumé whose settings.contactStyle is 'dots'. 2. No Style chip is selected and the icon controls are gone, while the PDF shows bar separators. | Confirmed |
| R2-096 | Contact icons · Sidebar Single (AUD-17 leftover) | Low | bug | 🔴 Open | — | — | Design → Contact icons hint says 'The Sidebar template always shows them' in Single · ATS-safe, where icons print only with Contact Style Icon (`src/constants/templates.js:269`) Repro: 1. Pick Sidebar, then Single · ATS-safe. 2. Set Contact Style to Bar. 3. Open Design → Contact icons: the hint claims icons always show, but the résumé shows none. | Confirmed |
| R2-097 | Photo / icon upload · ONB-10-NB1 leftover | Low | bug | 🔴 Open | — | — | Upload size budget ignores the résumé's sections: all three upload sites pass a section-less object to readImageFile (`src/components/PersonalInfoEditorPhoto.jsx:36; src/components/PersonalInfoEditor.jsx:122; src/components/CoverLetterPanel.jsx:47 (shadows its own full `resume` prop)`) Repro: 1. Take a long résumé (>100 KB of section text) that already has a résumé photo and a letter photo. 2. Upload a contact icon. It is accepted, and the résumé then stops syncing. | Confirmed |
| R2-098 | boards · moveCard | Low | bug | ✅ Fixed | `f5b92ad` | tests/unit/board-drop.unit.mjs, tests/unit/board-store.unit.mjs, tests/unit/normalize-board.unit.mjs | Moving a board card also deletes any other card with the same id in another list (ids are de-duplicated only per list) (`src/hooks/useBoardStore.js:233-251 with src/utils/normalizeBoard.js:165 (withOwnIds per list)`) Repro: Only with hand-edited or merged data containing a duplicate card id across two lists: drag either card, and the other one vanishes. **Now:** already fixed before the 2026-09-24 pass, by `f5b92ad`: Card ids are unique across the whole board since 08aaa66 (B-15), and v2 completeBoard keeps one `seen` set for every issue, column, label, sprint and checklist item; moveIssue takes the card out by index. A new drag test pins that dragging either of two cards saved with one id loses neither. | Unverified |
| R2-099 | jobs · kanban card | Low | bug | ⏸ Fixed | `addbb40`, `79afdf1` | tests/pdf/81-job-tracker-ui.test.mjs | Clicking the 'Open job posting' icon on a kanban card also navigates the tracker to the job's page (`src/components/job/KanbanView.jsx:24-34 (anchor has only onPointerDown) and :105 (parent onClick navigates)`) Repro: Hover a kanban card that has a URL and click the small external-link icon: the posting opens in a new tab and the current tab jumps to /jobs/:id. **Now:** The kanban card's 'Open job posting' icon stops its click from reaching the card, so it opens the posting only; it and the delete button start no drag. | Unverified |
| R2-100 | jobs · résumé link | Low | bug | ✅ Fixed | `9e6102d` | tests/pdf/68-job-overview.test.mjs | Deleting a résumé leaves jobs linked to it: Overview says 'Not linked yet' yet shows an 'Open resume' button that bounces to the dashboard (`src/components/job/OverviewTab.jsx:118-133; src/pages/JobForm.jsx:139-142`) Repro: Link a job to a résumé, delete that résumé on the dashboard, and open the job's Overview: the dropdown reads 'Not linked yet' beside an 'Open resume' icon that lands on the dashboard. **Now:** already fixed before the 2026-09-24 pass, by `9e6102d`: Fixed in Lane C (J-21): a job linked to a deleted résumé reads 'Résumé deleted' in Overview and the form, and the Open button shows only for a résumé that exists. | Unverified |
| R2-101 | jobs · status history | Low | bug | ✅ Fixed | `9e6102d` | tests/unit/job-query.unit.mjs, tests/pdf/81-job-tracker-ui.test.mjs | Status history labels an On Hold that was later closed as '→ reopened' (`src/components/job/StatusHistory.jsx:3 and :73-75`) Repro: On a job click Mark as: On Hold, then Close as: Rejected, and look at Application History. **Now:** already fixed before the 2026-09-24 pass, by `9e6102d`: Fixed in Lane C (J-20): historyLabels marks '→ reopened' only when a pipeline status follows a closed one; On Hold then Rejected is not reopened (now also pinned on the rendered StatusHistory). | Unverified |
| R2-102 | jobs · CSV export · security | Low | bug | ✅ Fixed | `7e49d33` | tests/unit/job-csv.unit.mjs | CSV export does not neutralise formula cells (CSV/formula injection) (`src/utils/jobCsv.js:8-12`) Repro: Import a job JSON whose company starts with '=', export CSV and open it in Excel: the cell is evaluated as a formula. **Now:** already fixed before the 2026-09-24 pass, by `7e49d33`: Fixed in Lane C (J-17): a cell starting with = + - @ tab or CR gets a leading apostrophe (FORMULA_START), so it opens as text. | Unverified |
| R2-103 | letter · generator | Low | bug | ⏸ Fixed | `0617247` | tests/unit/letter-generator-sentences.unit.mjs | Generator writes broken sentences for a blank or half-filled first job ('as at Acme', 'at ,') and for an empty résumé ('at prior roles') (`src/utils/coverLetterGenerator.js:91-94, :103, :111, :120`) Repro: Add an Experience section (it starts with one blank entry) → Auto-Generate → Growth → Apply → the PDF body prints 'In my experience at , …'. **Now:** The generator cites the first job that has a role or a company, and builds a clause from what is filled in ('as Lead at Acme', 'as Lead', 'at Acme', or none). A blank or half-filled job or an empty résumé no longer prints 'as  at Acme', 'at ,', 'at prior roles' or 'the  role', in any archetype. | Confirmed |
| R2-104 | pdf/sidebar-column | Low | bug | 🔴 Open | — | — | Sidebar dark column: headings are left at the bottom of a page and entries split across pages (`src/templates/pdf/shared/PdfSidebarColumn.jsx:105-116 (SideSectionTitle has no wrap={false}/minPresenceAhead), :129-143 SideEducation and :246-256 SideReferences entries (no keep-together), SidebarTemplatePDF.jsx:176-186`) Repro: Sidebar résumé with 18 skill groups ('CatN: React, Node.js, TypeScript, PostgreSQL, Docker') + Languages + Education → 'EDUCATION' ends page 1. | Confirmed |
| R2-105 | pdf/long-words | Low | bug | 🔴 Open | — | — | Sidebar main column, 2-column Projects: a 33–48-character URL overprints the neighbouring card (`src/templates/pdf/shared/PdfSidebarSections.jsx:158 (EntryLink with no settings/breaks in the main column) + pdfFontLoader.js:110 (breakLongWords(48) breaks only tokens > 48 chars)`) Repro: Sidebar template, Projects Grids 2, a project URL of 43 characters with a second project beside it. | Confirmed |
| R2-106 | Preview · memory (pdf.js documents) | Low | bug | 🔴 Open | — | — | pdf.js documents are never destroyed when the preview unmounts mid-render, or when a render fails after the document was opened (`src/components/PdfPreview.jsx:151 (unmount cleanup), :131-136 (catch), with :108/:114/:125`) Repro: Edit the résumé and, within about 0.35 s plus the render time, click Cover Letter. EditorPreviewPane.jsx:62-66 swaps the keyed PdfPreview. Or click Résumé again right after opening the Cover Letter tab (its first render has no delay). Or click Back to dashboard mid-render. Each time, one pdf.js document and its fonts stay alive for the session. | Confirmed |
| R2-107 | Preview · refresh/debounce race | Low | bug | 🔴 Open | — | — | A finished render newer than the one on screen is thrown away, so the preview stays frozen during steady editing (`src/components/PdfPreview.jsx:108 (generation bumped when the change arrives, before the debounce), :114, :125; contradicts its own comment at :11`) Repro: Type a paragraph into a summary or bullet with short pauses (about 0.4–1 s) between words or phrases. The preview does not change until you stop for longer than the render time plus 350 ms, although renders keep finishing in the background. | Confirmed |
| R2-108 | sections · date pickers | Low | bug | 🔴 Open | — | — | Month picker: choosing the blank 'Month' or 'Year' option does nothing, so 'Jan 2024' cannot become the year alone (`src/components/SectionEditorShared.jsx:42–43 (MonthPicker update)`) Repro: Experience → Start Date Jan 2024 → set the month select to 'Month' → it jumps back to Jan; the stored value is unchanged. | Confirmed |
| R2-109 | sections · section options · columns | Low | bug | 🔴 Open | — | — | Languages and References with no stored `columns` print in 2 columns, while Section Options → Grids shows 1 (`src/templates/pdf/shared/PdfSectionsTwo.jsx:121 and src/templates/pdf/shared/PdfSectionsThree.jsx:21 (`s.columns \|\| 2`) vs src/components/SectionEditorCustomizer.jsx:92 (`s.columns \|\| 1`)`) Repro: Import a file whose Languages section has no settings → the preview shows two columns; Section Options highlights Grids '1'; clicking '1' then changes the PDF. | Confirmed |
| R2-110 | sections · section options · imported values | Low | bug | 🔴 Open | — | — | A section's `columns` stored as text or out of range is not normalized and breaks the grid (`src/templates/pdf/shared/PdfSections.jsx:84 (RenderColGrid `i += cols`) and :40 (getColumnWidth); src/utils/normalizeResume.js:292 (no section-settings clamp)`) Repro: Import a .json with "columns": "2" on a section with 5 entries → the second row crams 3 entries. | Confirmed |
| R2-111 | sections · entry header · PDF vs Word | Low | bug | 🔴 Open | — | — | An entry whose leading field is empty prints an empty bold title line in the PDF; Word moves the next field up (`src/templates/pdf/shared/PdfSectionsOne.jsx:54 and :233 (primary passed even when empty); src/templates/pdf/shared/PdfSections.jsx:129–193 (ItemHeader stacked)`) Repro: Experience entry with Job Title only → the preview shows a line with just the date, and the title below it, not bold. | Confirmed |
| R2-112 | sections · data round-trip · legacy bullets | Low | bug | 🔴 Open | — | — | Legacy `bullets[]` print in the PDF, Word and ATS text, but the editor cannot show, edit or delete them, and the Description eye does not hide them (`src/templates/pdf/shared/PdfSectionsOne.jsx:66 (and every RenderBullets call); src/utils/wordExportBuilders.js:61; src/components/SectionEditorEntryItems.jsx (never reads `bullets`)`) Repro: Import a file whose experience entry has "bullets": ["x"] → 'x' prints on the résumé, but no field in the editor holds it. | Confirmed |
| R2-113 | sections · add section · collapse | Low | bug | 🔴 Open | — | — | After 'Collapse All', a newly added section opens collapsed, hiding its entry and Add button (`src/components/SectionEditor.jsx:33–35 (the effect on [forceOpenKey] also runs on mount)`) Repro: Collapse All → Add Section → Awards → the new card is collapsed. | Confirmed |
| R2-114 | sections · entry header · Word styling | Low | bug | 🔴 Open | — | — | Word: entry dates right-align to a fixed 9000-twip tab, 0.52 in (A4) / 0.75 in (Letter) short of the right margin (`src/utils/wordExportUtils.js:167 (dateRightPara tabStops position: 9000)`) Repro: Export Word (either paper size) → the experience dates stop about half an inch before the right margin. | Confirmed |
| R2-115 | sections · drag and drop · accessibility | Low | bug | 🔴 Open | — | — | Entries cannot be reordered from the keyboard: the entry drag handle has tabIndex -1 despite a KeyboardSensor (`src/components/SectionEditorShared.jsx:142 (tabIndex={-1} after {...attributes}); src/components/SectionEditor.jsx:38–41 (KeyboardSensor registered)`) Repro: Tab through an Experience section → focus never lands on an entry's grip → Space and the arrow keys cannot move the entry. | Confirmed |
| R2-116 | sections · date pickers | Low | bug | 🔴 Open | — | — | Month picker years stop at the current year + 5, so a later certificate expiry or expected graduation cannot be picked (`src/components/SectionEditorShared.jsx:27 (YEARS = CUR_YEAR+5 … CUR_YEAR−49)`) Repro: Certifications → Expiry Date → the year list ends at 2031. | Confirmed |
| R2-117 | Import (dashboard / job tracker) | Low | parity | 🔴 Open | — | — | Dashboard and Job Tracker imports still say nothing when the browser cannot read the file (AUD-23's reader.onerror fix covered only the editor's import) (`src/pages/Dashboard.jsx:56-83; src/pages/JobTracker.jsx:38-55`) Repro: From the dashboard, pick a file on a removed or unreadable drive: nothing happens and no error is shown. | Unverified |
| R2-118 | design/typography (fontSizeEntryDelta) → Word | Low | parity | 🔴 Open | — | — | Entry Header size disagrees between PDF and Word: the PDF sizes descriptions with it, Word sizes role and location with it (`src/utils/wordExportBuilders.js:57-60 (body at sizes.base), :76-78 (secondary and location at sizes.entry); PDF: src/templates/pdf/shared/PdfSectionsOne.jsx:63 (description at entrySize), src/templates/pdf/shared/PdfSections.jsx:120-121 (sub and loc at baseSize)`) Repro: Design → Typography → Entry Header 15 pt; compare the PDF with Export Word. | Confirmed |
| R2-119 | design/section headings | Low | parity | 🔴 Open | — | — | Section Headings → Border color does nothing on Modern under Boxed, yet the control stays enabled (PDF and Word) (`src/templates/pdf/shared/sectionHeadingLook.js:292 (box: template==='modern' ? tint(accent,…) ignores borderColor); enabled by src/constants/templates.js:136 headingBorderControls('box') → color:true`) Repro: Modern → Section Headings → Boxed → Border color → red. Nothing changes in the preview, the PDF or Word. | Confirmed |
| R2-120 | design panel / Sidebar single column | Low | parity | 🔴 Open | — | — | Sidebar in Single · ATS-safe: the Design panel shows controls that do nothing and says things that are false (`src/components/DesignPanelColors.jsx:244, :280 (raw resume.template === 'sidebar'); src/components/DesignPanelTypography.jsx:154 and src/components/DesignPanelHeadings.jsx:40 (template === 'sidebar', from DesignPanel.jsx:28 templateId, not headerTemplateId); src/constants/templates.js:269-270 contactIconHint uses templateId`) Repro: Pick Sidebar → Layout → Single · ATS-safe, then open Colors, Typography, Section Headings and Contact icons. | Confirmed |
| R2-121 | design/layout (sidebarSingleColumn) → PDF vs Word | Low | parity | 🔴 Open | — | — | Sidebar Single · ATS-safe: entry dates are grey in the PDF but the accent colour in Word (`src/utils/pdfExportReactPDF.js:35 (and :92) set _template: templateKey ('sidebar') while SidebarTemplatePDF.jsx:54 draws ClassicTemplatePDF; src/templates/pdf/shared/PdfSections.jsx:96-100 getDateColor, :115-118 isSidebar sub colour; src/utils/wordExport.js:52 uses 'classic'`) Repro: Sidebar → Single · ATS-safe with a coloured accent; compare the dates in the preview and the .docx. | Confirmed |
| R2-122 | design/dates → ATS plain-text export | Low | parity | ✖ Duplicate of R2-064 | `4a6f968` | tests/unit/text-exports-section-options.unit.mjs | The ATS text export ignores Design → Date format (`src/utils/atsChecker.js:466-468 (experience and volunteering), :488 (education), and every other dated line in generateAtsPlainText (starts :414), which prints raw stored dates and a hardcoded 'Present'`) Repro: Design → Dates → MM/YYYY, then export the ATS text. **Closed 2026-09-24:** Duplicate of R2-064: the ATS text prints every date in Design → Date format and 'Present' through presentLabel. | Confirmed |
| R2-123 | design/contact icons + typography (iconSize) | Low | parity | 🔴 Open | — | — | Contact icon size: some steps print an identical page, and Typography labels the size in pt although it is px (`src/templates/pdf/shared/PdfContact.jsx:26 (max(7, px*0.75)); src/templates/pdf/ModernTemplatePDF.jsx:26 (max(7, round(px*0.9))); src/templates/pdf/SidebarTemplatePDF.jsx:80 (max(6, round(px*0.75))); label: DesignPanelTypography.jsx:148 via SizeRow ('pt', DesignPanelShared.jsx:20) vs DesignPanel.jsx:141 ('px')`) Repro: Sidebar: Contact icons → Icon size 10 → +. The preview does not change. | Confirmed |
| R2-124 | Word export — sizes | Low | parity | 🔴 Open | — | — | Word font sizes still off after AUD-22: the contact line is always 9 pt, and entry descriptions ignore the Entry Header size the PDF applies (`src/utils/wordExportHeader.js:87 (contact style size: 18 hardcoded); src/utils/wordExportBuilders.js:60 and :80 (body at sizes.base) vs src/templates/pdf/shared/PdfSectionsOne.jsx:64 (PDF description at entrySize)`) Repro: Design → Font sizes → Base 14 → Export Word: the contact line stays at 9 pt. Raise Entry Header: the PDF's entry text grows and Word's does not. | Confirmed |
| R2-125 | Word export — layout | Low | parity | 🔴 Open | — | — | Word dates are not at the right margin: the right tab stop is hardcoded to 9000 twips on every paper (`src/utils/wordExportUtils.js:240 (tabStops RIGHT position 9000)`) Repro: Export Word on A4 or US Letter → the dates column sits noticeably inside the right margin that the name/contact rules reach. | Confirmed |
| R2-126 | Word export — header | Low | parity | 🔴 Open | — | — | Word résumé never includes the photo, with no notice (`src/utils/wordExportHeader.js:50-100 (buildPersonalSection has no ImageRun; no photo handling in any src/utils/word*.js)`) Repro: Add a photo and export PDF and Word → the PDF has the photo, the .docx has none. | Confirmed |
| R2-127 | PDF vs other exporters — hidden items | Low | parity | 🔴 Open | — | — | PDF prints the heading of a section whose entries are all hidden; Word, Markdown and ATS text leave it out (`src/templates/pdf/shared/PdfSections.jsx:274-277 (getVisibleSections filters on section.visible only) vs src/utils/wordExportBuilders.js:249 (returns [] when !shown(section).length)`) Repro: Hide every entry of a section with its eye toggle (not the section itself) → the preview and PDF show an empty section title, and the Word, Markdown and ATS exports do not. | Confirmed |
| R2-128 | Header · Word export (Minimal) | Low | parity | 🔴 Open | — | — | Word résumé prints Minimal's light name bold, while the PDF and the Word cover letter print it regular (`src/utils/wordExportHeader.js:60`) Repro: 1. Pick Minimal. 2. Export PDF, Word résumé and Word cover letter. Only the Word résumé shows the name bold. | Confirmed |
| R2-129 | Header data · Markdown export | Low | parity | ⏸ Fixed | `828025a` | tests/unit/markdown-export-contacts.unit.mjs | Markdown export ignores the LinkedIn/GitHub Display label and writes the Link URL override unsanitised (`src/utils/markdownExport.js:44,49,53`) Repro: 1. Set a Display label on LinkedIn. 2. Export Markdown. The label is absent. **Now:** The Markdown contact line is built from contactItems/contactHref, the PDF's contact lines: a website, LinkedIn or GitHub prints its Display label (else its address without the scheme) and links through safeHref, so a javascript: Link URL is no link and a bare-domain one links https://. The 'Email:'/'Phone:'/'Location:' prefixes are gone and a phone links tel:, as in the PDF. Fail-first: 3 of 4 tests failed before the fix. | Confirmed |
| R2-130 | letter · generator ↔ PDF | Low | parity | ⏸ Fixed | `0617247` | tests/pdf/83-letter-signature-photo.test.mjs, tests/unit/letter-generator-sentences.unit.mjs, tests/pdf/43-cover-letter-generator-escape.test.mjs | Generated letter prints as one block: paragraphs 2 pt apart in PDF/Word, while the modal's Live Letter Preview shows them spaced (`src/utils/coverLetterGenerator.js:129; src/components/CoverLetterGeneratorModal.jsx:144; src/templates/pdf/shared/PdfRichText.jsx:7`) Repro: Auto-Generate → Apply → the PDF shows salutation and paragraphs with no visible separation. **Now:** Generated paragraphs are separated by the editor's blank line (<p><br></p>), so the PDF and Word print a blank line between the salutation and each paragraph. The modal's Live Letter Preview no longer adds its own spacing (space-y-2), so it shows the same thing. | Confirmed |
| R2-131 | letter · export menu | Low | parity | ⏸ Fixed | `aced50a` | tests/pdf/78-letter-export-menu.test.mjs | On the Cover Letter tab, Markdown/ATS Text/JSON Resume export the résumé, and the letter has no text export (`src/hooks/useEditorExports.js:70-92 (vs :43, :55)`) Repro: Open the Cover Letter tab → Export → 'Export ATS Text (.txt)' → the file is the résumé's text. **Now:** On the Cover Letter tab the Export menu says what each item exports: 'Export Cover Letter PDF / Word', and 'Export Résumé as Markdown (.md) / ATS Text (.txt) / JSON Resume (.json)' (useEditorExports returns letterTab; EditorHeader passes it). The résumé tab's menu is unchanged. Fail-first: 3 of 4 tests failed before the fix. **Left for later:** The letter has no plain-text export of its own (the row allowed the smaller honest fix). | Confirmed |
| R2-132 | Word export · entry dates | Low | parity | 🔴 Open | — | — | Word right-aligned dates end 0.52 in (A4) or 0.75 in (US Letter) short of the right margin (`src/utils/wordExportUtils.js:167`) Repro: Export any résumé with dated entries to Word and compare the date column with the right margin. | Unverified |
| R2-133 | Dashboard / Word / starters | Low | parity | 🔴 Open | `b7a5e21` | tests/pdf/83-templates-ui-leftovers.test.mjs | Four lower-value Templates UI audit rows were never filed with IDs; three still reproduce and one is obsolete (`src/components/ResumeCard.jsx:30-50,88-89; src/utils/wordExportHeader.js:103-108; src/utils/wordExportCoverLetter.js:4; src/components/StarterTemplateModal.jsx:19`) Repro: Export a Modern résumé with a photo to Word: no banner and no photo, with no notice. **Partly fixed (2026-09-24):** Of the four audit leftovers (templates-ui-bugs.md, 'lower value'): the dashboard card now names its template with templateLabel(), so an unknown id reads 'Classic' as it opens; Export Word has a tooltip saying the .docx has no photo and the résumé prints without its banner or coloured column; and the starter modal's 'ATS-optimized' claim is obsolete, because Modern is now atsTier 'good' and atsRating('modern').safe is true. **Left:** Dashboard thumbnails are still identical: the mock is drawn from the accent colour alone and every creation path writes #374151. Making the thumbnail follow the template is a design job. | Unverified |
| R2-134 | Preview vs PDF · cover letter | Low | parity | ⏸ Fixed | `90811f8` | tests/pdf/83-letter-signature-photo.test.mjs | The empty-letter preview prints a second sign-off ("Best regards, <name>") above the closing that will actually print (`src/templates/pdf/CoverLetterTemplatePDF.jsx:76-82 (only when data._preview, from EditorPreviewPane.jsx:11-12)`) Repro: Create a cover letter from the dashboard and leave the body empty. The preview shows "Best regards, <name>" followed by "Sincerely, <name> <title>". Export PDF prints only the latter. **Now:** The empty-letter preview hint no longer signs off 'Best regards, <name>'. The only sign-off in the preview is the closing and signature that Export PDF prints. | Confirmed |
| R3-001 | Design → Colors · Banner | Low | parity | ✅ Fixed | `0b5f6e2` | tests/pdf/parity/01-offered.test.mjs | Design → Colors offers no Header Text Color on Banner (T7), whose band prints the name, title, contacts, their icons and marks and the header rule in it — the PDF takes a stored one; unset it is white, or a readable tint on a light accent (`src/components/DesignPanelColors.jsx:85 (header === 'modern' \|\| header === 'sidebar')`) Repro: Pick Banner, open Design → Colors: no Header Text Color; on Modern it is there. Fix: offer it wherever the header prints on a band — Modern, Banner, the two-column Sidebar (letterheadLook(t).band). Not fixed in T7: the file held BUILD-H's uncommitted B1 work. **Now:** fixed in `0b5f6e2`: Design → Colors offers Header Text Color wherever the header prints on a band — Modern's banner, Banner's band, the two-column Sidebar's column (`letterheadLook(t).band`). Fail-first: `01-offered` reported "headerTextColor: not offered, but the header prints on a band" for Banner on the old code. | Confirmed (code read, T7) |
| R3-007 | jobs · list view · accessibility | Low | bug | ⏸ Fixed | `b34b16b` | tests/pdf/81-job-tracker-ui.test.mjs | The job list's sort headers cannot be used from the keyboard: each is a `<th onClick>` with no button, tabindex or key handler, so Tab never reaches them and a screen reader does not announce them as sortable (`src/components/job/ListView.jsx` sort headers) Repro: Job Tracker → List → Tab through the header row: no header takes focus. Found by the jobs cluster, 2026-09-24. **Now:** Each sort header of the job list is a button Tab reaches, with aria-sort saying how the column sorts. | Confirmed (code) |
