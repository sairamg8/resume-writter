# Full audit 2026-09-23 — High and Medium defects

> Part of the tracker: [../bug-status.md](../bug-status.md) → **Full audit (`R2-`)**. Status legend as there: ✅ fixed and pushed · ⏸ fixed, local · 🔴 open · ✖ not a bug (proof in the row).
> Verified: *Confirmed* = an independent second agent reproduced it at `5aafca8`–`5b6ecc0`; *Unverified* = the session limit stopped its check — the fix's fail-first test settles it.

| ID | Area | Severity | Kind | Status | Commit | Tests | Defect (where) | Verified |
|---|---|---|---|---|---|---|---|---|
| R2-001 | ats / generateAtsPlainText | High | bug | ✅ Fixed | `8409472`, `968d1dd` | tests/unit/ats-plain-text.unit.mjs, tests/pdf/65-ats-text-parity.test.mjs | The ATS plain-text export drops content the PDF and Word print: all of Interests, award/reference/custom/cert details, project dates, and the paragraph before a bullet list (`src/utils/atsChecker.js:546-560 (generic branch reads only title\|name + description: interests, awards, references, custom); :535-537 (certifications); :517-534 (projects, no dates); :476-484, 497-505, 525-533, 551-559 (bullets found → description paragraph never printed)`) Repro: Add an Interests section with "Chess, Running", then use ATS Check → Download .txt (or Export → ATS text). The file has an INTERESTS heading and nothing under it. **Now:** one generator, moved to `src/utils/atsPlainText.js` (re-exported from `atsChecker.js`, so the ATS tab's Copy / Download and Export → ATS Text are unchanged callers), prints each section's fields as its PDF renderer does: Interests as one list; an award's issuer and date; a reference's job title, company, relationship, e-mail and phone; a custom entry's subtitle, date and location; a certificate's `Expires: … \| ID: …` and `Link: …`; a project's dates. Descriptions and the summary go through `parseRichText`, the parse the PDF and Word use: paragraphs and list items in the PDF's order, numbers kept, nested items indented, legacy bullets after the description. Hidden fields stay out; a section with nothing under its heading prints no heading. Owner away — took labelled lines for the certificate extras (`Expires:`, `ID:`, `Link:` with the address, not the link label) because the first line `name - issuer - date` stays as it was and a plain-text reader cannot follow a label; dates still print as stored (Date format and Show dates are R2-058/R2-122). Fail-first: all 12 unit tests and all 8 parity tests failed at HEAD (INTERESTS `[]` vs `['Chess, Running, Photography']`; the PDF's words `owned zephyr … acmorg ctox refco … chess running` missing from the text). `968d1dd` is the gate's repair: the contact line had become a second copy of the contact table that 31-contact-fields forbids. Still open as duplicates of this row: R2-003, R2-008 (and the ATS half of R2-053). | Confirmed |
| R2-002 | Exports · JSON Resume | High | bug | ✅ Fixed | `784c375`, `cc919de` | tests/unit/json-resume-sections.unit.mjs | JSON Resume export/import silently drops Languages, Volunteering, Interests, References and Custom sections (`src/utils/jsonResume.js:318 (export branches only experience/education/skills/projects/certifications/awards; returned object ~:402-409); import src/utils/jsonResume.js:62-240`) Repro: Editor → add a Languages and a Volunteering section → Export → JSON Resume (.json) → Import JSON with that file: both sections are gone. Importing a JSON Resume from another builder that has languages/volunteer/interests/references also loses them. **Now:** every section type goes both ways, from one table (`src/utils/jsonResumeSections.js`): Languages ↔ `languages` {language, fluency}, Volunteering ↔ `volunteer`, Interests ↔ `interests` (one per interest the PDF prints), References ↔ `references` (`reference` holds what the PDF prints under the name, so other tools show it; the app's own fields sit beside it and come back unchanged). Certificates keep expiry, credential ID and link label; education keeps its description; projects keep their technologies (the schema's `keywords`); legacy bullets go out as highlights. Another builder's `publications` import as their own custom section. The export writes the section list to `meta.sections` (type, title, settings, entry count, and a custom section's entries inline), so a round trip keeps every section with its title, place and settings. Entries that no meta row claims (another tool's file, or one edited by hand) join the last section of their type, so none are dropped. `jsonResume.js` (417 lines) is split into import, export, sections and text modules, and callers are unchanged. Owner away. I took one JSON Resume interest per printed interest, because the schema and other tools expect that; a section's interests come back as one entry that prints the same chips. An award's summary stays one text holding every line, so other tools print them all; a list inside an award comes back as lines. Fail-first: 5 of 6 tests failed at `cea5368` (the round trip returned only `experience … awards`; `file.languages` was undefined). `cc919de` is the gate's repair: 31-contact-fields flagged a hand-written reference field list, which is now read from the blank entry. This also closes most of R2-006 and R2-052 (sections both ways, titles, order, fields). R2-006 still covers the `current` inference and a list in the summary. | Confirmed |
| R2-003 | Exports · ATS plain text | High | bug | ✖ Duplicate of R2-001 | — | tests/unit/ats-plain-text.unit.mjs, tests/pdf/65-ats-text-parity.test.mjs (R2-001's) | ATS text export (and the ATS tab's Copy/Download) drops whole sections and fields the PDF prints (`src/utils/atsChecker.js:547 (generic fallback prints only title\|\|name + description); :535 certifications; :517 projects (no dates)`) Repro: Add an Interests section → Export → ATS Text (.txt): the INTERESTS heading is empty. The same happens with the ATS tab's Copy / Download plain text. **Now:** it does not reproduce at `cea5368`. I ran the row's own repro through `generateAtsPlainText`: Interests `Chess, Climbing`, a reference with job title, company, relationship, e-mail and phone, an award with issuer and date, a certificate with expiry, ID and link, a custom entry with subtitle, date and location, and a project's dates. The text printed every one of them (e.g. `Chess, Climbing`, `CTO, RefCo`, `Best Paper - ACM`, `Expires: Jan 2025 \| ID: XYZ-1`, `TALKTITLE - CONFNAME`, `Feb 2021 - Mar 2021`). R2-001 fixed it (`8409472`, `968d1dd`), and that row already names this one as its duplicate. No code change. | Confirmed |
| R2-004 | cloud sync / conflict resolution | High | bug | ✅ Fixed | `ef067e9` | tests/pdf/18-cloud-sync-stale-tab.test.mjs | A page left open overwrites another device's later edits with its stale copy, silently and for every device (`src/utils/cloudSyncEngine.js:287-343 (resumesChanged/sendPending), src/utils/cloudSyncIo.js:53, src/hooks/useCloudSync.js:53-57, src/utils/cloudSyncBrowser.js:156`) Repro: 1. Open the app signed in on device 1 and leave the tab open. 2. On device 2, edit résumé X (the change syncs). 3. Back on device 1, without reloading, make any change to X, such as a rename or one character. 4. Reload device 2: its edit is gone. **Now:** a page left open no longer writes over another device's edit. A flush first reads the server's copies of the résumés it sends (never the deletion list, R8-4) and checks their versions (`updatedAt`, compared for equality only: device clocks never pick a winner) against the copies this page has seen (`src/utils/cloudSyncLineage.js`, `src/utils/cloudSyncQueue.js`). A copy made on another device since is kept: this page's copy is written under its id and the other one as a new résumé "<name> (conflict copy)", on every device. A deletion made from a stale copy is not sent, and the newer copy comes back. The first sync does the same across reloads and offline edits: the store keeps the cloud versions it last knew (`cloudVersions`), and a copy here that the cloud had takes the cloud's newer one whatever the clocks say. A tab shown again 10 s or more after the account was read reads it again, so the next edit starts from the other device's copy. Owner away — took "keep both, conflict copy" over "newer wins" or a field merge, because it is the only one that never loses an edit and the editor open on the résumé is not changed under the user. Fail-first: 5 of the 9 new tests failed at `856a449` (cloud `['renamed on laptop | v1']`, the phone's edit gone; the stale deletion removed X from the cloud); the other 4 guard against conflict copies without a conflict. The same tests cover the repros of R2-028 (a deletion from a stale page) and R2-030 (offline edit vs another device's edit); those rows are left for their own check. | Confirmed |
| R2-005 | auth / privacy | High | bug | ✅ Fixed | `66a3da9` | tests/pdf/18-cloud-sync-sign-out.test.mjs, tests/unit/sign-out-leave.unit.mjs | Shared browser: sign-out leaves account A's résumés on screen, and account B's sign-in copies them into B's cloud (`src/hooks/useAuth.js:80-85 (signOut); src/utils/cloudSyncEngine.js:147-154; src/utils/cloudSyncPlan.js:10-12 and :107-109; src/utils/firebase.js:37-39`) Repro: On a shared or public computer: sign in as A, sign out. A's résumés stay on the dashboard, readable and exportable. Sign in as B: A's résumés appear in B's list and are written to users/B/resumes. **Now:** an account's list leaves a shared browser with it (`src/utils/cloudSyncLeave.js`). When the account signs out, or another signs in while the list is still the last one's (an earlier build's sign-out, a session ended in another tab), what its cloud holds goes (its next sign-in brings it back). A change that cloud lacks (typed within the pause, offline, or held back as too large) is kept aside for it (`stashed[uid]`), off the dashboard and out of every other account's sync, and sent at its next first sync. Its waiting deletions stay: they are its own (R8-6). A list made signed out still joins whoever signs in, and a build with no cloud keeps its list (its only copy). An account change also resets the first-sync state, so the leaving list is never queued as the next account's deletions. Firestore's cache is in memory only (`memoryLocalCache` in `src/utils/firebase.js`; nothing read it, since every read asks the server), and the IndexedDB cache an earlier build left is deleted at startup (`src/utils/firestoreOldCache.js`). Owner away — took "remove at sign-out, no dialog" over "ask first", because a dialog on a public computer is one more way to leave the data behind, nothing typed is lost, and signing in again brings the list back. Fail-first: the 4 sign-out tests failed on the unfixed sync (after sign-out the list was `['Alice']`; B's cloud got Alice's résumé). tests/pdf/18-cloud-sync-accounts.test.mjs no longer pins the carry-over; its R8-6 cases start from a list an earlier build left for A on a signed-out page. | Confirmed |
| R2-006 | JSON Resume export/import | High | bug | ✅ Fixed | `677c6ca` | tests/unit/json-resume-reprint.unit.mjs (with R2-002's tests/unit/json-resume-sections.unit.mjs) | JSON Resume export and import drop 5 of the 11 section types and several fields, and the round trip changes what prints (`src/utils/jsonResume.js:316-387 (export loop handles only experience/education/skills/projects/certifications/awards); :99-237 (import reads only work/education/skills/projects/certificates/awards); :124 (current inferred from empty endDate)`) Repro: Editor → Export JSON Resume on a résumé with Languages, Volunteering, References, Interests or a custom section → Import that file → those sections are gone, the others are reordered and retitled, and an open-ended past job now reads 'Present'. **Now:** R2-002 brought every section back; the rest is fixed in `677c6ca`, and the round trip prints what the résumé printed. A job's `current` flag goes out beside its dates and the import reads it, so a past job with no end date stays past (a file with no flag — another tool's — keeps JSON Resume's convention: a start and no end is the current job). The summary and an award's description still go out as plain text for every tool and, only when that text alone would print differently (a list, a second paragraph, bold), the rich text beside it as `summaryHtml`; the import reads it back sanitized as the editor reads HTML, and only while the plain text is still the one written beside it (a summary edited in the file wins). The website's, LinkedIn's and GitHub's display labels and link URLs go out on `basics` under the app's names and come back. A skill group with no category goes out with an empty name and comes back with none (it came back labelled "Skills"; a group with no name at all in another tool's file still gets the default). Owner away — took the rich copy beside the plain text (not bullets typed into the text) because other tools keep reading the schema's plain text unchanged and the app gets back exactly what it printed; the import ignores the copy once the text beside it was edited. Fail-first: 6 of the 9 tests in json-resume-reprint failed at `221cdfc` (Past Co `current` true; the summary list back as `Ops lead for ten years\nKubernetes…`; the award's list as lines; the website printing `ada.dev`, not `My site`; category `'Skills'`, not `''`; the full-résumé ATS text differing: `2021-03 - Present` for a past job). The guard for every type is the full-résumé test: the ATS text of the imported résumé equals the exported one's. | Confirmed |
| R2-007 | JSON Resume export/import — hidden data | High | bug | ✅ Fixed | `677c6ca` | tests/unit/json-resume-hidden.unit.mjs | JSON Resume export writes everything the user hid, and the import brings it back visible (a hidden phone prints again) (`src/utils/jsonResume.js:297-416 (no check of section.visible, item.visible, item.hiddenFields or personal.hiddenFields; basics.phone :396, basics.image :394); :96 (import sets hiddenFields: [])`) Repro: Hide Phone (Personal Info eye) and one job → Export JSON Resume → the phone and the job are in the file → Import it → the new résumé prints the phone and the job. **Now:** (`677c6ca`) the JSON Resume export writes only what prints, as every other export does: a hidden section and a hidden entry stay out (`meta.sections` counts only the shown entries, so each one lands in its own section on import), a field hidden with its eye goes out blank, a hidden e-mail, phone, location, website, LinkedIn or GitHub (with its label and link URL), photo or summary is not in the file, and a job whose end date is hidden goes out with no end and not current, as the PDF prints neither. The file holds no `hiddenFields`; the Backup JSON stays the lossless copy. Fail-first: 3 of the 4 tests failed at `221cdfc` (`email: "secret-email.example" is in the file`; the file held `Secret Town`, `Secret Employer`, `Secret Section`, `Secret Category`…; a hidden end date printed `2020-01 - Present` after the import). | Confirmed |
| R2-008 | ATS plain text export | High | bug | ✖ Duplicate of R2-001 | — | tests/unit/ats-plain-text.unit.mjs, tests/pdf/65-ats-text-parity.test.mjs (R2-001's) | ATS text export silently drops content: Interests print nothing, References print only the name, project dates, award issuer and date, cert expiry and ID, custom subtitle/date/location, and any paragraph before a bullet list (`src/utils/atsChecker.js:414 generateAtsPlainText; :516-533 (projects: no dates); :535 (certLine: name, issuer, date only); :546-557 (generic fallback prints only title\|\|name + description, used by awards/references/interests/custom); :26-33 (extractBulletsFromItem: when any <li> exists only li text is kept; lazy regex glues nested items)`) Repro: Editor → Export ATS Text (.txt) on a résumé with Interests, References, Projects with dates, or a job description that starts with a sentence and then a bullet list. **Now:** it does not reproduce at `41766b0`. I ran the row's own evidence through `generateAtsPlainText` (the helpers' every-type résumé, the job description `<p>Led the platform team of 6.</p><ul><li>Cut cost 30%</li><li>Shipped v2<ul><li>nested child</li></ul></li></ul>`): it printed `Led the platform team of 6.`, `* Cut cost 30%`, `* Shipped v2`, `  * nested child` on their own lines, `Hiking, Photography, Open Source` under INTERESTS, `CTO, Acme Corp` / `Manager` / `jane@acme.com` under the reference, `Employee of the Year - Acme Corp` / `2023`, the project's `01/2023 - 03/2023`, the certificate's `ID: AWS-123` and `React Performance Patterns - Tech Blog` / `2024`. R2-001 fixed it (`8409472`, `968d1dd`) and names this row as its duplicate. No code change. | Confirmed |
| R2-009 | Markdown export | High | bug | ✅ Fixed | `9c4bdce` | tests/unit/markdown-export-sections.unit.mjs, tests/pdf/70-markdown-parity.test.mjs | Markdown export: Interests print nothing; References, custom sections and education lose fields; nested bullets glue; links without a scheme become relative (`src/utils/markdownExport.js:214-236 (generic branch for awards/references/interests/custom reads title\|name\|role and org\|organization\|company\|issuer only); :101-122 (education never reads fieldOfStudy); :13 (<li> → '- ' with no line break before a nested <li>); :127 and :205 (project/credential URL used raw as link target)`) Repro: Editor → Export Markdown (.md) on a résumé with Interests, References or a custom section, or an education entry with a Field of Study. **Now:** fixed in `9c4bdce`: each section type has its own branch and prints the fields its PDF renderer prints — Interests as one comma list of every entry's interests (no heading over none), a reference's job title and company, relationship, e-mail (a mailto link) and phone, an award's issuer and date, a custom entry's subtitle, date and location, "degree, field of study", a certificate's link under its Link label, and every entry's legacy bullets after its description. Descriptions and the summary go through `parseRichText` (the PDF's and Word's parse): list items one per line, nested ones indented a level each, numbers kept, a line break kept, paragraphs around a list set off, links kept as links. Every link goes through `safeHref`, so `github.com/user/app` links https:// and a `javascript:`/`data:` address prints as text. A section prints its heading only over something printed under it. The same fix covers R2-026 and the Markdown half of R2-053 (left for their own pass). Owner away — took two trailing spaces for a line break (not a trailing backslash) because every Markdown flavour reads them, a 4-space indent per list level because it nests under both `- ` and `1. ` items in CommonMark and the older flavours, and a lettered or Roman item as `- a.` because Markdown numbers in digits only. Fail-first: 13 of the 14 unit tests failed before the fix (`markdownExport.js` as of `b5068d0`: '## Interests' over nothing; `### **Jane Doe** — *Acme Corp*`; `- Shipped v2- nested child- grandchild`), and all 11 parity tests (classic: 20 printed words missing — physicsy, ctox, managerx, chess, jsconfx, berlinx, legacy walrus bullet, verifylabel …); 17/17 unit and 11/11 parity pass (all 7 templates, Timeline and Banner included, and the Sidebar's single column). | Confirmed |
| R2-010 | pdf/fonts | High | bug | ✅ Fixed | `a163871` | tests/pdf/05-fonts-scripts.test.mjs | CJK, Korean, Arabic, Hebrew, Thai and emoji print as garbage, even with a matching custom font (`src/templates/pdf/shared/pdfFontLoader.js:161-169 (chosenFont registers only the 'latin' subset), :186-212 (fallbacksFor ignores code points outside SUBSETS/SYMBOL_FONTS), :34-42 (SUBSETS)`) Repro: resume({template:'classic', personal:{name:'王小明'}}) → renderResumePdf → the name is drawn as Helvetica WinAnsi garbage. The same happens with settings.customFont='Noto Sans JP'. **Now:** a character no loaded face draws brings in its script's font, one at a time until it is drawn (`src/templates/pdf/shared/pdfFontCoverage.js`): Noto Sans SC/TC/JP/KR (kana or hangul in the text put JP or KR first), Hebrew, Thai, the Indic scripts, Armenian, Georgian, Ethiopic, IBM Plex Sans Arabic, and the monochrome Noto Emoji; a chosen font that has the script draws it itself (Noto Sans JP's japanese subset, not only its latin one). The textkit patch walks code points, not UTF-16 halves (an emoji was two Helvetica glyphs, 'Rocket =€' over 'Star'); keeps a space between two words of one fallback font in that font, so a right-to-left phrase is one run and reads back in the order typed; reorders bidi text by runs (a Hebrew line drew one font's glyph ids in another's run); and keeps mark offsets in thousandths of an em, so Hebrew points and Arabic vowels sit on their letters instead of after them. `textWidth` (the letterhead fit) measures spaces the same way. Every template (Timeline included) and the cover letter print all six scripts and emoji. Offline, those characters stay undrawn and the PDF still renders. Owner away — took IBM Plex Sans Arabic over Noto Sans Arabic because Noto draws a letter as a dotless skeleton plus a shared dot glyph, so the text layer (copy-paste, every ATS) read 'أحمد' as 'أجمد'; took the monochrome Noto Emoji font over `Font.registerEmojiSource` images because emoji then stay text for copy-paste and ATS and no image is fetched per emoji; right-to-left words read right to left inside the templates' left-to-right layout. Fail-first: all 20 cases failed at `856a449` ('王小明' read as '‹ … ¬'f', '김민준' as '@ü', 'Rocket =€Star', Helvetica embedded); 21/21 pass (Banner, BUILD-T's new template, enters the per-template case when it lands). The yarn patch file (`.yarn/patches/@react-pdf-textkit-*.patch`) is now 349 lines: yarn applies one diff per `resolutions` entry, and splitting it means changing that entry in package.json. | Confirmed |
| R2-011 | design/template picker | Medium | regression | ✅ Fixed | `41766b0` | tests/pdf/69-picker-ats-badge.test.mjs | TUI-5 not fully fixed: the template picker still shows no ATS badge on the Sidebar in Single · ATS-safe, while the ATS Check calls it ATS-Certified (`src/constants/templates.js:182 (TEMPLATE_PICKER is built once at module load with atsRating(id), no settings); src/components/DesignPanel.jsx:64 renders t.ats`) Repro: Sidebar → Single · ATS-safe; look at the Design → Template list. The Sidebar card has no ATS badge, but the ATS Check tab rates the template ATS-Certified. **Now:** each template card's ATS badge is `atsRating(id, settings).safe` (`src/components/DesignPanel.jsx`). That is the answer the ATS Check gives for the template as this résumé would print it. In Sidebar → Single · ATS-safe the Sidebar card carries the badge. In Two columns it has none. Owner away — took the stored Layout for the Sidebar card on every template, not only while the Sidebar is picked, because the Layout is kept across a template switch, so the card shows what picking it would print. `TEMPLATE_PICKER` keeps its static `ats` for the ATS Check's list of templates to switch to. Fail-first: 3 of the 4 tests failed at `221cdfc` (`sidebar (single true): badge false, ATS Check pass`). | Confirmed |
| R2-012 | Sidebar Single · sections (found while checking header parity) | Medium | regression | ✅ Fixed | `c06a9de` | tests/pdf/72-sidebar-single-title-order.test.mjs | Sidebar 'Single · ATS-safe': PDF leads experience with the role, Word and Section Options with the company (a side effect of the AUD-17 fix; outside the header) (`src/utils/pdfExportReactPDF.js:38 vs src/utils/wordExport.js:53,57 and src/components/SectionEditorCustomizer.jsx:46`) Repro: 1. Pick Sidebar, then Layout → Single · ATS-safe. 2. Add an experience entry. Preview and PDF show the role on top. 3. Export Word. The company leads. **Now:** a section's defaults come from the résumé's own template in every Layout (`c06a9de`): Word (`src/utils/wordExport.js`) and Section Options → Order (`src/components/SectionEditorCustomizer.jsx`) resolve with `templateId(template)`, as the PDF, the preview and the ATS title-order check already did, so Sidebar → Single · ATS-safe leads a job with the role in all of them; the page (header, one column, no side-column sections) stays Classic's. Owner away — took "keep the Sidebar's role-first everywhere" over "print Classic's order in the PDF" because the preview and PDF the user already sees do not change, the ATS-safe layout keeps the role-first order the ATS check rewards, and switching Layout never reorders an entry. The test checks the PDF, Word and the Order control for unset, Co. / Role and Role / Co., Two-column vs Single, and every template; fail-first 3/4 (`sidebar: {"pdf":"role","word":"company","editor":"company"}`). Also covers R2-059 (not flipped). | Confirmed |
| R2-013 | Templates UI / ATS badge (TUI-5) | Medium | regression | ✖ Duplicate of R2-011 | — | tests/pdf/69-picker-ats-badge.test.mjs | TUI-5 marked fixed, but the Design picker still shows no ATS badge on Sidebar in Single · ATS-safe mode while the ATS Check tab calls it ATS-Certified (`src/constants/templates.js:181-182; src/components/DesignPanel.jsx:64`) Repro: Open a Sidebar résumé, go to Design → Layout and pick 'Single · ATS-safe'. The Sidebar row still has no ATS badge. The ATS Check tab gives it 'ATS-Certified Template' and 5/5 template points. **Closed 2026-09-24:** Duplicate of R2-011 (✅): each Template card's ATS badge is atsRating(t.id, settings).safe (src/components/DesignPanel.jsx), so in Sidebar → Single · ATS-safe the Sidebar card shows the badge the ATS Check's verdict gives; test 69 passes at HEAD. | Unverified |
| R2-014 | vite config / performance | Medium | bug | ✅ Fixed | `e4ae706` | tests/pdf/71-startup-chunks.test.mjs | Every page loads the 1.43 MB react-pdf chunk at startup: React was bundled into the react-pdf manual chunk (`vite.config.js:23-35 (manualChunks)`) Repro: Open dist/index.html. Or open the Dashboard or #/terms with devtools → Network: react-pdf-*.js (1.43 MB) is fetched and evaluated before anything renders. **Now:** fixed in `e4ae706`: `vite.config.js` uses Rolldown's `output.codeSplitting` groups instead of `manualChunks` — React and react-dom in their own group, claimed first; the react-pdf, docx and firebase groups name only their own node_modules packages — so the entry imports React from `react-*.js` and index.html modulepreloads no PDF or Word library. Start-up JS on CI's build: 1.29 MB raw / 378 KB gzip -9 (was about 2.77 MB / 860 KB): the entry, react, firebase, templateSettings, fontsource and timelineRail chunks. Owner away — kept named vendor chunks (the old config's intent) rather than dropping them for automatic splitting, and left the top-level `scheduler` with react-pdf (it is react-reconciler's; react-dom carries its own nested copy). The check the row suggested lives in the suite: test 71 builds for production (write: false, as test 38 does) and walks the start-up path; it also checks React is bundled once and that the PDF engine and the Word writer each load without the other. Fail-first, on GitHub Actions (no local builds): the test failed on the old config (run 35955634727: `assets/react-pdf-6nKo6XXA.js: 20 lazy modules` on the start-up path and preloaded), 5/5 with the fix (run 35956108148). | Confirmed |
| R2-015 | Editor layout / usePanelResize | Medium | bug | ✅ Fixed | `a15b045` | tests/unit/panel-resize-storage.unit.mjs | usePanelResize: its unguarded localStorage calls crash the Editor when site storage is blocked, and leave the drag stuck when storage is full (`src/hooks/usePanelResize.js:6-7 (getItem in the useState initializer), :27-33 (setItem before cleanup in onMouseUp)`) Repro: (b) Fill localStorage until the editor shows 'Not saved'. Clear any 'cpwtcv-panel-width' key. Drag the split handle and release: the panel keeps resizing as the mouse moves, and no text can be selected. (a) Block cookies/site data for the origin in Chrome and open any résumé: 'Something went wrong'. **Now:** fixed in `a15b045`: the stored width is read in try/catch and kept only when it parses to a finite number (clamped to 240–640, else 360); `onMouseUp` lets go of the drag first (listeners, cursor, user-select), sets the final width, then remembers it best-effort in its own try/catch. Fail-first: 3 of 4 tests failed (`crashed: Access is denied for this document.`; after release `mousemove 1 · mouseup 1`; stored "wide" → `width NaN`); 4/4 with the fix. | Confirmed |
| R2-016 | Editor preview / performance | Medium | bug | ✅ Fixed | `fdaed6b` | tests/pdf/71-preview-hidden-builds.test.mjs | Hidden preview keeps building the full PDF after every edit (default on phones, and in editor-only mode) (`src/components/EditorPreviewPane.jsx:41-44 and :63-66; src/components/PdfPreview.jsx:107-139`) Repro: On a phone, open a résumé (Edit tab) and type. Each pause fires a full PDF build and paint that nobody sees; the same happens in desktop 'Editor only' layout. **Now:** fixed in `fdaed6b`: `PdfPreview` takes `active` (default true, so a shown preview is unchanged); `EditorPreviewPane` passes `layoutMode !== 'editor'` to both previews (résumé and letter). Hidden, it builds and repaints nothing and reports `data-preview-status="paused"` while its input is ahead of what it built; shown, it builds once, from the latest input; shown again with nothing changed, it keeps its pages. Owner away — took a 'paused' status (not the stale last one) so a hidden, behind preview says so to tests, and no debounce on reveal, since nobody types into a preview as it appears. Fail-first: 4 of 5 tests failed (4 edits while hidden → 5 builds; the Editor-only and phone Edit-tab column reported `rendering`); 5/5 with the fix. | Confirmed |
| R2-017 | Editor preview / performance | Medium | bug | ✅ Fixed | `d4b1731` | tests/pdf/71-preview-typing-debounce.test.mjs, tests/pdf/71-preview-hidden-builds.test.mjs | The preview debounces only after its first successful build; before that, or after any failure, every keystroke starts a full PDF build (`src/components/PdfPreview.jsx:110 (`const delay = docRef.current ? DEBOUNCE_MS : 0`)`) Repro: Open a résumé on a slow phone and type straight away, before the first preview appears. Or make one preview build fail. Every keystroke then queues another full build. **Now:** fixed in `d4b1731`: every change after the first build has started waits `DEBOUNCE_MS` (350 ms) for the pause in typing — also before the first page appears (a cold open: fonts and template still loading) and after a failed build; only the first build, a Retry and a preview just shown (R2-016) start at once (`src/components/PdfPreview.jsx`; both previews, résumé and letter). Fail-first: 2 of 5 tests failed ('builds started: 11' for 10 keystrokes 60 ms apart after a failed build); 5/5 with the fix. R2-016's test now pauses past the debounce between its edits. | Confirmed |
| R2-018 | Auth / privacy | Medium | bug | ✖ Duplicate of R2-005 | — | tests/pdf/18-cloud-sync-sign-out.test.mjs, tests/unit/sign-out-leave.unit.mjs (R2-005's) | Sign-out leaves the account's résumés in the browser, and the next account to sign in there uploads them to its own cloud (`src/hooks/useAuth.js:41-46; src/utils/cloudSyncEngine.js start(null) (~:69-86); src/utils/cloudSyncPlan.js planInitialSync / mergeResumeLists`) Repro: On one browser: sign in as A and let it sync, then sign out. The dashboard still lists A's résumés. Sign in as B: A's résumés are written to B's Firestore account. **Now:** it does not reproduce at `d4b1731`: the same defect as R2-005, fixed in `66a3da9`. At sign-out, or another account's sign-in, the account's list leaves the browser (`src/utils/cloudSyncLeave.js`, run by `src/utils/cloudSyncEngine.js:101-102`); a change its cloud lacks is kept aside for that account only; Firestore's cache is in memory (`src/utils/firebase.js:40`) and the old IndexedDB cache is deleted. R2-005's tests pass there, 15/15, among them 'A signs out: A's résumés leave the dashboard and the browser; B signing in gets none of them'. | Confirmed (as R2-005) |
| R2-019 | Mobile / AuthBar SyncDot | Medium | bug | ✅ Fixed | `6f9abd2` | tests/pdf/75-sync-status-tap.test.mjs | Sync status and the 'not synced (a large photo?)' message only appear on mouse hover: phone, keyboard and screen-reader users cannot read them (`src/components/AuthBar.jsx:51-61`) Repro: On a phone, sign in with a résumé over the Firestore limit. The header shows a red cloud; tapping it does nothing, so the user never learns which résumé is held back. **Now:** fixed in `6f9abd2`: `SyncDot` (`src/components/AuthBar.jsx`) is a `type="button"` named by its status (`aria-label`; the icon `aria-hidden`), so screen readers read it and keyboards reach it. Its words open on mouse hover (as before), on keyboard focus, and on a tap, Enter or Space; a second tap, Escape, leaving it, or a tap anywhere else closes them. A tap's emulated mouseenter and focus are not counted, so one tap opens and the next closes; a mouse click keeps what its hover shows. The words wrap within min(18rem, 100vw − 6rem) instead of one nowrap line that ran off a 320 px phone. Owner away — took the finding's option A (a labelled button with a toggled tip), not an editor banner, because it works on every page the header is on (dashboard and editor); no `title`, since the native tooltip would double the custom one on hover. Fail-first: 6 of 6 tests failed ('the sync icon is not a button'); 6/6 with the fix. | Confirmed |
| R2-020 | ats / analyzeAtsScore | Medium | bug | ⏸ Fixed | `6dffd05`, `fc3c411` | tests/unit/ats-printed-score.unit.mjs, tests/pdf/77-ats-score-printed.test.mjs | ATS score gives full points for contacts, summary, company, dates, bullets and skills that the PDF does not print (`src/utils/atsChecker.js:629,643,664,685,699 (contacts/summary ignore personal.hiddenFields); :808,:824,:862 (experience ignores item.hiddenFields and section settings.showDates); :993 (education ignores showDates); :1020,:1044 (skills ignore item.hiddenFields)`) Repro: Take a résumé scoring 20/20 on Contact and click the eye to hide email, phone, location, LinkedIn and summary. The PDF header is just the name, and the ATS Check still shows 20/20 with every contact check passing. **Now:** The ATS score reads what the PDF prints. Hidden contacts and entry fields were already skipped (R2-033, 179fb59). Show dates / Show location off now blank those fields (read through resolveSection, as the PDF and Word read them), and the item names the section. The summary is counted through parseRichText, and skill groups through skillGroup, so skills stored as a list no longer crash the tab. The 5 todo cases from 0905add are live tests. | Confirmed |
| R2-021 | ats / layout score | Medium | bug | ⏸ Fixed | `894d5bb` | tests/unit/ats-section-grids.unit.mjs, tests/pdf/77-ats-section-grids.test.mjs, tests/pdf/77-ats-panel.test.mjs | The layout check certifies single-column parsing while Section Options → Grids 2 prints entries side by side, which Poppler interleaves and loses (`src/utils/atsChecker.js:1068-1075 (template tier only; no check of section.settings.columns); Grids offered at src/components/SectionEditorCustomizer.jsx:88-96`) Repro: On Classic, set Professional Experience → Section Options → Grids to 2. The ATS Check Layout category still says the text flows in one column with 100% sequential parsing. **Now:** A shown multi-line section (experience, education, projects, volunteering, custom) with Grids above 1 and two or more entries now gets a section_grids layout warning. The check skips the Sidebar's side column. The text flow scores as two columns, and the fix button 'Print entries one under another (Grids 1)' sets Grids 1 on exactly those sections. Checked against the rendered PDF on all 9 templates. | Confirmed |
| R2-022 | ats / extractResumeCorpus + matchResumeWithJob | Medium | bug | ⏸ Fixed | `218b8be` | tests/unit/ats-job-match-printed.unit.mjs, tests/pdf/77-ats-score-printed.test.mjs | The job-description matcher reports printed words as missing and hidden or markup words as matched (`src/utils/atsChecker.js:211-254 (no item.language/proficiency, no contacts, no references jobTitle/relationship, custom subtitle, cert credentialId, url; pushes p.summary and item.description raw at :217, :231 without hiddenFields; no personal.hiddenFields / item.hiddenFields filtering)`) Repro: Add Spanish under Languages, then paste a job posting that asks for Spanish into Target Job Description Scanner. Spanish is listed under Missing Keywords. **Now:** The job match reads every field an export prints: language and proficiency, contacts as the header prints them, a reference's job title and relationship, custom subtitle, certificate ID and link, and GPA. Rich text is read as its words, never its markup or entities. | Confirmed |
| R2-023 | ats / extractJobKeywords | Medium | bug | ⏸ Fixed | `046936e`, `d9b4339` | tests/unit/ats-job-keywords.unit.mjs | Keyword extraction splits accented words and turns abbreviations into junk keywords, which "+" then writes into Skills (`src/utils/atsChecker.js:275-287 ([^\w\s+#.-] and [^\w+#] are ASCII-only; dotted abbreviations keep inner dots)`) Repro: Paste a job description containing "e.g." or "München" into the scanner. "e.g" or "nchen" appears as a missing keyword, and clicking + adds it to Skills. **Now:** Keywords are read as Unicode letters, combining marks and digits, after NFC normalisation, so 'München' (composed or decomposed) stays whole. Letter abbreviations such as 'e.g.' and 'i.e.' are dropped, and the matcher's word boundaries are Unicode too. | Confirmed |
| R2-024 | ats / AtsCheckerPanel handleAddMissingSkill | Medium | bug | ⏸ Fixed | `c873b88` | tests/pdf/77-ats-panel.test.mjs | "+" (add missing keyword) writes into the first skill entry even when that entry or its skills field is hidden (`src/components/AtsCheckerPanel.jsx:151-171 (items[0] at :159, no visible / hiddenFields check)`) Repro: Hide the first entry of your Skills section with its eye, paste a JD and click + on a missing keyword. The button flashes a check, the keyword never appears in the preview, and it stays in the Missing list. **Now:** '+' writes the keyword into the first skill group that prints, in a shown Skills section: the group must be shown and its skills not hidden (keywordSkillTarget). With no such group it adds a new group to that section. The keyword then prints and moves to Matched. | Confirmed |
| R2-025 | ats / bulletOptimizer vs atsChecker | Medium | bug | ⏸ Fixed | `2a51e3c` | tests/unit/bullet-optimizer-ats-agree.unit.mjs | The STAR Optimizer and the ATS score disagree on action verbs and metrics, and Auto-Fix output fails the optimizer's own verb check (`src/utils/bulletOptimizer.js:105-107 (hasActionVerb only against ACTION_VERBS_BY_CATEGORY's 60 verbs), :34-44 (replacements); src/utils/atsChecker.js:69-108 (ACTION_VERBS), :885, :897 (metric = any digit)`) Repro: In any description, open STAR Optimizer on "Responsible for migrating 12 services to AWS" and click Auto-Fix. The text becomes "Led migrating…" and the badge still says Verb Missing. **Now:** The STAR Optimizer and the ATS score share one ACTION_VERBS list, which includes every verb Auto-Fix writes. They also share leadsWithActionVerb and hasMetric, so 'Led migrating 12 services…' passes both. | Confirmed |
| R2-026 | Exports · Markdown | Medium | bug | ✅ Fixed | `9c4bdce` | tests/unit/markdown-export-sections.unit.mjs | Markdown export loses Interests entirely, references' contact details and custom subtitle/location (`src/utils/markdownExport.js:215-229 (generic 'General item list' reads title/name/role + org/company/issuer + date only)`) Repro: Add Interests 'Chess' → Export → Markdown (.md): the Interests section has a heading and no content. **Now:** already fixed before the 2026-09-24 pass, by `9c4bdce`: Fixed by R2-009 (9c4bdce): at HEAD the Markdown prints Interests as one list, a reference's job title, company, e-mail and phone, and a custom entry's subtitle and location (checked with a script). | Unverified |
| R2-027 | ATS checker · score / job match | Medium | bug | ✖ Duplicate of R2-033 | — | tests/unit/ats-hidden-fields.unit.mjs, tests/pdf/77-ats-score-printed.test.mjs | ATS score and Job Match count data the user hid (contacts, summary, per-entry fields) (`src/utils/atsChecker.js:629 (email), :643 (phone), :664 (location), :699 (summary) read personal.* without personal.hiddenFields; extractResumeCorpus :217, :227-251 ignores personal.hiddenFields and item.hiddenFields`) Repro: Hide Phone and the Summary with their eye icons → ATS Check: 'Valid phone number format' still passes. Paste a JD mentioning a skill that is only in the hidden summary: it shows as matched. **Closed 2026-09-24:** Duplicate of R2-033 (179fb59, before the base): hidden phone and summary already score as absent and are not matched. ats-hidden-fields passes at 504b313. The remaining print-parity (Show dates, markup, printed fields) is R2-020 / R2-022. | Unverified |
| R2-028 | cloud sync / deletion propagation | Medium | bug | ✅ Fixed | — | tests/pdf/18-cloud-sync-stale-tab.test.mjs | A deletion from a stale page permanently removes a résumé that another device edited later (R8-0 guards only the first sync) (`src/utils/cloudSyncPlan.js:202-206 (planFlush has no version check) vs :94 (planInitialSync checks it); src/utils/cloudSyncIo.js:55-56`) Repro: Device 1 is open and synced. Device 2 edits résumé X. Device 1, not reloaded, deletes X from the dashboard. X is deleted for good on every device, including device 2's newer edit. **Now:** already fixed before the 2026-09-24 pass, by —: Fixed by R2-004 (ef067e9): a flush reads the server's copy of what it deletes, and a deletion made from a copy older than another device's edit is not sent — the newer copy comes back (cloudSyncLineage.checkFlush `back`). Pinned by the stale-tab test 'a deletion made from the stale copy is not sent'. | Confirmed |
| R2-029 | cloud sync / deletion propagation / retention | Medium | bug | ⏸ Fixed | `8986943`, `c4c3c7a` | tests/pdf/18-cloud-sync-deleted-elsewhere.test.mjs, tests/pdf/18-cloud-sync-originals.test.mjs | Deleted on one device, edited on a stale page: the later edit is dropped at the next reload and the résumé's content stays in Firestore for good (`src/utils/cloudSyncPlan.js:79-81 and :102-105 (listed ids are excluded but never hard-deleted); src/utils/cloudSyncIo.js:53`) Repro: Device 2 deletes X. Device 1 (open, stale) edits X, and the change flushes. Reload device 1: X and the edit vanish, but users/{uid}/resumes/X still holds it. **Now:** An edit made where a deletion was never seen (a page left open, or offline across a reload) now wins: the résumé is written under its id and comes off the account's deletion list in the same batch (arrayRemove), so it is back on every device. A flush reads the list only when a copy the cloud had is gone and the version sent is one the cloud never had; a first sync does the same by cloudVersions, and takes back a listed copy the cloud holds again. A listed copy nothing brings back (a demo account's original) is removed from Firestore; an unedited copy still goes. Fail-first: 5 of 7 new tests failed at 9c3589a. | Confirmed |
| R2-030 | cloud sync / conflict resolution | Medium | bug | ✅ Fixed | — | tests/pdf/18-cloud-sync-stale-tab.test.mjs | Offline work is thrown away whole on reconnect when another device touched the same résumé meanwhile (`src/utils/syncMerge.js:217-227; src/utils/cloudSyncPlan.js:127-146 (afterSync)`) Repro: Edit résumé X offline on device 1. Make any small edit to X online on device 2. Reconnect device 1: its offline edits are replaced by device 2's copy. **Now:** already fixed before the 2026-09-24 pass, by —: Fixed by R2-004 (ef067e9): at the first sync back online, a résumé changed on both sides keeps this device's copy under its id and the other device's as '<name> (conflict copy)', in the same visit and after a reload (cloudSyncLineage.sortOut, cloudVersions). Pinned by the stale-tab tests 'an edit made offline…'. | Confirmed |
| R2-031 | Import (Dashboard / ExportDropdown) + normalizeResume | Medium | bug | ⏸ Fixed | `73aa4f3`, `e0970bc` | tests/pdf/80-import-section-shapes.test.mjs | Import accepts a backup with a null section, a null entry or non-array items; the saved résumé then crashes the editor, the preview/PDF, Word, Markdown and JSON Resume export (`src/pages/Dashboard.jsx:63 and src/components/ExportDropdown.jsx:120 (accept any personal + sections array); src/utils/normalizeResume.js:289-292 (keeps non-object sections/items); crash sites src/components/EditorResumeTab.jsx:72-73 (resume.sections.map(s => s.id)) and src/components/SectionEditor.jsx:163 (section.items.map)`) Repro: Import a .json such as {"personal":{"name":"A"},"sections":[null]} (or a section with no items array) from the dashboard → the editor opens on 'Something went wrong', and every export of that résumé fails. **Now:** normalizeResume() now runs withSectionShapes() on every load, import, restore and cloud merge: a null or non-object section or entry is dropped, items that is not a list becomes one, sections that is not a list becomes []. The editor, PDF, Word, Markdown and JSON Resume export all work on such a file. | Confirmed |
| R2-032 | Header spacing · GapStepper | Medium | bug | ✅ Fixed | `64b6ec4` | tests/pdf/73-typed-number-fields.test.mjs | Header spacing fields rewrite the gap when merely focused and left, and Escape saves the typed value instead of cancelling (`src/components/HeaderSpacingControls.jsx:50,52,55`) Repro: 1. Open Personal Info → Header Customization with a photo and a title. 2. Tab through the Header spacing inputs, or click into Name ↔ Title and click away. The value goes dark, ↺ appears, and the PDF spacing changes. 3. Type 30 in a field and press Escape. 30 is saved. **Now:** one hook, `src/hooks/useTypedNumber.js`, drives the typed box of Header spacing's GapStepper and of Design's SizeRow / NumberRow (`64b6ec4`): a draft is written once — on Enter or on leaving the box — and only when its text differs from what the box showed; Escape writes nothing (a ref marks the edit settled, since the blur it causes runs before React renders again); the arrow keys still step a gap by a whole pixel. Sibling fixed in the same commit: SizeRow / NumberRow stored Contact Icons 11 where nothing was stored, a Full Name of 18.5 pt as 18 and Line Height 1.15 as 1.1 on focus + leave, and saved Escape's text too. Fail-first 6/7: focus + leave wrote photoTextGap [13], nameTitleGap [1], titleContactsGap [4], iconTextGap [3], contactGapX [16], contactGapY [2]; Escape after typing wrote it on every box; Enter wrote twice. | Confirmed |
| R2-033 | Header hide toggles · ATS checker | Medium | bug | ✅ Fixed | `179fb59` | tests/unit/ats-hidden-fields.unit.mjs | ATS score and job match count contact fields and the summary the user hid (`src/utils/atsChecker.js:628 (email), :642 (phone), :684 (LinkedIn), :698 (summary), :216 (corpus includes p.summary)`) Repro: 1. Hide Email, Phone and Summary on Personal Info. 2. Open ATS Check. Contact still scores 20/20 ('Valid professional email address'), and a job keyword that appears only in the hidden summary is reported as matched. **Now:** the ATS score and the job match read the résumé as it prints (`179fb59`, `src/utils/atsChecker.js`): a personal field in `hiddenFields` counts as absent (`shownField`), and its item says "… is hidden on the résumé" with how to show it again, quoting no hidden value; entries go through `printedItem` (an entry's hidden fields blanked; a hidden End Date is no end, not "Present", as the PDF and Word print it), so the corpus and the Experience / Education / Skills checks skip them too. Fail-first 5/7 (contact 20/20 with five fields hidden; matched ["Kubernetes", …] from the hidden summary). Also covers R2-027 and the hidden-field half of R2-020 (not flipped; R2-020 keeps Show dates, the summary's HTML word count and skillGroup). | Confirmed |
| R2-034 | Header data · JSON Resume export/import | Medium | bug | ✅ Fixed | `677c6ca` | tests/unit/json-resume-hidden.unit.mjs | JSON Resume export writes hidden contacts, the hidden photo and the hidden summary; a round trip un-hides them and drops Display label / Link URL (`src/utils/jsonResume.js:391-402 (export basics), :95-96 (import sets hiddenFields: [])`) Repro: 1. Hide Phone and the photo on Personal Info. 2. Export as JSON Resume. The phone and photo are in the file. 3. Import that file. The new résumé prints the phone and photo. **Now:** already fixed before the 2026-09-24 pass, by `677c6ca`: Fixed by R2-007/R2-006 (677c6ca): at HEAD a hidden phone, photo and summary are not in the JSON Resume file, and the LinkedIn Display label and Link URL come back on import (checked with a script). | Confirmed |
| R2-035 | jobs · notes | Medium | bug | ✅ Fixed | `671e064` | tests/unit/normalize-job.unit.mjs | Job notes have two incompatible editors: the form's textarea shows the Notes tab's HTML as source, and text typed in the form is mangled (and later lost) in the Notes tab (`src/pages/JobForm.jsx:149 (plain <textarea value={form.notes}>) vs src/components/job/NotesTab.jsx:7 (RichTextEditor, stores innerHTML); KanbanView.jsx:59-61 reads notes as rich text`) Repro: 1) Add Job, and in Notes type two lines, one containing '<tbd>'. 2) Open the job → Notes tab: the lines are merged and '<tbd>' is gone. 3) Type one character there: the original text is lost for good. 4) Alternatively, bold some text in the Notes tab, then click Edit (pencil): the textarea shows the raw HTML tags. **Now:** already fixed before the 2026-09-24 pass, by `671e064`: Fixed in Lane C (J-03): the job form's Notes use the same RichTextEditor as the Notes tab, and plain notes are converted once to editor HTML (notesToHtml / completeJob), so text is neither mangled nor shown as tags. | Unverified |
| R2-036 | jobs · CSV export | Medium | bug | ✅ Fixed | `7e49d33` | tests/unit/job-csv.unit.mjs | CSV export writes the Notes column as raw HTML with entities (`src/utils/jobCsv.js:31`) Repro: Write notes in a job's Notes tab using bold or a new line, then click Export CSV: the Notes cell holds <div>, <b> and &amp;. **Now:** already fixed before the 2026-09-24 pass, by `7e49d33`: Fixed in Lane C (J-08): the Notes column is richTextToPlain(notesToHtml(notes)): plain text with its line breaks, no tags or entities. | Unverified |
| R2-037 | boards · persistence | Medium | bug | ⏸ Fixed | `b181e7c` | tests/pdf/82-board-pages.test.mjs | The Board page never says when changes fail to save: edits made while storage is full are silently lost on reload (`src/pages/Board.jsx:74 (reads useBoardStore() but never store.persistError or store.recovery); compare src/pages/Boards.jsx:40`) Repro: Fill localStorage close to the quota, open a board, add cards: no alert appears. Reload and the cards are gone. Go back to /boards and the alert is shown there. **Now:** The board page now shows the same not-saved alert as the grid (role=alert, NOT_SAVED_MESSAGES.boards) and the RecoveryNotice for a saved list that could not be read in full, through one BoardStorageNotice both pages use. | Unverified |
| R2-038 | jobs · kanban DnD | Medium | bug | ⏸ Fixed | `addbb40`, `79afdf1` | tests/pdf/81-job-tracker-ui.test.mjs | Job kanban drag-and-drop does not work on touch screens (PointerSensor without touch-action: none) (`src/components/job/KanbanView.jsx:142 (PointerSensor only) and :101-107 (draggable div has no touch-none)`) Repro: On a phone, open /jobs (Kanban) and drag a job card to another column: the board scrolls, or nothing happens, and the status never changes. **Now:** The job kanban drags with MouseSensor (8px) plus TouchSensor (press-and-hold 200ms), as the boards do, so a finger drags a card to another column while a swipe still scrolls. | Unverified |
| R2-039 | jobs/boards · accessibility | Medium | bug | ⏸ Fixed | `addbb40`, `79afdf1` | tests/pdf/81-job-tracker-ui.test.mjs | Keyboard users cannot open a job or a board card: focusable role=button cards do nothing on Enter or Space, and the drag instructions they announce are false (`src/components/job/KanbanView.jsx:101-105 and :142; src/components/job/ListView.jsx:60-62; src/components/board/BoardCard.jsx:59-64; src/pages/Board.jsx:85-88; src/components/board/BoardColumn.jsx:41-49`) Repro: On /jobs, press Tab until a kanban card is focused, then press Enter and then Space: nothing happens. Switch to List view: rows cannot be focused. The same happens on /boards/:id. **Now:** Enter or Space on a focused kanban card or board card opens it (keys on a link or button inside are left to them); each list row's company is a button Tab reaches; both DndContexts announce their own instructions instead of dnd-kit's false space-bar/arrow-key drag; hover-only card controls show while focused. **Left for later:** No keyboard way to move a board card or list (the list grip does nothing from the keyboard); job cards move via the job page's status. | Unverified |
| R2-040 | jobs · edit form / two tabs | Medium | bug | ✅ Fixed | `7f4a3f8` | tests/pdf/67-job-form-save.test.mjs, tests/unit/job-edits.unit.mjs | Save Changes on the job form writes back a stale copy of the whole job: to-dos and history another tab added are overwritten, and editing a deleted job discards the edit silently (`src/pages/JobForm.jsx:35-49 (form = { ...existing }) and :57 (updateJob(id, form))`) Repro: Open the same job in two tabs. In tab A click Edit. In tab B add a task. In tab A change the role and click Save Changes: B's task disappears in both tabs. **Now:** already fixed before the 2026-09-24 pass, by `7f4a3f8`: Fixed in Lane C (J-02, J-16): Save writes only the fields the form changed (formPatch), so another tab's to-dos, history and status stay; a job deleted meanwhile keeps the typed input and offers 'Save as a new job'. | Unverified |
| R2-041 | boards · normalisation | Medium | bug | ⏸ Fixed | `b181e7c`, `60b40ff` | tests/pdf/82-board-pages.test.mjs | A saved board list with no cards array crashes both board pages on every visit, with no recovery notice (`src/utils/normalizeBoard.js:118 (readList leaves cards missing/null) and :178 (completeLists skips it); crashes at src/pages/Boards.jsx:68, src/components/board/BoardColumn.jsx:24, src/pages/Board.jsx:196, and useBoardStore.js:213 (addCard)`) Repro: Set localStorage cpwtcv_boards_v1 to {"boards":[{"id":"b","title":"B","lists":[{"id":"l","title":"x"}]}],"dataVersion":1} and open /boards: the ErrorBoundary replaces the page on every load. **Now:** The normaliser part was already fixed (a9eacee; boardMigrate turns a v1 list with missing/null cards into an empty column). But at base both board pages still read v1 boards (b.lists, store.moveCard…) over the v2 store, so /boards and /boards/:id crashed for every saved board, the demo and the row's repro included. Both pages now read a v2 project through src/utils/boardView.js and call the v2 actions; the repro opens on both pages with an empty column. | Unverified |
| R2-042 | jobs · CSV export | Medium | bug | ✅ Fixed | `7e49d33` | tests/unit/job-csv.unit.mjs | CSV export has no UTF-8 BOM, so Excel shows mojibake for every non-ASCII character (including the demo job's own salary and contact) (`src/utils/jobCsv.js:41 and src/pages/JobTracker.jsx:33-36`) Repro: Export CSV with the demo job and double-click the file in Excel on Windows: the salary shows 'â€“' and the contact shows 'Â·'. **Now:** already fixed before the 2026-09-24 pass, by `7e49d33`: Fixed in Lane C (J-09): the CSV starts with a UTF-8 BOM, so Excel reads – · ₹ as written. | Unverified |
| R2-043 | letter · generator | Medium | bug | ⏸ Fixed | `0617247` | tests/pdf/83-letter-signature-photo.test.mjs, tests/unit/letter-generator-sentences.unit.mjs, tests/unit/cover-letter-generator.unit.mjs | Generator saves 'Candidate'/'Professional' placeholders as the real signature, and Apply locks the signature so later name/title edits never reach it (`src/utils/coverLetterGenerator.js:67-68, :138-139; src/components/CoverLetterPanel.jsx:63-64; src/utils/coverLetter.js:101-102`) Repro: Dashboard → New Cover Letter → Auto-Generate from Resume → Apply → the preview signs 'Candidate / Professional'. Then fill Personal Info name 'Jane Doe': the letterhead reads Jane Doe, the signature still reads Candidate. **Now:** The generator writes no signature and no 'Candidate' / 'Professional' placeholder (the subject reads 'Application for <role>' with no name). Apply clears the letter's own Signature Name and Designation, so the signature follows the résumé's name and title as they print, including edits made after Apply, and a signature an earlier Apply stored stops pinning it. **Left for later:** Letters saved by an earlier Apply still hold 'Candidate' until the user applies again or edits the field. These can't be told apart from a typed name, so there is no migration. | Confirmed |
| R2-044 | letter · panel ↔ PDF/Word | Medium | bug | ⏸ Fixed | `e0dfd2e` | tests/pdf/83-letter-signature-photo.test.mjs | Clearing Signature Name/Designation prints no signature, though the panel then shows the same empty field and placeholder as the untouched state, which does print it (`src/utils/coverLetter.js:101-102; src/components/CoverLetterPanel.jsx:307-308`) Repro: Type 'J' in Signature Name, delete it → the field shows the placeholder 'Jane Doe', but the preview, PDF and Word lose the signature name. **Now:** A Signature Name or Designation that is blank ('' or only spaces) prints the résumé's name or title (letterSignature), matching the panel's placeholder and the untouched field, in both PDF and Word. A signature the user typed still prints as typed. **Left for later:** By design, a deliberately empty signature name can no longer print while the résumé has a name. | Confirmed |
| R2-045 | pdf/fonts | Medium | bug | 🔴 Open | — | — | ↑ and ↓ print as curly quotes; ‐ and ‑ hyphens vanish; U+202F prints as '/' (`src/templates/pdf/shared/pdfFontLoader.js:47 (LATIN includes U+2000-206F, U+2191, U+2193, U+2215), :191 (a code point in LATIN_RANGES never gets a fallback)`) Repro: personal.summary = '<p>Revenue ↑ 40%</p>' in any template with the default font → the PDF prints 'Revenue ‘ 40%'. | Confirmed |
| R2-046 | pdf/pagination | Medium | bug | 🔴 Open | — | — | A long summary is clipped, overprinted or pushed into the bottom margin because the header block cannot break (`src/templates/pdf/ClassicTemplatePDF.jsx:89, MinimalTemplatePDF.jsx:86, ExecutiveTemplatePDF.jsx:84, ModernTemplatePDF.jsx:83 (wrap={false} header/banner contains the summary); Sidebar single-column ATS-safe inherits Classic`) Repro: resume({template:'modern', settings:{fontSizeBase:12,lineHeightValue:1.8}, personal:{summary:'<p>'+30 sentences of 11 words+'</p>'}}) → the last sentences are missing from the PDF. | Confirmed |
| R2-047 | pdf/pagination | Medium | bug | 🔴 Open | — | — | A heading stays alone at the page bottom when the block after it cannot break (References, 2-column grids, Sidebar cards) (`src/templates/pdf/shared/PdfSection.jsx:30 (presence = 3 lines ≈ 49.5 pt) vs PdfSectionsThree.jsx:33-35 (References: breakable wrap row of wrap={false} cards), PdfSections.jsx:81-90 (grid rows), PdfSidebarSections.jsx:32-37 (CardItem draws dot and border before the wrap={false} CardHeader)`) Repro: Classic: experience with 30 one-line bullets, then references (2 entries, columns 2) → 'REFERENCES' ends page 1. | Confirmed |
| R2-048 | pdf/pagination | Medium | bug | 🔴 Open | — | — | A 2-column grid can print entries out of order across a page break (right cell on page 1, left cell on page 2) (`src/templates/pdf/shared/PdfSections.jsx:81-90 (RenderColGrid rows split cell by cell)`) Repro: As above, or any 2-column Experience, Education, Projects or Custom section whose left cell has a taller first block than its right cell, placed near a page bottom. | Confirmed |
| R2-049 | pdf/pagination | Medium | bug | 🔴 Open | — | — | Certifications: the date stays on page 1 while its entry moves to page 2; an award title is left alone at the page bottom (`src/templates/pdf/shared/PdfSectionsTwo.jsx:57-62 (certification row: name View flex:1 + date Text, no wrap={false}), :168-176 (Awards title/issuer/date/description as loose Texts, no keep)`) Repro: certifications item {name:'… long name that wraps …', issuer:'Amazon Web Services', credentialId:'ABC-123', date:'2021'} positioned at a page bottom. | Confirmed |
| R2-050 | Editor page layout · split panel resize | Medium | bug | ✖ Duplicate of R2-015 | — | tests/unit/panel-resize-storage.unit.mjs | The editor crashes when the browser blocks site storage, and the panel drag gets stuck when storage is full (`src/hooks/usePanelResize.js:6 and :28 (called on every render from src/pages/Editor.jsx:42, before the `if (!resume) return null`)`) Repro: (a) In Chrome, block all site data for the app's origin (Settings → Site settings → Cookies and site data) and open any résumé: the error screen shows instead of the editor. (b) Fill localStorage until the editor says 'Not saved: browser storage is full', on a browser that has never resized the panel. Drag the split handle and let go: the panel keeps following the mouse, the cursor stays col-resize and text cannot be selected until a reload. **Closed 2026-09-24:** Duplicate of R2-015, fixed in a15b045: usePanelResize reads the stored width in try/catch (blocked storage opens at 360 px) and lets go of the drag before a best-effort setItem (full storage no longer leaves the panel following the pointer). panel-resize-storage passes at HEAD (now driven by pointer events, see R2-144). | Confirmed |
| R2-051 | sections · Sidebar single column · section defaults | Medium | bug | ⏸ Fixed | `4d7c5fb`, `49e4050` | tests/pdf/86-sidebar-single-entry-colours.test.mjs, tests/pdf/72-sidebar-single-title-order.test.mjs | Sidebar 'Single · ATS-safe': the PDF prints entries with the Sidebar's defaults (role first, grey dates, accent subtitles), while Section Options and Word use Classic's (`src/utils/pdfExportReactPDF.js:35 and :38 (also :92 for the letter); compare src/utils/wordExport.js:52,57 and src/components/SectionEditorCustomizer.jsx:47`) Repro: Design → Sidebar → Layout 'Single · ATS-safe'. Leave the experience Order unset. The preview prints the role first; Section Options shows 'Co. / Role'; the .docx prints the company first. **Now:** In Sidebar → Single · ATS-safe, Word builds each section with the résumé's own template, as the PDF does: role first (R2-012, c06a9de), grey dates, the second field in the accent; Section Options' Order already resolved the same (c06a9de). | Confirmed |
| R2-052 | sections · data round-trip · JSON Resume | Medium | bug | ✅ Fixed | `784c375`, `cc919de` | tests/unit/json-resume-sections.unit.mjs | JSON Resume export silently drops Languages, Volunteering, References, Interests and Custom sections, and import drops volunteer, languages, interests, references and publications (`src/utils/jsonResume.js:316 (export loop handles only 6 types) and :102–235 (import reads only work, education, skills, projects, certificates, awards)`) Repro: Add a Languages section → Export → JSON Resume → Import that file → the Languages section is gone. **Now:** already fixed before the 2026-09-24 pass, by `784c375`, `cc919de`: Fixed by R2-002: Languages, Volunteering, Interests, References and custom sections go out and come back (checked with a script at HEAD). | Confirmed |
| R2-053 | sections · data round-trip · text exports | Medium | bug | ✅ Fixed | `9c4bdce`, `8409472` | tests/unit/markdown-export-sections.unit.mjs, tests/unit/ats-plain-text.unit.mjs | Markdown and ATS text exports lose Interests entirely, References' details, Custom subtitle and location, and (ATS) Awards issuer and date (`src/utils/markdownExport.js:215 (general branch); src/utils/atsChecker.js:546 (generateAtsPlainText generic fallback)`) Repro: Add an Interests section 'Chess, Hiking' → Export → Markdown (or ATS text) → the Interests heading has nothing under it. **Now:** already fixed before the 2026-09-24 pass, by `9c4bdce`, `8409472`: Markdown half fixed by R2-009 (9c4bdce), ATS half by R2-001 (8409472): Interests, reference details, custom subtitle and location, and an award's issuer and date print in both (checked with a script at HEAD). | Confirmed |
| R2-054 | sections · section options · text exports | Medium | bug | ✖ Duplicate of R2-064 | `4a6f968`, `54f270f` | tests/unit/text-exports-section-options.unit.mjs | Markdown and ATS text print dates and locations that Section Options → 'Show dates' / 'Show location' turned off (`src/utils/markdownExport.js:70–120 (experience, education, volunteering and certifications blocks never read section.settings); src/utils/atsChecker.js:464–490`) Repro: Section Options → Show location off on Experience → Export Markdown → the location is still printed. **Closed 2026-09-24:** Duplicate of R2-064 (Markdown and ATS text ignored Show dates / Show location); fixed there. | Confirmed |
| R2-055 | sections · store actions · imported data | Medium | bug | ⏸ Fixed | `73aa4f3` | tests/pdf/80-import-section-shapes.test.mjs | Imported sections or entries with no `id`: editing one entry overwrites its siblings, renaming one section renames the others, deleting one entry deletes all of them (`src/hooks/useResumeSectionActions.js:12, :55, :62 (match by id); src/utils/normalizeResume.js:292 (no id repair); src/components/SectionEditor.jsx:61,65`) Repro: Import a hand-written or other-tool CPWT JSON whose entries have no id → type in the first entry's Company → every entry in that section changes to match. **Now:** A section or entry with no id, or an id another already holds, gets its own (deterministic: <type>_<n> / <sectionId>_item<n>, the same on every load); every valid id is kept, the first holder keeping a repeated one. Editing, renaming or deleting one no longer hits its siblings. | Confirmed |
| R2-056 | sections · imported data · shape | Medium | bug | ⏸ Fixed | `73aa4f3` | tests/pdf/80-import-section-shapes.test.mjs | An imported section with no `title` crashes the PDF preview and export (Word exports it); one with no `items` crashes the editor (`src/templates/pdf/shared/PdfSection.jsx:20 and src/templates/pdf/shared/PdfSidebarColumn.jsx:111 (title.toUpperCase()); src/components/SectionEditor.jsx:137,163 (section.items.length / .map); src/utils/textFields.js:50–58 (leaves both as they are)`) Repro: Import a .json whose section object has no "title" → the preview stays blank and Export PDF fails; with no "items", the editor shows the error screen. **Now:** A section with no title gets its type's title (custom's for an unknown type), and one with no items an empty list: the PDF and editor render it. | Confirmed |
| R2-057 | sections · hide entries · PDF vs Word | Medium | bug | 🔴 Open | — | — | A section whose entries are all hidden (or all deleted) prints a bare heading in the PDF and preview; Word, Markdown and ATS text leave it out (`src/templates/pdf/shared/PdfSections.jsx:218–219 (SectionRouter renders any visible section) and :274–276 (getVisibleSections ignores entries); each section renderer, e.g. PdfSectionsTwo.jsx:148–165; compare src/utils/wordExportBuilders.js:249`) Repro: Awards section with one award → click that award's eye to hide it → the preview still prints the AWARDS heading with nothing under it. | Confirmed |
| R2-058 | ats / generateAtsPlainText | Medium | parity | ✖ Duplicate of R2-064 | `4a6f968`, `54f270f` | tests/unit/text-exports-section-options.unit.mjs | The ATS plain-text export ignores Design → Date format and prints dates and locations the user hid with Show dates / Show location (`src/utils/atsChecker.js:467-471 (experience/volunteering), :487-492 (education), :536 (certs): raw item.startDate/endDate and item.location, no formatDate/dateRange, no section.settings.showDates / showLocation`) Repro: Set Design → Date format to MM/YYYY and turn off Show dates on Education, then ATS Check → Copy Text. The graduation dates are in the text, in the stored "Sep 2014" form. **Closed 2026-09-24:** Duplicate of R2-064 (ATS text ignored Date format, Show dates and Show location); fixed there. | Confirmed |
| R2-059 | ats / title order vs PDF, Word, editor | Medium | parity | ✖ Duplicate of R2-012 | — | tests/pdf/72-sidebar-single-title-order.test.mjs | Sidebar Single · ATS-safe: the PDF and preview print Role first, Word prints Company first, and Section Options shows "Co. / Role" (`src/utils/pdfExportReactPDF.js:38 with key from :63 (resolveSection(s, templateId(template)) = 'sidebar' although SidebarTemplatePDF.jsx:54 prints ClassicTemplatePDF); src/utils/wordExport.js:52 and SectionEditorCustomizer.jsx:47 use 'classic'; src/utils/atsChecker.js:842 hardcodes ['executive','sidebar'] defaults`) Repro: Pick Sidebar, switch Layout to Single · ATS-safe, leave Section Options → Order untouched. The preview leads each job with the role, the Order control shows Co. / Role, and the .docx leads with the company. **Closed 2026-09-24:** Duplicate of R2-012 (✅ c06a9de): PDF, Word and Section Options → Order all resolve with templateId(template), and atsChecker reads TEMPLATE_SECTION_DEFAULTS[currentTemplate], so Single · ATS-safe leads a job with the role everywhere; test 72 passes at HEAD. | Confirmed |
| R2-060 | Exports · Markdown / ATS text vs PDF/Word | Medium | parity | ✖ Duplicate of R2-064 | `4a6f968`, `54f270f` | tests/unit/text-exports-section-options.unit.mjs | Markdown and ATS text ignore Section Options → Show dates / Show location / Order; ATS text ignores Design → Date format (`src/utils/markdownExport.js:78-92 (experience; also :110, :132, :180 date lines); src/utils/atsChecker.js:462-471`) Repro: Section ⋯ → Customize layout → turn off Show dates and Show location → Export Markdown or ATS Text: the dates and location the PDF hides are still there. **Closed 2026-09-24:** Duplicate of R2-064, Order included: an experience section's Order (Co. / Role, Role / Co., or the template's default when unset) now applies in Markdown and the ATS text. | Unverified |
| R2-061 | Exports · Word | Medium | parity | 🔴 Open | — | — | Word export ignores Design → Spacing and most Section Options layout controls (`src/utils/wordExport.js:34-40 (fixed margins from WORD_MARGIN_IN, src/utils/wordExportUtils.js:8 = 0.75 in); src/utils/wordExportBuilders.js (never reads lineHeightValue, sectionGap, itemGap, spacing, columns, titleStyle, spaceBefore, spaceAfter)`) Repro: Design → Spacing → Top/Bottom margin 30 mm, Line Height 2.0 → Export Word: the .docx has the same 0.75 in margins and single spacing as before. | Unverified |
| R2-062 | design/spacing → Word (résumé and cover letter) | Medium | parity | ✅ Fixed | `21d64d1` | tests/pdf/69-word-spacing.test.mjs (29-page-size and 35-word-contact-layout now expect the default margins) | Word export ignores all of Design → Spacing: margins, line height, Between Sections, Between Items and the 3 Page Fit presets (`src/utils/wordExport.js:33-40 (fixed WORD_MARGIN_IN), src/utils/wordExportUtils.js:7, :54 (spacer 60 twips), :77 (heading before 180/after 60)`) Repro: Design → Spacing → 1-Page Fit (or set margins to 35 mm). The PDF/preview changes; Export Word gives a byte-identical layout. A résumé tuned to fit 1 page in the PDF can run to 2 in Word. **Now:** the Word résumé and letter follow Design → Spacing (`21d64d1`). Page margins are `marginV`/`marginH` in mm (`wordMargins`), 14 / 18 mm by default, as in the PDF. Line Height is an "at least" line spacing of Line Height × the text size on descriptions, legacy bullets, skills rows, section titles, the summary, and the letter's body and closing. Between Items is an exact-height empty paragraph between two entries; the section's Spacing preset scales it, or its own Item gap replaces it (`getEffectiveSpacing`, as the PDF). Between Sections, or the section's Spacing Override After, sits under every section but the last, and Spacing Override Before is the space above the title. The three Page Fit presets set these keys, so all of them reach the .docx. The dates' right tab now sits on the right margin (it was a fixed 9000 twips), 2 Grid contacts lay out between the new margins, and legacy bullets print at the description's size. Owner away — took "at least" Line Height × size over the Word "multiple" (240 × value) the finding suggested: a multiple scales the font's own single spacing (≈1.36 em in Noto Sans), so 1.5 would print ≈2 em lines, looser than the PDF, while "at least" matches the PDF's line box and never clips letters. Fail-first: 10 of 10 tests failed on the unfixed code (`classic {}: 1080,1080,1080,1080 (want 794,1020,794,1020)`, the date tab at 9000, no line spacing, an auto-height empty paragraph between entries). The same commit fixes R2-066 and R2-068 (duplicates of this row) and R2-114's date tab; those rows are left for their own check. | Confirmed |
| R2-063 | design/colors → Word | Medium | parity | ✅ Fixed | `d92c1ec` | tests/pdf/69-word-text-colour.test.mjs | Word section bodies ignore Design → Text Color: descriptions print fixed #374151, company, role and skills print black (`src/utils/wordExportBuilders.js:60 (color '374151' hardcoded), :76-78, :112-113 (runs with no colour); no textColor read anywhere in wordExportBuilders.js`) Repro: Design → Colors → Text Color → any non-grey colour (e.g. navy #1e3a8a). The PDF body turns navy; the .docx sections stay black and grey. **Now:** Word's section bodies print in the PDF's colours (`d92c1ec`; `entryInk` in `src/utils/wordExportLook.js`). Entry titles print in the Text colour. The second field (role or company, degree, organisation, subtitle) prints as ItemHeader prints it: the accent at 85 % on Modern, at 80 % on the Sidebar's page, else the sub shade. Descriptions and legacy bullets print in the body shade (an award's in the sub shade). Skills, issuers and a reference's role print in sub; relationships and technologies in meta (the Sidebar's technologies in the accent at 70 %); locations and credential IDs in muted. Skill categories print in the Text colour (the accent on Modern and in Tags / Bars); Tags print in the accent (Minimal's in sub), Bars in the Text colour at 80 %. Interests take the accent of the PDF's chips, and the date takes the Text colour the PDF prints. The Sidebar's side column takes the page's colours: Word has no dark panel. Fail-first: 9 of 9 tests failed on the unfixed code (`Acmecorp: pdf #1e3a8a ≠ word #000000`, `Grewrevenue: pdf #3e569b ≠ word #374151`, `Frontendstuff: pdf #1e3a8a ≠ word #e11d48`). This also fixes R2-065, a duplicate that adds the skill category colour; that row is left for its own check. | Confirmed |
| R2-064 | Markdown / ATS text vs PDF and Word | Medium | parity | ⏸ Fixed | `4a6f968`, `54f270f` | tests/unit/text-exports-section-options.unit.mjs | Markdown and ATS text ignore Section Options → Show dates / Show location, and ATS text ignores Design → Date format (`src/utils/markdownExport.js:87-92, :110-114, :132, :178-183 (dates and location printed regardless of section.settings.showDates / showLocation); src/utils/atsChecker.js:466-471 and :491-495 (raw f('startDate') / f('endDate') with no formatDate and no showDates / showLocation check)`) Repro: Section Options → turn off Show dates (or Show location) on a section, or set Design → Date format to MM/YYYY → Export Markdown and ATS Text → dates and locations still print, and ATS dates stay as typed. **Now:** Markdown and the ATS text follow Section Options as the PDF and Word do: Show dates off prints no date on any dated section (experience, education, volunteering, projects, a certificate's date and expiry, awards, custom), Show location off no location on experience, education and volunteering (a custom entry's location prints whatever the options, as in the PDF), and an experience section's Order puts the company or the role first — unset, the résumé template's default through resolveSection. The ATS text prints every date in Design → Date format (formatDate) and a current job's end as presentLabel. Fail-first: 7 of 10 tests failed before 4a6f968; the template-Order tests failed before 54f270f. | Confirmed |
| R2-065 | Word export — colours | Medium | parity | 🔴 Open | — | — | Word ignores Design → Text colour for every entry, and prints skill categories in the accent where the PDF uses the Text colour (`src/utils/wordExportBuilders.js:60 (descriptions hardcoded color '374151'); :76-78 (entry title/role runs have no colour); :112-113 (skill category always accentHex, skills text no colour)`) Repro: Design → Text colour = a dark brown or navy, and Accent = red → Export Word → entry text is black/grey and the skill categories are red, while the preview and PDF show brown/navy text and dark categories. | Confirmed |
| R2-066 | Word export — spacing | Medium | parity | 🔴 Open | — | — | Word ignores the whole Design → Spacing group: margins, line height, section gap and between-items gap (`src/utils/wordExport.js:19-45 (margins always WORD_MARGIN_IN, from src/utils/wordExportUtils.js:81; no line spacing set); src/utils/wordExportUtils.js:150 and wordExportBuilders.js spacer() (fixed twips); no read of marginH, marginV, lineHeightValue, sectionGap or itemGap anywhere in src/utils/word*.js`) Repro: Design → Spacing → set margins to 30 mm and line height to 1.2 → Export Word → the .docx still has 0.75 in margins and single spacing. | Confirmed |
| R2-067 | Header · Word export | Medium | parity | ✅ Fixed | `4203257` | tests/pdf/38-word-contact-size.test.mjs | Word résumé contact line is fixed at 9 pt, ignoring Design → Base font size (the PDF and the Word cover letter follow it) (`src/utils/wordExportHeader.js:87`) Repro: 1. Set Design → Typography → Base to 14. 2. Export PDF and Word. The Word contacts stay 9 pt while the PDF's are 13.5 pt. **Now:** fixed in `4203257`: the contact size lives in one place, `src/templates/pdf/shared/contactSize.js`, and the PDF's headers and the Word export both read it — the contact row (Classic, Minimal, Executive, Timeline, Banner, Academic, the letterhead) half a point under Base, never under 8 pt; Modern's banner 1.5 pt under Base; the two-column Sidebar's dark column its own 9 pt, which Word now matches. The PDF prints exactly what it printed before. Fail-first: 42 mismatches on the old code (e.g. "classic Base 14: Word 9 pt, PDF 13.5 pt"), 0 with the fix, every template at Base 8, 11 and 14. | Confirmed |
| R2-068 | letter · Word export | Medium | parity | ✅ Fixed | `21d64d1` | tests/pdf/69-word-spacing.test.mjs | Word letter ignores Design → Page margins and Line height that the PDF letter follows (`src/utils/wordExport.js:35-40; src/utils/wordExportUtils.js:8, :130`) Repro: Design → Margins 30 mm, Line Height 2 → Export Word on the Cover Letter tab → Word shows 0.75 in margins and single spacing. **Now:** already fixed before the 2026-09-24 pass, by `21d64d1`: Fixed by R2-062 (21d64d1). renderCoverLetterDocx builds through buildDocument, which applies wordMargins(settings). The letter's body and closing use Line Height (lineSpacing). 69-word-spacing checks the letter's margins on every template and its line height at 1.2, 1.5 and 2, and passes. | Confirmed |
| R2-069 | pdf/sections | Medium | parity | 🔴 Open | — | — | A section with no visible entries prints a bare heading in the PDF and preview; Word leaves the section out (`src/templates/pdf/shared/PdfSections.jsx:274-277 (getVisibleSections filters only section.visible), SidebarTemplatePDF.jsx:75; vs src/utils/wordExportBuilders.js:249 (Word returns [] when !shown(section).length)`) Repro: Hide every entry of a section with the eye toggle → the PDF still shows its heading; Export Word does not. | Confirmed |
| R2-070 | Word export parity with the PDF | Medium | parity | 🔴 Open | — | — | Word export ignores Design → Spacing, section Spacing and Spacing Override, Grids, entry Title layout and the Stacked skills style (`src/utils/wordExport.js:36-41 (margins fixed at WORD_MARGIN_IN, src/utils/wordExportUtils.js:8); src/utils/wordExportBuilders.js (no read of sectionGap/itemGap/spacing/spaceBefore/spaceAfter/columns/titleStyle)`) Repro: Set Top/Bottom margin 30 mm, Line Height 2.0 and Grids 2 on Experience, then export Word. The .docx is identical to the default one. | Unverified |
| R3-002 | pdf · entry header (EndRow) | Medium | bug | 🔴 Open | — | — | An entry title whose word is wider than the room its date leaves prints over the date: EndRow gives the title `flex: 1` and the date its whole width, and a word cannot wrap (`src/templates/pdf/shared/PdfItemHeader.jsx:75-82 (EndRow); src/templates/pdf/shared/PdfSectionsTwo.jsx:109 (Projects); src/templates/pdf/shared/PdfSidebarSections.jsx:63, :78 (the Sidebar card: its date prints at entry size − 1.5 pt)`) Repro: (1) Sidebar → Projects → Section Options → Grids 2: "Ledgerline" (57.6 pt wide) gets 45 pt beside "02/2022 – 08/2022" and runs 4.6 pt into it, "Queuebird" likewise; (2) Sidebar → Typography → Entry size at its largest (+13 pt): the card date prints at 22.5 pt (189 pt wide) and the second line of "Staff Engineer" (24 pt, 105.8 pt) gets ~98 pt, over the date. Fix: let the row wrap the date under the title when the widest word does not fit (pdfMeasure widestWord), or give the title that minimum width. Found by the B1 parity matrix — tests/pdf/parity/30-sections and 11-type report these as TODO (registry.mjs KNOWN) until the fix lands. | Confirmed (B1 matrix: pdf.js run boxes) |
| R3-003 | ats · contacts (Bar, Bullet) | Medium | bug | 🔴 Open | — | — | Contact style Bar or Bullet glues the words of a contact value under `pdftotext -raw` in the narrow-space fonts (Lato; by the same measure Source Sans 3 and Literata): the phone reads "+15550142", the location "Austin,TX" (`src/templates/pdf/shared/PdfContact.jsx:13` keepTogether joins a value's words with U+00A0 so it never wraps, and `src/templates/pdf/shared/pdfFontLoader.js:259` widenNarrowSpace widens only the U+0020 glyph; the textkit word-gap floor never sees a no-break space) Repro: Classic, Contact style Bar, font Lato, the demo résumé → `pdftotext -raw` reads "jordan.rivera@example.com | +15550142 | Austin,TX | …"; Icon reads it apart. Fix: widen the no-break space glyph as the space is (widenNarrowSpace), and floor it as a word gap. Found by T8: Academic keeps Icon as its default contact style because of it (tests/pdf/40-ats-parse "every demo résumé reads whole under -raw, lato" failed with Bar). | Confirmed (pdftotext -raw, Classic + Bar + Lato, T8) |
| R3-004 | ats · entry header (pdfminer) | Medium | bug | 🔴 Open | — | — | pdfminer.six's default layout analysis (`LAParams()`: what `pdf2txt.py` and most pdfminer-based résumé parsers run) reads an entry's flush-right dates and location as a column of its own. Where the lines between two entries leave the right-hand strip empty (short bullets), its `group_textboxes` joins consecutive entries' date/location boxes before either joins its entry, and prints them after the next entry's role and company: a job's dates read as the following job's (`src/templates/pdf/shared/PdfItemHeader.jsx` ItemHeader / EndRow: the date at the right end of the title's line, the location under it). Every template lays entries out this way. Repro: Classic's demo résumé, role first, each job's bullets cut to "Shipped release N on time." → `qa-visual-compare/tools/pyenv/bin/pdf2txt.py` prints all three jobs' dates and locations after the Experience section; Compact's demo at its own 9 pt (`demo_compact`) prints job 2's `06/2019 – 02/2022` / `Remote` after job 3's `Web Developer` / `Fabrikam Studio` (A4 and Letter), and its one-pager starter from job 2 on. pdf.js, Poppler's three modes, MuPDF, pypdf and OpenResume read every entry whole and in order. At 11 pt the demos' long bullets reach under the dates and keep them apart, which is why no template's demo showed it before. Found by T9. | Confirmed (pdf2txt.py, Classic demo with one short bullet a job, and Compact's demo, T9) |
| R3-005 | ui kit · overlays (Dialog, useConfirm, toasts) | Medium | bug | 🔴 Open | — | tests/unit/ui-overlays.unit.mjs (skipped) | The kit's overlay tests hang: on CI (run 35970636799, the merge of Lane C into master) `tests/unit/ui-overlays.unit.mjs` was SIGKILLed after 60 s while every other ui-*.unit file passed. The file mounts Dialog, useConfirm and the toast stack over fake-dom through Vite's SSR loader; one of them never settles (a timer, an unresolved confirm, or a render loop) or runs out of memory. The shell these belong to is live (AppRoutes puts the Job Tracker and Boards in it); Playwright passed on that run, so the pages load — whether a real Dialog/toast misbehaves is unknown. Its three suites are `skip`ped with this id, not deleted. Next: find which suite hangs — on CI only (the owner's rule), e.g. un-skip one suite per master push. | Confirmed (CI log) |
| R3-006 | boards · pages (Boards v2) | High | regression | ⏸ Fixed | `b181e7c`, `60b40ff` | tests/pdf/82-board-pages.test.mjs, tests/unit/board-drop.unit.mjs | The Boards pages crash on every board: `src/pages/Boards.jsx` and `src/pages/Board.jsx` still read the v1 shape (`board.lists`, `list.cards`) while `useBoardStore` holds Boards v2 (columns and issues; `boardMigrate.js` turns every v1 board's lists into columns on load). `Boards.jsx:68` `b.lists.reduce(…)` throws as soon as one board exists, and `Board.jsx:100-216` read `board.lists` throughout. The v2 wiring landed half-done in 0b58cb2 ("as the 08:33 crash left it") and nothing opens /boards in a test, so CI stayed green. (`src/pages/Boards.jsx:68,88; src/pages/Board.jsx:100-216; src/hooks/useBoardStore.js:1-3; src/utils/boardMigrate.js:1,55`) Repro: open #/boards, create a board: the page falls into the error boundary; with a v1 board saved, #/boards crashes on load. Found by the jobs cluster, 2026-09-24. **Now:** Both board pages read the v2 store through src/utils/boardView.js (columns and issues shown as the familiar lists and cards) and drag through the real moveIssue / moveColumn, so /boards and /boards/:id open for every saved board and the demo. Deleting a list with cards moves them to the list beside it. Until the planned v2 pages replace these: sprints, WIP limits, hide-done and epics are not shown (v1 never had them). | Confirmed (code) |
