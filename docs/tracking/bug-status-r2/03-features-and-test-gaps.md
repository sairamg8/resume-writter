# Full audit 2026-09-23 — Missing features and verification gaps

> Part of the tracker: [../bug-status.md](../bug-status.md) → **Full audit (`R2-`)**. Status legend as there: ✅ fixed and pushed · ⏸ fixed, local · 🔴 open · ✖ not a bug (proof in the row).
> Verified: *Confirmed* = an independent second agent reproduced it at `5aafca8`–`5b6ecc0`; *Unverified* = the session limit stopped its check — the fix's fail-first test settles it.

| ID | Area | Severity | Kind | Status | Commit | Tests | Defect (where) | Verified |
|---|---|---|---|---|---|---|---|---|
| R2-135 | letter · multiple letters | Medium | missing-feature | ✅ Fixed | `00f65d6`, `152fee2` | tests/pdf/99-cover-letters.test.mjs, cypress/e2e/01-dashboard.cy.js | One letter per résumé: 'New Cover Letter' creates a blank résumé with no name or contacts, listed as a résumé (`src/pages/Dashboard.jsx:121, :217; src/hooks/useResumeStore.js:259`) Repro: Dashboard → New Cover Letter → the letter shows 'Your Name', no contacts; back on the dashboard a 'Cover Letter' card appears as a résumé. **Now:** **Now:** New Cover Letter makes a letter (kind: 'letter', src/utils/letters.js) from a résumé: its name, job title, contacts, photo and look. With one résumé it uses that one; with several a picker lists them most recently edited first, plus Blank letter; with none the letter is blank. The dashboard lists letters in a Cover Letters group of their own, never counted as résumés, with letter thumbnails. Each letter opens on its letter tab and is copied, renamed and deleted like a résumé. Data version 13 marks an older build's 'Cover Letter' résumé (that name, no entries) as a letter and changes nothing else, so it prints as before. Sync and the JSON backup carry the whole record, and an imported letter file opens on its letter tab. **Left for later:** No live link from a letter to its résumé: later edits to the résumé's contacts do not reach letters already made. Job Tracker résumé pickers (JobForm, OverviewTab) still list letters alongside résumés, as they listed the old 'Cover Letter' résumés. | Confirmed |
| R2-136 | Design · page | Medium | missing-feature | ✅ Fixed | `ccc9c21`, `302335d` | tests/pdf/91-page-size-control.test.mjs, tests/pdf/91-page-size-round-trip.test.mjs, tests/unit/page-size.unit.mjs, tests/pdf/parity/registry-design.mjs | Page size (A4 / US Letter) has no control: the PDF, Word and preview honour settings.pageSize, but nothing in the UI writes it (`src/constants/pageSize.js:13-18 (PAGE_SIZES, PAGE_SIZE_IDS, dims 'as the editor states them'); src/components/DesignPanel.jsx (no page control)`) Repro: Look through Design → every section for a paper size choice. There is none. **Now:** Design → Spacing offers Page size, A4 · 210 × 297 mm or US Letter · 8.5 × 11 in; the résumé PDF (= the preview), its cover letter and both Word files print on the paper picked, and Backup JSON and JSON Resume keep it through export → import. A résumé that stores none prints on A4 as before. | Unverified |
| R2-137 | Personal Info → Header spacing | Medium | missing-feature | ✅ Fixed | `7d175c3`, `7aa467a`, `f35e664` | tests/pdf/92-header-spacing-rows.test.mjs, tests/pdf/27-header-spacing.test.mjs, tests/pdf/73-typed-number-fields.test.mjs, tests/pdf/parity/registry-header.mjs, tests/pdf/parity/01-offered.test.mjs | Header spacing, which the owner asked for: 6 of the 13 engine keys still have no control, and Modern and Sidebar hard-code the matching gaps (`src/utils/headerSpacingRows.js:11-19 (TEXT lists 7 keys); src/templates/pdf/ModernTemplatePDF.jsx:79-82,103; src/templates/pdf/SidebarTemplatePDF.jsx:204`) Repro: Open a Modern résumé → Personal Info → Header spacing. There is no Contacts ↔ Summary, Banner padding, Header ↔ First section or Text ↔ Border row. **Now:** **Now:** Personal Info → Header spacing offers Contacts ↔ Summary, Text ↔ Border, Banner padding (top & bottom, sides) and Header ↔ First section, each only where it moves something. Modern and the Sidebar print them instead of fixed gaps and are unchanged when unset. The cover letter follows Header ↔ First section and Modern's band padding. Word follows a set Contacts ↔ Summary and Header ↔ First section. The letter's own Name ↔ Contacts gap (contactsSideGap) is a stepper under Cover Letter → Header Layout → Right of Name. **Left for later:** Word draws no Modern banner and no side-by-side letter contacts, so headerPadX/Y and contactsSideGap have no Word counterpart. contactsSideGap has no parity-registry entry because the parity walker does not walk the Cover Letter panel; tests/pdf/92-header-spacing-rows covers it instead. | Unverified |
| R2-138 | Templates (B1, B2, B4) | Medium | missing-feature | 🔴 Open | `c75f02d`, `c2eff4d` | tests/pdf/93-template-presets.test.mjs, tests/pdf/42-ats-fields.test.mjs, tests/pdf/parity/00-registry.test.mjs, tests/pdf/parity/15-template-resets.test.mjs | 'At least 10 templates' is not started: still 5 engines, no presets, no new layouts, no save-my-design (`src/constants/templates.js:59-85`) Repro: Design → Template lists 5 entries. **Partly fixed (2026-09-24):** B1 shipped. Design → Template now offers 17 choices: the 9 templates plus 8 designs. The designs are Harbor, Ledger, Nordic, Crimson, Midnight, Sunrise, Grove and Inkwell. Each one is a named look over an existing engine: its own font and accent, plus a heading style, header and spacing (src/constants/templatePresets.js). A design is picked through the store's setTemplate(engine, preset), using the same switch the parity walker uses (withTemplate). It is stored as settings.templatePreset, and the picker marks its card. Picking a plain template takes the design's look away wherever the user kept it. Reset returns to the design. JSON Resume writes it as meta.design and imports it back; Backup keeps it. It never writes a section's own settings. Every design passes the ATS field battery on pdf.js and every Poppler mode (42-ats-fields). Its badge is atsRating over its engine and settings. Its name and job title read on its header. Its values stay inside the panel's ranges. The parity registry measures the new control: a design picked prints what a résumé started on it prints, unlike its engine, and its accent prints. Word prints the designs' fonts and accents. With no design set, everything prints as before. **Left:** B2: new layout code (the ten designed layouts). B4: save my own design. A preset-keyed entry layout (resolveSection) was not built, because designs deliberately leave section settings alone. The dashboard card and the cover-letter panel name the design's template, not the design. | Unverified |
| R2-139 | Template picker / dashboard / new-résumé flow | Medium | missing-feature | 🔴 Open | `4a67f20`, `8142dae` | tests/pdf/93-template-picker-copy.test.mjs, tests/pdf/69-picker-ats-badge.test.mjs, cypress/e2e/04-design.cy.js, cypress/e2e/05-cover-letter.cy.js, cypress/e2e/07-regressions-store.cy.js, cypress/e2e/14-contacts.cy.js | None of the Templates UI suggestions is built (A1–A14, B3, C1, C2, D1, E1, F1) (`src/components/DesignPanel.jsx:53-60,66,87; src/components/EditorHeader.jsx:107-113; src/components/ResumeCard.jsx:30-50,88-89; src/components/DesignPanelShared.jsx:107-117; src/constants/templates.js:61`) Repro: Open Design → Template on a 375 px screen, then open the dashboard: every card looks the same. **Partly fixed (2026-09-24):** Task 'one atsRating, honest badges, switch copy, name on card'. A10 and C2 were already done on master: one atsRating(template, settings) feeds the picker badge and the ATS Check (TUI-5 / R2-011, pinned by 69-picker-ats-badge), and each dashboard card names its template (b7a5e21, R2-133). Now (A6): each template card says what its engine draws and makes no ATS claim in words. The badge makes that claim, from atsRating. Classic no longer claims a 'Two-column header'. Executive names its role-first entries on one line. The Sidebar card names the page its Layout prints. (A5): one line under the picker says what a switch keeps and what it changes. A test runs every pair of templates and checks that a switch changes only the settings that line names. The cards carry data-testid="template-<id>", and the Cypress specs pick templates by it rather than by the old descriptions. **Left:** Deferred by the owner (accessibility): A7 Design button label/size/aria-pressed, A8 radiogroup/aria-checked + visible Selected marker, A11 44 px SegmentControl targets, A13 description contrast, A14 aria-expanded on DesignSection (parked a11y commit 67c88c5 on claude/wf-templates). Not built: A1 thumbnails, A2 gallery dialog, A3 filter chips, A4 undo a switch, A9 two Sidebar picker entries, A12 keep a collapsed Template open state, B3 categories, C1 page-1 card image, D1, E1 phone sheet, F1 letterhead thumbnail. | Unverified |
| R2-140 | Boards / Job Tracker / cloud sync | Medium | missing-feature | ✅ Fixed | `e96514b`, `8cd4901` | tests/unit/board-sync.unit.mjs, tests/pdf/95-sync-privacy-notices.test.mjs | Boards Phase 3 (Google sync) and Phase 4 (tests) not started, and Boards and the Job Tracker never leave localStorage while the Privacy page says all content syncs (`src/hooks/useBoardStore.js:16,260; src/pages/PrivacyPage.jsx:50,60; boards-plan.md Phase 3-4`) Repro: Sign in, create a board and a job, then clear site data (or open another device). The résumés come back; the board and the job do not. **Now:** **Now:** boards-plan Phase 3 and 4 are done on the same shared engine as the jobs (collectionSyncEngine/Plan/Io/Meta + useCollectionSync, wired in App.jsx) rather than a third copy of the sync code. One document per board, per-board last-writer-wins, first sync, debounced queue, offline/retry, and sign-out that takes the boards off the browser. A board over Firestore's 1 MiB is held back and named on every board page while the rest keep syncing. Tests use the real board store over fake-firestore. **Left for later:** Order is last-writer-wins at first sync: a reorder made offline gives way to the cloud's order (content is never lost). A delete sent after the first sync does not check for a later edit elsewhere. No sync-status icon for jobs or boards in the AuthBar (plan's 'polish'). | Unverified |
| R2-141 | ATS checker / ATS battery | Medium | missing-feature | ✅ Fixed | `39c7925`, `83a2414`, `db48a15`, `1229e78`, `9f705bd`, `c11e001`, `7cc6ec7`, `9221b0a`, `2b23b72` | tests/pdf/98-ats-claims.test.mjs, tests/pdf/98-ats-parser-view.test.mjs, tests/pdf/98-ats-job-fields.test.mjs | The ATS checker's claims are unproven and it misses layouts the research says are risky; 'what a parser reads' and per-field scoring are not built (`src/components/AtsCheckerPanel.jsx:208; src/utils/atsChecker.js:1073-1074; tests/pdf/ats-fields.mjs:210`) Repro: Set Experience → Grids 2 and open ATS Check: the layout score is unchanged. **Now:** The ATS Check says only what the ATS battery reads from the app's own PDF: the template verdicts, the Sidebar and side-by-side (Grids 2) warnings name pdf.js and Poppler instead of the untested "100% sequential parsing on Workday, Taleo, and Greenhouse" / "modern AI parsers handle sidebars", a LinkedIn printed under a Display label warns (no reader finds it), and the tab header no longer says "Tested for Workday, Taleo, Greenhouse, Lever & iCIMS" — tests/pdf/98-ats-claims sets each claim beside pdf.js and pdftotext. A new collapsible "What a parser reads" in the ATS tab shows the text of the résumé's own PDF as pdf.js reads it (lines of runs by the battery's item-parser rules), with Copy, re-read after a pause in typing, Retry on error and a side-by-side caveat; under it each job's title, company, dates and location is marked as a field of its own, sharing a run with other text, or not found. | Unverified |
| R2-142 | Editor responsiveness | Medium | missing-feature | 🔴 Open | `9633e36`, `541c85f`, `992725a`, `ef1fabb`, `239ea7f`, `959c8cb` | tests/pdf/97-preview-steady-typing.test.mjs, tests/pdf/97-store-same-value.test.mjs, tests/pdf/97-store-sign-out-write.test.mjs, tests/pdf/97-store-other-tab-same.test.mjs | Performance plan PERF-1…6 not started, and the PERF-1 work-in-progress clone is gone (`src/components/PdfPreview.jsx:16,111; src/hooks/useResumeStore.js:111`) Repro: Type a sentence in Summary on a 3-page résumé: the preview stays frozen until typing stops. **Partly fixed (2026-09-24):** Typing that never pauses now repaints the preview at least every 1.2 s. It no longer freezes until typing stops, and it still runs one build at a time; the stale-render drop, zoom repaint, hidden-preview pause and Retry work as before. The store's saves stay coalesced (R2-077). They are written at once on pagehide, beforeunload, the tab being hidden, unmount, and now also when the list changes account (sign-out), so no edit is lost. A value set to what it holds already leaves the store as it was: nothing is written, built or synced. Another tab's save keeps the unchanged résumés as the same objects here, so the open résumé's preview is not rebuilt for nothing. Cloud sync reads the in-memory state, not storage, so it needs no flush. **Left:** PERF-1 (harness + WOFF cache), PERF-4 (editor commits + colour pickers), PERF-5 (pdf.js paint + chunk split), PERF-6 (PDF in a Web Worker). | Unverified |
| R2-143 | Open-source release | Medium | missing-feature | 🔴 Open | `52e864f`, `d7a98d9`, `40cf922` | tests/unit/site-owner.unit.mjs, tests/pdf/79-site-owner.test.mjs, tests/pdf/38-owner-resume-not-embedded.test.mjs, tests/pdf/18-cloud-sync-waiting-notice.test.mjs, cypress/e2e/11-demo-account.cy.js, cypress/e2e/11-demo-account-keep.cy.js, cypress/e2e/20-regressions-job-store.cy.js | The open-source release checklist is mostly undone: LICENSE and CONTRIBUTING are missing, the README is wrong, owner-specific defaults remain in the source, and there is no tag (`README.md:12-13,113-144,205-211; src/utils/demoAccounts.js:9; src/hooks/useAuth.js:23,34`) Repro: Clone the repo: there is no LICENSE file, and the README's file map and template list do not match src/. **Partly fixed (2026-09-24):** The owner's e-mail, uid and name come from VITE_* env (src/utils/siteOwner.js, documented in .env.example): VITE_DEMO_ACCOUNTS, VITE_CONTACT_EMAIL, VITE_DEV_USER_UID/EMAIL/NAME. A fork that sets none gets no demo account, Terms and Privacy pages that say 'Contact the people who run this site.', and a made-up dev sign-in (dev@example.com). No src file holds a personal mailbox address. The e2e build's demo account is the made-up one in the tracked .env.e2e. The owner's résumé file was already dev-only and in no bundle (vite-plugin-owner-resume.js, tests 24/38), and the app already ran fully without Firebase (firebase.js firebaseEnabled; useAuth cloudAvailable). The README now matches src/: file map, templates, routes (boards, backlog, settings, Your work), scripts, the CI gate and its inputs, and the env vars. **Left:** Owner only: tag v0.1.0; confirm the Terms and Privacy pages match the hosting domain; set the env vars listed in notes on the live site's build BEFORE this reaches master. Also cosmetic: the git-ignored private file is still named private/sairam-resume.json (vite-plugin-owner-resume.js default; it is not in any bundle). **Partly fixed (2026-09-24):** The owner's e-mail, uid and name come from VITE_* env (src/utils/siteOwner.js, documented in .env.example): VITE_DEMO_ACCOUNTS, VITE_CONTACT_EMAIL, VITE_DEV_USER_UID/EMAIL/NAME. A fork that sets none gets no demo account, Terms and Privacy pages that say 'Contact the people who run this site.', and a made-up dev sign-in (dev@example.com). No src file holds a personal mailbox address. The e2e build's demo account is the made-up one in the tracked .env.e2e. The owner's résumé file was already dev-only and in no bundle (vite-plugin-owner-resume.js, tests 24/38), and the app already ran fully without Firebase (firebase.js firebaseEnabled; useAuth cloudAvailable). The README now matches src/: file map, templates, routes (boards, backlog, settings, Your work), scripts, the CI gate and its inputs, and the env vars. **Left:** Owner only: tag v0.1.0; confirm the Terms and Privacy pages match the hosting domain; set the env vars listed in notes on the live site's build BEFORE this reaches master. Also cosmetic: the git-ignored private file is still named private/sairam-resume.json (vite-plugin-owner-resume.js default; it is not in any bundle). | Unverified |
| R2-144 | Editor layout / accessibility | Low | missing-feature | ✅ Fixed | `2561233`, `e0ab2e3` | tests/unit/panel-resize-separator.unit.mjs, tests/unit/panel-resize-storage.unit.mjs | The split-view resize handle is mouse-only: no touch drag (tablets at 768 px and wider are in split mode), no keyboard, no separator semantics (`src/pages/Editor.jsx:140-142; src/hooks/usePanelResize.js:10-38`) Repro: On a tablet in split view, drag the grey bar between editor and preview: nothing moves. **Now:** The split-view handle is a focusable role=separator with aria-orientation=vertical, aria-valuenow/min/max and a label. Arrow keys move it 16 px and Home/End go to 240/640. It drags with pointer events (mouse, pen or finger; pointercancel ends the drag), with touch-action: none and a wider hit area over the preview. Checked in real Chromium with touch, mouse and keyboard. | Unverified |
| R2-145 | jobs/boards · persistence and sync | Low | missing-feature | ✅ Fixed | `e96514b`, `7f0d3b8`, `8cd4901` | tests/unit/job-sync.unit.mjs, tests/unit/firestore-rules.unit.mjs, tests/pdf/95-sync-privacy-notices.test.mjs | Job tracker (and boards) never sync or back up to the cloud, but the Privacy page says all content syncs and is restored after clearing local storage (`src/pages/PrivacyPage.jsx:50 and :61; src/hooks/useJobStore.js:7; src/hooks/useBoardStore.js:6-8`) Repro: Sign in, add jobs and boards, clear site data, reload and sign in again: résumés come back, but jobs and boards are gone. **Now:** **Now:** signed in, the Job Tracker's jobs sync with the account: one document per job under users/{uid}/jobs, and users/{uid}/meta/jobs lists deleted ids and the order. The first sync merges job by job (newer updatedAt wins, nothing lost, a deleted job stays deleted, a fresh demo never overwrites the account's copy). Later changes are sent after a pause and checked against the cloud's copy; offline and failed sends retry. Sign-out takes the jobs off the browser, keeping unsent changes aside for that account. The Privacy page says exactly what syncs: résumés, jobs and boards. | Unverified |
| R2-146 | Design → Typography | Low | missing-feature | ⏸ Fixed | `d141c53`, `ba90225`, `f1208ca`, `ae78679`, `32074e8` | tests/pdf/93-side-title-spacing.test.mjs, tests/pdf/93-name-heading-font.test.mjs, tests/pdf/93-word-font-fallback.test.mjs, tests/pdf/91-design-resets.test.mjs, tests/pdf/parity/registry-design.mjs, cypress/e2e/13-regressions-fonts.cy.js | Typography gaps: no name font, job-title size, heading letter-spacing or per-section font, and the offline font fallback is silent (`src/components/DesignPanelTypography.jsx:144-148; src/templates/pdf/shared/PdfSection.jsx:24; src/templates/pdf/shared/pdfFontLoader.js:218-225`) Repro: Pick Lora, go offline and reload the editor: the PDF prints in Noto Sans and nothing says so. **Partly fixed (2026-09-24):** **Now:** A chosen web font that can't load is named in a notice above the preview ("Lora could not be loaded — the PDF uses Noto Sans until you are back online"). The notice clears once the font loads, and the preview retries when the browser comes back online. Preview and export use the same fallback. Typography → Title Spacing (sectionLetterSpacing, % of title size) prints in the PDF section titles and in Word. Typography → Job Title (fontSizeTitleDelta) sizes the job title on every template, the letterhead and Word. Unset, both print as before. **Left:** Separate name font and per-section font were not started. Title Spacing does not reach the Sidebar's side-column titles. Word's font fallback is unchanged (Word does not embed fonts). **Now:** Typography → Name Font and Heading Font (settings.nameFont / headingFont: a picker font or a saved custom font; 'Same as text' = unset) print the name (every template, the cover letter's letterhead) and the section titles (main column, the Sidebar's About Me and side-column titles) in fonts of their own, in the PDF and in Word; one that cannot load prints in Noto Sans and is named in the font-fallback notice ('Lato and Inter could not be loaded …'); the Typography ↺ resets both. Title Spacing now reaches the Sidebar's side-column titles (PDF and Word). The Word file's font table names an installed stand-in for every font it uses — Arial for a sans, Georgia for a serif (w:altName + PANOSE + family), none for Georgia — and Typography says so ('Word does not embed fonts: where they are not installed, Inter shows as Arial …'). Unset, the PDF prints as before. | Unverified |
| R2-147 | Section Options / Design | Low | missing-feature | 🔴 Open | `c017a8b`, `14e212c`, `19ce8d1`, `39f4a60`, `893aa53` | tests/pdf/94-bullet-style.test.mjs, tests/pdf/94-skills-pipe.test.mjs, tests/pdf/94-page-numbers.test.mjs, tests/pdf/parity/17-lists.test.mjs, tests/pdf/parity/18-page-numbers.test.mjs | Per-section styling and layout parity items not started (section icons, bullet marker, link style, level visuals, pipe separator, grouped roles, page-number footer, column layout, photo greyscale and position) (`src/utils/richText.js:186; src/components/SectionEditorCustomizer.jsx:104,113; src/templates/pdf/shared/PdfSectionsOne.jsx:110; src/templates/pdf/shared/PdfSidebarSkills.jsx:50; src/constants/photoOptions.js:12-18`) Repro: Section Options for Skills: there is no level or pipe choice, and no bullet-marker choice anywhere. **Partly fixed (2026-09-24):** Design → Lists offers Bullet •, Dash –, Circle ◦ or None for every rich-text bulleted list (summary, descriptions, legacy bullets[], cover letter) in the PDF and Word, the text where it was; unset prints as before. Skills' Separator offers Pipe ('Languages \| JavaScript') in the PDF main and side columns and Word. Both pass on CI (runs 36088243126, 36088741695; pipe fail-first ok). **Left:** FAILING: the page-number footer (19ce8d1, 893aa53) — CI finds no 'Page n of N' in the PDF on any template and the Sidebar's content moves (94-page-numbers tests 1–2, parity/18-page-numbers on all 10 variants); the Word footer and the cover-letter check pass. Do not merge 19ce8d1/893aa53 as-is (or merge with page numbers left off by default — off prints as before, but its tests are red). Next steps in wf-resume/section-style.md. Fail-first results for c017a8b (run 36088243126) and 39f4a60 (run 36088741695) not read yet. Rest of the row: section icons, link styling, level visuals, grouped roles, column layout, photo greyscale/position. | Unverified | **Withdrawn 2026-09-25:** Design → Page numbers (19ce8d1…) printed no number on a résumé's first page after four fixes (f0f9784, 1923da1, 9e4c0fa); taken out of this release with its tests — open again.
| R2-148 | Import / localisation / letters | Low | missing-feature | 🔴 Open | `c9fbe96`, `dd867fc`, `4331297`, `96726aa`, `083b7d5`, `07d8051`, `a33931f`, `4284972`, `a23ecb7`, `1582dc0`, `b572d25`, `8d856ff` | tests/pdf/99-public-link.test.mjs, tests/unit/firestore-rules.unit.mjs, tests/pdf/99-import-roundtrip-columns.test.mjs, tests/pdf/99-import-roundtrip-one-column.test.mjs, tests/unit/import-pdf-columns.unit.mjs, tests/unit/import-file-errors.unit.mjs | 'Beyond design' items not started: import from PDF/DOCX/text, résumé language and RTL, several cover letters, public link (`src/components/ImportMenu.jsx:39; src/utils/dates.js:44-46; src/utils/defaultData.js:111`) Repro: Import menu: only JSON files are accepted. **Partly fixed (2026-09-24):** **Now:** Import on the Dashboard and in the editor's Export menu takes .json, .pdf, .docx, .txt and .md. A PDF, Word, Markdown or text résumé becomes a new résumé: name, job title, contacts and summary go in the header, each known heading gets a section with its entries found by their dates, and every line it cannot place is kept in a custom section. The editor then shows a best-effort notice until it is dismissed. JSON imports as before. An old .doc is asked to be saved as .docx, a damaged Word file says so, and a file over 20 MB is refused unread. The app's own PDF, Word, Markdown and ATS-text exports round-trip back through it. No new dependency: .docx is unzipped with DecompressionStream. **Left:** Not built: résumé language for dates (month names, 'Present'), RTL, and the public web link. Several cover letters is done by R2-135. Only the Classic single-column PDF is round-trip tested: two-column PDFs (Sidebar, Compact grid) interleave their columns, and scanned PDFs get a 'no text' error. **Partly fixed (2026-09-24):** **Now:** A signed-in account on a site with Firebase gets Export → Share a public link: it lists exactly what anyone with the link sees, publishes a read-only copy (only what the PDF prints: no hidden field, link, section, entry, date or location, no cover letter, dashboard name or id) at #/r/<random id>, says when the résumé changed since and updates it, and unpublishes it; deleting the résumé on the Dashboard takes the copy down. The link's page prints the copy with the editor's PDF preview and a Download PDF button. firestore.rules lets anyone get a public/{shareId} copy by id (never list), and only its owner write it; without Firebase nothing offers it, and the Privacy page, README and docs say so. PDF import reads the Sidebar's two columns in order (side column, then main) and Compact's inline name/title and grid; a PDF with no text layer says it looks like a scanned image and to export as text, save as Word or run OCR. **Left:** Not built: résumé language for dates (month names, 'Present') and RTL. A résumé deleted on another device keeps its public copy until unpublished (the copy's panel is gone; would need the sync to unpublish). PDF import: only two columns, left first (a right-hand sidebar reads after the main column); on Executive and Timeline PDFs the parser's company/role guess is off (company read as the location / empty) — likely pre-existing, not proven on base. | Unverified |
| R2-149 | Design → Spacing presets | Low | missing-feature | ✅ Fixed | `c3ce291`, `2449fba`, `6f30051`, `314052d`, `fe9709e`, `396af0e` | tests/pdf/91-page-fit.test.mjs | '1-Page Fit' writes fixed numbers and never checks the page count (`src/components/DesignPanel.jsx:162-172`) Repro: Take a 3-page résumé and click 1-Page Fit: it is still 2+ pages. **Now:** 1-Page Fit writes its preset at once, then prints the résumé with the app's own renderer and, while it runs past one page, steps down a ladder (margins, section/entry gaps, line height, then the base font size a point at a time to 9 pt, all inside the panel's ranges), keeping the first step that fits. When even the tightest is over a page it keeps the tightest and says 'Still N pages at the tightest spacing — shorten the content to fit one page.' Content is never touched; the button reads 'Fitting…' and ignores clicks while it measures, and stops without writing if the résumé is edited, switched or closed meanwhile. Balanced and Spacious are unchanged. | Unverified |
| R2-150 | sections · present/current flags | Low | missing-feature | ✅ Fixed | `232a601`, `d1603f4` | tests/pdf/88-present-entries.test.mjs, tests/unit/json-resume-present.unit.mjs | Education, Volunteering and Projects have no 'Present' or current option; an ongoing one prints its start date alone (`src/components/SectionEditorEntryItems.jsx:53, :71, :88 (plain End Date); src/templates/pdf/shared/PdfSectionsOne.jsx:225, PdfSectionsTwo.jsx:96 and :209 (never read `current`)`) Repro: Education entry started Sep 2022 and still studying → the only option is to leave End Date blank → the PDF prints 'Sep 2022'. **Now:** Education, Projects and Volunteering have Experience's current flag ('Currently studying here', 'Ongoing project', 'Currently volunteering here'), which clears and disables End Date. A current entry prints '<start> – Present' in the PDF on every template, and in Word, Markdown and ATS text (endDateOf in dates.js). The flag survives a JSON Resume export and re-import. | Confirmed |
| R2-151 | sections · entry/section actions | Low | missing-feature | ✅ Fixed | `64f3fb8` | tests/pdf/88-duplicate.test.mjs | No duplicate for entries or sections (`src/hooks/useResumeSectionActions.js:78 (actions list); src/components/SectionEditor.jsx:115–145 (menu); src/components/SectionEditorShared.jsx:100–128 (ItemCard)`) Repro: Try to copy an experience entry to reuse its layout → there is no control for it. **Now:** Each card-style entry has a Duplicate button, and the section menu has 'Duplicate section'. The copy is a deep copy placed right after the original, with a fresh unique id for the section and for every entry. **Left for later:** Language and Interest rows (single-line entries without a card) have no duplicate button. | Confirmed |
| R2-152 | Test suites · Cypress | High | verification-gap | ✅ Fixed | `7007dd3`, `f00c26e`, `dd6325e`, `94d0ba9` | cypress/e2e/01-dashboard.cy.js, cypress/e2e/03-export.cy.js, cypress/e2e/06-job-tracker.cy.js, cypress/e2e/07-regressions-store.cy.js, cypress/e2e/09-regressions-deletes.cy.js, cypress/e2e/11-demo-account.cy.js, cypress/e2e/11-demo-account-keep.cy.js, cypress/support/demoAccount.js, cypress/e2e/14-contacts.cy.js, cypress/e2e/19-regressions-ids.cy.js, cypress/e2e/20-regressions-job-shape.cy.js, cypress/e2e/20-regressions-job-store.cy.js, cypress/e2e/20-regressions-job-unsaved.cy.js, cypress/e2e/21-a11y.cy.js, cypress/e2e/24-image-uploads.cy.js, cypress.config.js | Cypress suite is not green and not runnable in the workflow: at least 3 specs are stale against HEAD (`cypress/e2e/01-dashboard.cy.js:20-56; cypress/e2e/06-job-tracker.cy.js:104; cypress/e2e/14-contacts.cy.js:107,138,267; /mnt/Storage/my-learning/claude/flowcv/pdf-harness/e2e-wt.sh (refuses unless FLOWCV_E2E_ALLOW=1)`) Repro: FLOWCV_E2E_ALLOW=1 e2e-wt.sh /mnt/Storage/Projects/flowcv 4174 --spec cypress/e2e/06-job-tracker.cy.js (not run here: read-only audit). **Now:** **Now:** the whole Cypress suite (33 specs, 314 tests) passes against the e2e build, with retries off. At 504b313, 12 specs failed with 36 failing tests. Every failure was a stale spec: the app had changed on purpose (the starter picker, the second compact header for phones, the renamed export buttons, J-04's import merge, the reworded icon hint, taller panels). One spec had its own bug (ONB-10's second cy.visit never loaded the page). One spec had a race (R6-2 filled storage before the demo job was saved). No app bug was hidden: every assertion about app behaviour was kept or updated to the intended behaviour. The download tasks now use the configured downloadsFolder, so two runs can go side by side. | Unverified |
| R2-153 | Test suites · Playwright | Medium | verification-gap | ✅ Fixed | `a9262ff` | tests/playwright/parity-preview-download.spec.mjs, tests/playwright/parity-ui-controls.spec.mjs | Playwright suite tests a stale bundle: `yarn test:pw` never builds and serves whatever is in ./dist (`playwright.config.js:22-27 (webServer 'npm run preview', reuseExistingServer: true); package.json scripts.test:pw`) Repro: ls -la dist/assets; git log --since='2026-09-22 21:13' --oneline -- src \| wc -l → 25. **Now:** fixed in `a9262ff`: `yarn test:pw` builds first (`vite build --mode production && playwright test`), and `playwright.config.js` serves `PW_DIST` on `PW_PORT` with `--strictPort` (default ./dist on 4173) and reuses a server only for a local default-port run. The browser suite now also runs on every push in GitHub Actions against that push's own build (`.github/workflows/ci.yml`, `856a449`), so it can no longer test a stale bundle. | Unverified |
| R2-154 | Push gate | Medium | verification-gap | ✅ Fixed | `f5d5d69` | .github/workflows/ci.yml (dispatches 36088228020, 36088456748) | Push gate is advisory: exits 0 when the suite or build fails, hides skipped tests, never runs browser suites or lint (`/mnt/Storage/my-learning/claude/flowcv/wip/pushgate.sh:18-22 and last line`) Repro: Make one node test fail and run pushgate.sh: it prints 'suite exit 1' and still ends 'gate done' with status 0. **Now:** The pushgate.sh the row cites is replaced by .github/workflows/ci.yml, and its claims no longer hold. A failing node test fails its job: steps run under bash -e -o pipefail (defaults.run.shell: bash, ci.yml:59-61) with set -o pipefail and tee (ci.yml:80-84, 127-129). A failed build fails its job (vite build, build job). Browser suites and lint run on every master push and on a full dispatch (the suite/build/playwright/cypress if: conditions start with github.event_name == 'push'; lint has none). The one real gap, skipped tests being hidden, is fixed: the Summary steps of the suite and tests jobs now emit a ::warning:: naming each skipped test. It is additive and non-fatal, and the dispatch inputs are unchanged. Run 36088228020's log shows '##[warning]1 skipped: … # SKIP private/sairam-resume.json is not on this machine'. | Unverified |
| R2-155 | Boards | Medium | verification-gap | ✅ Fixed | `b181e7c`, `f5b92ad`, `60b40ff` | tests/unit/board-drop.unit.mjs, tests/pdf/82-board-pages.test.mjs | Boards feature has zero tests anywhere (store, normaliser, pages, drag-and-drop) (`src/hooks/useBoardStore.js (exports _resetBoardStoreForTest, never used); src/utils/normalizeBoard.js; src/pages/Boards.jsx; src/pages/Board.jsx:109-136 (onDragEnd → moveList/moveCard)`) Repro: n/a (coverage gap). **Now:** Store, normaliser, ops, migration and transfer tests already existed (tests/unit/board-*.unit.mjs, normalize-board.unit.mjs). Now added: drag-and-drop (boardView.dropTarget over the real moveIssue/moveColumn, every index checked against arrayMove) and both pages (SSR markup, plus a fake-dom mount driving add card, labels, delete list and the card title). | Unverified |
| R2-156 | Job tracker pages | Medium | verification-gap | ✅ Fixed | `addbb40`, `79afdf1` | tests/pdf/81-job-tracker-ui.test.mjs | Job tracker UI (list/kanban/filters, JobDetail tabs: tasks, notes, stages, pipeline, status history) is verified only by Cypress, which is partly stale and never run (`src/pages/JobTracker.jsx, src/pages/JobDetail.jsx, src/components/job/{KanbanView,ListView,Pipeline,TasksTab,NotesTab,StatusHistory,InterviewStageSelector,OverviewTab,TodoItem,Field}.jsx`) Repro: n/a (coverage gap). **Now:** The kanban (columns, tasks, notes text, open, delete, keys, sensors), list (open button, sorting), status history, Tasks tab and the job page's tabs are tested on the real components over fake-dom. **Left for later:** Pipeline, InterviewStageSelector, TodoItem and JobTracker's filters have no fake-dom test of their own yet (Pipeline and Field partly via 68-job-overview). | Unverified |
| R2-157 | Design panel / Personal info controls | Medium | verification-gap | ✅ Fixed | `dac527f`, `f16a4dc` | tests/pdf/91-design-resets.test.mjs, tests/pdf/91-design-presets.test.mjs, tests/pdf/91-design-typography.test.mjs, tests/pdf/91-icon-picker-modal.test.mjs, tests/pdf/91-starter-modal.test.mjs | Design-panel presets, section resets and pickers have no test in the gate (`src/components/DesignPanel.jsx:35-39 (resetSection), :164-200 (page-fit presets), :131-146 (icon size); src/components/DesignPanelTypography.jsx:27-50 (custom font add/remove), :56-64 (setBase delta keeping), :111-117 (Small/Normal/Large); src/components/HeaderIconPickerModal.jsx; src/components/StarterTemplateModal.jsx`) Repro: n/a (coverage gap). **Now:** The gate now drives the real components over the fake DOM: every Design section's ↺ (exact writes, template defaults for Academic/Compact/Sidebar/Executive, and keys no ↺ may touch), Balanced and Spacious, the icon size stepper and its limits, Small/Normal/Large and Base with delta keeping, custom font add/remove/unknown, the header icon picker (open, pick, packs, search, close/×/backdrop write nothing, reset) and the starter picker (each starter, blank, ×, the store's createResume). A bug the tests found is fixed: the header icon picker kept the last field's search and tab when opened for another field; it now opens fresh (f16a4dc, fail-first ok). Already covered before: 86-icon-reset-keeps-contact-style, 15-design-defaults, 86-line-height-shown, 69-word-spacing, 86-icon-size-steps, 16-saved-data-typography, 09-contact-icons, starter-templates.unit, 16-saved-data-starter-skills; Cypress 30/13/04 smoke. | Unverified |
| R2-158 | Section editor · structure | Medium | verification-gap | ✅ Fixed | `ee4d19f`, `c1f085d` | tests/pdf/96-section-structure.test.mjs, tests/pdf/96-structure-keyboard.test.mjs, tests/pdf/resume-tab.mjs | Section/entry structure actions: add, rename, hide, delete, and reorder (drag or keyboard) are untested in the gate; reorder is untested anywhere (`src/components/EditorResumeTab.jsx:28-35 (handleSectionDragEnd → updateSections(arrayMove)); src/components/SectionEditor.jsx:46-51 (handleItemDragEnd → reorderItems); src/hooks/useResumeSectionActions.js:40-73; src/hooks/useResumeStore.js createResume/duplicateResume/renameResume/keepResume`) Repro: n/a (coverage gap). **Now:** The Résumé tab's structure actions run on the real components and store in the gate: Add Section (a new section goes last, open, with its first entry; the picker closes), renaming in the title box, the eye (hide, then show), ⋯ → Delete section (the confirm names the section and counts its entries; Cancel keeps it), an entry's Add and Delete (an untouched entry goes without asking), and reordering sections and entries by a drop (through the tab's and the section's own onDragEnd) and by the keyboard (dnd-kit's own KeyboardSensor: Space, the arrow keys, Space or Escape). Each result is checked in the store, on the tab and in the printed PDF. The tests found a real bug: a section let go over nothing threw on over.id and the tab went down. **Now:** it stays where it was (c1f085d, proven fail-first on CI). | Unverified |
| R2-159 | jobs/boards · tests | Medium | verification-gap | ✅ Fixed | `b2a73c6`, `ae65084` | tests/pdf/95-job-notes-tab.test.mjs | Boards have no tests at all, and the job kanban drag, Pipeline, Tasks and Notes are untested (`tests/ (no board file); src/utils/normalizeBoard.js:6 cites a non-existent tests/unit/normalize-board.unit.mjs`) Repro: n/a **Partly fixed (2026-09-24):** Boards now have tests (see R2-155), and the file normalizeBoard.js cites exists. The job side: Pipeline (every state), the Tasks tab (add, tick, rename, delete, newest completed first) and the kanban drop (another column changes the status; its own column or outside does nothing) are tested on the real components. **Left:** NotesTab (RichTextEditor, contenteditable) is still untested. **Now:** **Now:** a job's Notes tab has a test on the real component: stored notes show sanitized, and typing, each toolbar command, a link, a paste and clearing write the notes through the job store (CI 36021742269, green again in 36089463049). Test-only: no bug found. | Unverified |
| R2-160 | Test coverage of controls | Medium | verification-gap | ✅ Fixed | `6ae3d50`, `443321a` | tests/pdf/parity/*.test.mjs (00-registry, 01-offered, 10…31, 40-structure) | The 'tests cover every customisation control' order is open: no registry or meta-test, reorder has no test, underline is unchecked in the PDF and Word, and the Spacing Override tests pass even if the override did nothing (`tests/pdf/51-section-spacing-override.test.mjs:38-45; src/components/EditorResumeTab.jsx:34; src/hooks/useResumeSectionActions.js:66; tests/pdf/02-rich-text-parser.test.mjs:60-64`) Repro: Change getEffectiveSpacing to ignore spaceBefore/spaceAfter and run node --test tests/pdf/51-section-spacing-override.test.mjs. It still passes (by reading the assertions). **Now:** fixed in `6ae3d50` (+ `443321a` for Academic): `tests/pdf/parity/` walks the real panels in the fake DOM on every template and pins every setting key it finds (`00-registry`: a new control without a measure fails the suite), renders each control at every value it offers and asserts its own measured effect in the PDF (spacing overrides move by what they say, underline and reorder included — `40-structure`: section and entry reorder through the store's drag actions, bold / italic / underline in the PDF and the .docx). The browser half (`a9262ff`) proves the preview equals the download. On GitHub CI 2026-09-24: 3756 tests, 0 fail. | Unverified |
| R2-161 | E2E | Medium | verification-gap | ✅ Fixed | `e5750f9`, `32bd830` | cypress/e2e/26-mobile-layout.cy.js, cypress/e2e/27-ats-checker.cy.js, cypress/e2e/28-writing-helpers.cy.js, cypress/e2e/29-exports-imports.cy.js, cypress/e2e/30-starters-and-design.cy.js | Cypress has not run in this checkout since 2026-09-15, and nothing covers the 09-17…09-20 features end to end (`cypress/e2e/*; cypress.config.js:56`) Repro: ls -la cypress/screenshots/11-demo-account.cy.js **Now:** **Now:** each feature from 09-17…09-20 has an end-to-end smoke spec. Each one opens the feature and checks what it shows and what it writes to the store, the PDF or the downloaded file: the ATS tab (score, categories, job-ad scanner, keyword to Skills, Copy Text, .txt), the STAR optimiser, the cover-letter generator, Markdown export, JSON Resume export and import (editor and dashboard), the jobs CSV, the starter templates, the 1-Page Fit / Balanced / Spacious presets (the store, the controls and the PDF margin) and the header icon picker. The mobile layout is covered in 26-mobile-layout. All 23 new tests pass. | Unverified |
| R2-162 | Tests | Low | verification-gap | ✅ Fixed | `372cbac`, `32bd830` | tests/unit/media-query.unit.mjs, cypress/e2e/26-mobile-layout.cy.js | The mobile layout, the hidden preview and usePanelResize failure paths have no real test; the useMediaQuery 'test' never calls the hook (`tests/unit/media-query.unit.mjs:1-14; cypress.config.js:52-53; cypress/support/e2e.js:4-6`) Repro: grep -rn 'viewport(' cypress tests → no hits. **Now:** **Now:** tests/unit/media-query.unit.mjs mounts useMediaQuery and useIsMobile over fake-dom, using a matchMedia fake that fires change events. It covers 375, 767, 768 and 1440 px, a resize across 768 px in both directions, a changed breakpoint (the old listener is removed), listener cleanup on unmount, and false with no window or no matchMedia. Mutating the hook turns the tests red. 26-mobile-layout.cy.js is the suite's first cy.viewport(375, 812) spec (5/5 pass). It covers the Edit tab (preview hidden and 'paused'), the Preview tab (PDF with the latest edits), no drag handle or layout toggle, Design and ATS tabs on a phone, live resizing to 1024 px and back, and a dashboard with no sideways scroll. The usePanelResize failure paths were left to their owner; they are already covered by tests/unit/panel-resize-storage.unit.mjs (R2-015). | Unverified |
| R2-163 | ats / tests | Low | verification-gap | ✅ Fixed | `034f46c`, `c873b88`, `d5e4c34` | tests/pdf/77-ats-score-printed.test.mjs, tests/pdf/77-ats-panel.test.mjs | No test covers the ATS panel's keyword flow, the title-order fix, or the score and plain text against what the PDF prints (`tests/unit/ats-checker.unit.mjs, tests/pdf/56-ats-layout-fix-buttons.test.mjs, tests/pdf/58-ats-standardize-headings.test.mjs`) Repro: n/a **Now:** New tests cover the score and the job match against the rendered PDF on every template, the panel's keyword flow and the title-order fix. Plain text against the PDF was already covered by tests/pdf/65-ats-text-parity.test.mjs. | Confirmed |
| R2-164 | PDF/Word assertions | Low | verification-gap | ✅ Fixed | `6ae3d50` | tests/pdf/parity/13-spacing.test.mjs, 22-photo-visibility.test.mjs, 31-section-overrides.test.mjs, 40-structure.test.mjs | Several controls' gate tests would still pass if the control did nothing: Spacing Override Before/After, photo hide eye, letter Show photo, rich-text Underline (PDF+Word) and bold/italic in Word, Grids beyond languages (`tests/pdf/51-section-spacing-override.test.mjs:37-47 (only compares clamped value against clamped value); tests/pdf/46-header-photo-text-gap.test.mjs:55,90 (hidden photo: only 'gap changes nothing'); tests/pdf/02-rich-text-parser.test.mjs:59 (underline parsed, never asserted in 03 or 04); tests/pdf/10-section-options.test.mjs:18-54 (Grids only for languages)`) Repro: n/a (coverage gap). **Now:** fixed in `6ae3d50`: each named control now fails its test if it does nothing — Spacing Override Before / After / Between items must move what they space by at least the change (`registry-sections.mjs` `override()`), the photo's eye must remove the photo, the letter's Show photo off prints no photo, Underline is asserted as a drawn line in the PDF and as `w:u` in the .docx with bold and italic, and Grids (`section.columns`) is checked on every section type that offers it, not only Languages. | Unverified |
| R2-165 | Preview · PdfPreview.jsx | Low | verification-gap | ✅ Fixed | `a9814a6`, `a972ebd` | tests/pdf/90-preview-mechanics.test.mjs, tests/pdf/preview-stub.mjs | Preview component mechanics (debounce, stale-render drop, zoom repaint, error + Retry) have no gate test; error/Retry has no test anywhere (`src/components/PdfPreview.jsx:96-140 (render effect, generation check), :186-191 (error + Retry)`) Repro: n/a (coverage gap). **Now:** The preview's mechanics now have gate tests that run all the way to pages on screen, using a stand-in pdf.js (PdfPreview._setPdfjsForTest) and builds the test finishes itself. They cover the typing debounce with pages up, the stale-render drop, the zoom repaint (and none while hidden), the error alert with its message, Retry clearing it, and a failure superseded by a newer change. | Unverified |
| R2-166 | ATS checker panel | Low | verification-gap | ✅ Fixed | `d5e4c34`, `9207af7` | tests/pdf/77-ats-panel.test.mjs | ATS tab buttons without behaviour tests: Put Job Title First, Copy plain text, Download plain text (`src/components/AtsCheckerPanel.jsx:74-85 (handleOptimizeExperienceOrder), :136-148 (copy/download)`) Repro: n/a (coverage gap). **Now:** Put Job Title First, Copy Text (success, denied, no clipboard) and the .txt download (its blob text and file name) now have click-through tests over the real store. | Unverified |
| R2-167 | Dashboard / ResumeCard / store actions | Low | verification-gap | ✅ Fixed | `8e26d02`, `f61af54` | tests/pdf/96-dashboard.test.mjs | Dashboard create, duplicate, rename, delete and import are tested only in Cypress, which the push gate never runs (`src/hooks/useResumeStore.js:153-197; cypress/e2e/01-dashboard.cy.js`) **Now:** The node gate now tests the real Dashboard and ResumeCards over the real store and router. New Résumé covers Start from Scratch, a role starter and New Cover; each opens the right route. Copy gives '<name> (Copy)' as a deep copy with a new id, without `keep`. Inline rename: Enter, Escape, a blank name, the same name, Save name and blur. Delete: the exact confirm, Cancel keeps the résumé, OK removes it and records deletedIds/deletedInfo for the sync; deleting every résumé leaves the empty dashboard. Import: a CPWT-CV backup (fresh id, file's keep ignored), a JSON Resume file (converted) and invalid files (the error shown, nothing imported). A demo account's Keep / Stop keeping / last-original Delete lock and ImportMenu are covered too. Each test would catch a regression in placement, ids, deep copy, the saved state, the confirm wording or the deletion record. | Unverified |
| R2-168 | Process / tracking | Low | verification-gap | ✅ Fixed | — | — | The push gate's hardening lives only as an uncommitted change in the memory store, and several tracking documents are stale or off master (`/mnt/Storage/my-learning/claude/flowcv/wip/pushgate.sh; PENDING-ALL.md:17-23,31-37; templates-ui-plan.md:188-194; branch templates-ui-audit (45f4acc)`) Repro: git -C /mnt/Storage/my-learning/claude status --short **Partly fixed (2026-09-24):** Push-gate half: superseded by CI (R2-154); the memory-store pushgate.sh hardening is moot, because the gate is .github/workflows/ci.yml and CLAUDE.md makes CI the only place tests run. Branch templates-ui-audit does not exist on origin (git ls-remote), but its commit 45f4acc ('Phase 1 audit complete') is an ancestor of origin/master, so its content is on the work branch. The stale tracking lines are handed to the coordinator in notes. **Left:** Coordinator: apply the docs/tracking edits listed in notes. **Now:** Push-gate half: superseded by CI (R2-154); the memory-store pushgate.sh hardening is moot, because the gate is .github/workflows/ci.yml and CLAUDE.md makes CI the only place tests run. Branch templates-ui-audit does not exist on origin (git ls-remote), but its commit 45f4acc ('Phase 1 audit complete') is an ancestor of origin/master, so its content is on the work branch. The stale tracking lines are handed to the coordinator in notes. The coordinator applied those edits: PENDING-ALL.md §0 (D1–D5 answered) and §1 (0 open in bug-status.md), templates-ui-plan.md §8 (the CI gate, claude/wf-* branches) and §9 (P1 audit ✅ 45f4acc). | Unverified |
| R2-169 | Housekeeping | Low | verification-gap | ✅ Fixed | `7741845`, `1f6a4cb`, `731ae2c` | tests/unit/knowledge-docs.unit.mjs, tests/pdf/11-photo*.test.mjs | Repo housekeeping: an obsolete conflicts TODO, stale knowledge docs, a stale tracked graphify-out, and undeclared or unused dependencies (D5) (`TODO_RESOLVE_CONFLICTS.md; docs/knowledge/05-state-auth-sync.md:11; docs/knowledge/06-templates-export.md:23-25; graphify-out/GRAPH_REPORT.md; package.json`) Repro: grep -n DATA_VERSION docs/knowledge/05-state-auth-sync.md src/utils/dataVersion.js **Now:** TODO_RESOLVE_CONFLICTS.md was already gone. graphify-out/ is no longer tracked (it is in .gitignore, and nothing read it). @napi-rs/canvas is a declared devDependency, so the photo paint tests always run and fail rather than skip if it is missing; file-saver, imported by nothing, is gone. docs/knowledge was checked claim by claim against src/, and tests/unit/knowledge-docs.unit.mjs fails when the stated DATA_VERSION, a named path or a relative link drifts. CI 36021843381 passed; confirmed by the reviewer. | Unverified |
| R2-170 | Preview · canvas memory | Low | verification-gap | ✅ Fixed | `b41d4ea`, `dad94c7` | tests/pdf/90-preview-canvas-memory.test.mjs | Replaced preview canvases are never shrunk to 0×0, a known way to hit iOS Safari's total canvas-memory cap (not reproduced on a device) (`src/components/PdfPreview.jsx:60-74 (paint), :76-88 (PageCanvas.replaceChildren), :141-149 (zoom repaint)`) Repro: Unverified. On an iPhone, open the Preview tab of a 2-page résumé and alternate edits with zoom +/− many times; watch for 'Total canvas memory use exceeds the maximum limit' in the Web Inspector and a 'Preview failed to render' alert. **Now:** Every canvas that leaves the screen (a newer render, a zoom repaint, unmount) or never reaches it (a stale render, a failed paint, a cancelled or overtaken zoom repaint) is shrunk to 0×0 at once instead of waiting for the garbage collector. Not checked on an iPhone. | Confirmed |
| R2-171 | sections · tests | Low | verification-gap | ✅ Fixed | `7dbc10e` | tests/pdf/96-visibility-exports.test.mjs, tests/pdf/96-month-picker.test.mjs, tests/pdf/96-sidebar-single-column.test.mjs | The gate has no test for reordering, visibility, deletes, MonthPicker writes, single-column section resolution, or all 11 types in the text and JSON exports (`tests/pdf/30-date-editor.test.mjs (display only); tests/pdf/10-section-options.test.mjs (5 templates, no sidebarSingleColumn case); tests/unit/markdown-export.unit.mjs, ats-checker.unit.mjs, json-resume-roundtrip.unit.mjs (no interests, references or custom)`) **Partly fixed (2026-09-24):** The export half is covered at HEAD: every one of the 11 section types (interests, references and custom included) is checked word for word against the PDF in Markdown (70-markdown-parity, R2-009) and the ATS text (65-ats-text-parity, R2-001), and goes both ways through JSON Resume, reprinting the same (json-resume-sections / json-resume-reprint, R2-002 / R2-006). **Left:** The editor half — tests for section reorder, visibility, delete, MonthPicker writes and the Sidebar's single-column resolution. Not taken here: the sections cluster is changing EditorResumeTab, the section editors and useResumeSectionActions, and app-shell / design-sidebar useResumeStore, so it belongs after those merge. **Now:** The editor half is covered. For each of the 11 section types, on Classic and on the Sidebar's two columns: hidden, it drops out of the PDF, Word, Markdown, ATS text and JSON Resume, heading included; a hidden section prints exactly what a deleted one prints; with every entry hidden no heading prints; shown again, every export matches the original. MonthPicker writes 'Apr', then 'Apr 2019' (either order); a stored '05/2023' with a new year picked becomes 'May 2024'; × clears the date. Picked dates print in every export and date format. 'Currently working here' writes endDate '' and every export prints Present, even when an imported entry has a leftover end date. The Sidebar's Single · ATS-safe layout prints every section in one column in stored order in the PDF and Word, while two columns split the six side types (SIDEBAR_COLUMN_TYPES / inSidebarColumn pinned). The Word export centres sections as Alignment says in Single, and only the main-column sections in two columns. **Left for later:** 96-sidebar-single-column checks Section Options → Alignment in Single mode only in Word, not in the PDF (the reviewer's note). | Confirmed |
