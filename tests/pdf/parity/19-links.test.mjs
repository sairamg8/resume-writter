// Parity matrix (how it works: matrix.mjs): every template × every control of the 'links' family the
// editor offers on it — Design → Links' Style (R2-147) at every value it offers, and its ↺ — each
// changes the PDF (= the preview) as registry-design.mjs says, loses no text and overlaps nothing.
import { parityFamily } from './run.mjs';

await parityFamily(['links']);
